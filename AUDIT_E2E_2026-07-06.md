# Báo cáo Audit end-to-end BigBike — 4 luồng nghiệp vụ cốt lõi

> **Ngày:** 2026-07-06
> **Phạm vi:** `bigbike-web` (website bán hàng) + `bigbike-admin` (quản trị) + `bigbike-backend` (máy chủ). Bốn luồng: (1) Đặt hàng–Thanh toán–Vận chuyển, (2) Tồn kho–Sản phẩm–Giá, (3) Đổi trả–Bảo hành–Khiếu nại, (4) Đăng nhập–Phân quyền–Tài khoản.
> **Phương pháp:** rà soát đa tác nhân có phản biện chéo — 8 luồng tìm × kiểm chứng độc lập từng phát hiện (30 tác nhân, ~480 lượt kiểm) + đối chiếu tài liệu chuẩn `docs/business` & `docs/engineering` + **truy vấn dữ liệu thật** trên stack Docker local.
> **Môi trường:** 6 container đang chạy healthy (backend :8080, web :3000, admin :4000, postgres :5432, minio :9000, redis :6379). Backend trỏ **DB dev nội bộ** (`jdbc:postgresql://postgres:5432/bigbike`), **KHÔNG đụng VPS**. Toàn bộ thao tác **chỉ đọc** (query SELECT, gọi API GET, đọc log) — không đặt đơn test, không ghi/sửa dữ liệu.
> **Mục tiêu:** TÌM VẤN ĐỀ, **không sửa code** trong lượt này.

---

## 1. Kết luận cho chủ shop

Các luồng cốt lõi **về cơ bản chạy đúng**: đặt hàng không tạo đơn trùng khi bấm hai lần, cộng tiền đúng (tổng đơn = cộng các dòng), email hỏng SMTP không làm gãy việc đặt hàng, phân quyền quản trị đúng theo cơ sở dữ liệu. **Không có lỗi Critical đang phá luồng** ngay lúc này.

Cần xử lý: **2 lỗi Cao (High)**, **3 lỗi Trung bình (Medium)**, ~**8 lỗi Thấp** (dọn dẹp / tài liệu lệch), cùng **6 quyết định phạm vi** chủ shop cần chốt. **4 phát hiện bị loại** sau kiểm chứng.

> **Lưu ý quan trọng:** hai lỗi Cao hiện **tiềm ẩn** (chưa hại khách hôm nay) vì dữ liệu hiện tại chưa kích hoạt — nhưng đường code sai sẽ bung ra ngay khi có biến thể được bật bán, hoặc có nhân viên vai trò quản lý đơn được tạo.

| Mức độ | Số lượng |
|---|---|
| 🔴 Cao (High) | **2** |
| 🟠 Trung bình (Medium) | **3** |
| 🟡 Thấp / dọn dẹp | **8** |
| 🟣 Chủ shop chốt (không phải lỗi) | **6** |
| ⚪ Loại sau kiểm chứng | **4** |

---

## 2. Bảng ưu tiên xử lý — nặng trước

| Mã | Luồng | Vấn đề | Mức | Kiểm thật |
|---|---|---|---|---|
| **I-01** | Đặt hàng · Giá | Giá biến thể **hiển thị trên web khác giá tính tiền** khi đặt (thu thiếu/thừa) — 56 biến thể / 6–7 SP lệch, 57 biến thể gắn nhãn "giảm giá" ảo | 🔴 Cao | ✓ query |
| **I-02** | Đơn · Phân quyền | Thông báo đơn **trực tiếp chết với vai trò Quản lý cửa hàng**; app quản trị nối lại vô hạn 4s; ADMIN bị thu quyền đọc đơn vẫn nhận luồng | 🔴 Cao | code |
| **I-03** | Vận chuyển | Đơn cũ (fulfillment trống) **nhảy thẳng "Đã giao" không cần "Đã gửi"/mã vận đơn** rồi hoàn tất — 1.657 đơn hở, 503 đơn dính đủ chuỗi | 🟠 TB | ✓ query |
| **II-02** | Tồn kho | Nút "còn hàng" biến thể **lệch trạng thái web** — 2 biến thể admin bật bán nhưng web báo hết hàng (không bán được) | 🟠 TB | ✓ query |
| **IV-01** | Phân quyền | Khoá/hạ quyền một admin **không hiệu lực ngay** — vẫn giữ quyền cũ tới ~15 phút | 🟠 TB | code |
| **III-01** | Đổi trả | Trang chi tiết đơn **ghi nhầm mã vận đơn thành "Mã RMA"** (tính năng đổi trả đã gỡ) | 🟡 Thấp | code |
| **III-02** | Đổi trả | Bộ lọc/nhãn **"Đã hoàn tiền" (REFUNDED)** còn trong admin nhưng máy chủ đã bỏ — lọc luôn ra 0 kết quả | 🟡 Thấp | ✓ query |
| **IV-04** | Đăng nhập | Màn đăng nhập admin **giấu thông báo khoá tài khoản** — bị khoá vẫn chỉ báo "sai thông tin" | 🟡 Thấp | code |
| **IV-03** | Phân quyền | Tài liệu phân quyền **thiếu dòng quyền "Thư viện ảnh"** + cổng super-admin xoá vĩnh viễn ảnh (code đúng, docs thiếu) | 🟡 Thấp | code |
| **I-04·I-05·III-04·II-03** | Đơn · Đổi trả · Kho | Tài liệu tự mâu thuẫn hoàn tất đơn chưa thanh toán; mẩu "REFUNDED/hoàn tiền" chết còn sót; thiếu chốt bật/tắt tồn kho cấp SP | 🟡 Thấp | 1 phần ✓ query |

**Ghi chú:** `✓ query` = đã tái hiện bằng truy vấn dữ liệu thật · `code` = xác nhận qua code (không đặt đơn/không ghi dữ liệu theo nguyên tắc chỉ-đọc; đã kèm cách tái hiện).

---

## 3. Chi tiết theo luồng

### Luồng 1 — Đặt hàng · Thanh toán · Vận chuyển

#### 🔴 I-01 — Giá biến thể hiển thị trên web ≠ giá thực sự tính tiền khi đặt
- **Mức độ:** Cao · **Kiểm:** ✓ tái hiện live (query + API GET); phần "tính tiền" xác nhận qua code (không đặt đơn thật).
- **Mô tả:** Trang sản phẩm hiển thị **giá của biến thể khách chọn**, nhưng giỏ hàng và bước đặt đơn **cố tình bỏ qua giá biến thể** và luôn tính theo giá sản phẩm cha. Khách thấy một giá, đơn ghi số tiền khác. Hiện **56 biến thể (6–7 SP) đang bán giá lệch**, tất cả trường hợp thấy được đều **thu thiếu** (mất 100.000–800.000đ/SP); code tương tự sẽ **thu thừa** nếu biến thể rẻ hơn cha. Ngoài ra **57 biến thể gắn nhãn "-% giảm giá" ảo** (gạch giá gốc) dù không thực sự giảm.
- **Tiềm ẩn:** 56 biến thể lệch giá đang để "hết hàng" nên chưa mua được — nhưng nhãn giảm giá ảo khách đã thấy, và code sai với **mọi** biến thể có giá riêng khi được bật bán.
- **Bằng chứng:** Web hiển thị giá biến thể `bigbike-web/components/wp/WpPurchaseSection.tsx:117`, nhãn giảm giá `lib/pricing.ts:24-31`. Backend trả giá riêng biến thể `JpaCatalogReadRepository.java:1140-1147`. Tính tiền bỏ qua biến thể `CheckoutSupport.java:246-254` + `CartService.java:306-310` (dùng `product.salePrice ?? product.retailPrice`, tham số biến thể không dùng). Docs `DATA_CONTRACT.md:130` (giá biến thể là dữ liệu chính thức). **Live:** `GET /api/v1/products/giap-bao-ho-tay-vai-lung-d30/snapshot` → web hiện 1.150.000đ, đặt đơn tính 350.000đ (−800.000); query DB: đúng **56** biến thể PUBLISHED lệch giá.
- **Hướng xử lý:** Cho hiển thị và tính tiền **dùng chung một nguồn giá** (web ngừng đọc giá biến thể, hoặc đặt đơn tính đúng giá biến thể — docs coi giá biến thể là chính thức, nghiêng về sửa phía đặt đơn). Dọn 197 dòng giá biến thể cũ từ WordPress để hết nhãn giảm giá ảo. Thêm kiểm tra bất biến "giá hiện == giá tính".

#### 🔴 I-02 — Thông báo đơn trực tiếp chết với vai trò "Quản lý cửa hàng"; app nối lại vô hạn
- **Mức độ:** Cao · **Kiểm:** qua code (chưa có tài khoản Quản lý cửa hàng để test live). Còn tồn từ audit 2026-07-05, đã kiểm lại vẫn nguyên.
- **Mô tả:** Kênh thông báo trực tiếp đẩy "đơn mới / đổi trạng thái" chỉ chấp nhận **ADMIN / SUPER_ADMIN**. Nhưng **Quản lý cửa hàng** (SHOP_MANAGER) — vai trò chuyên xử lý đơn — bị chặn ngay khi kết nối → **không bao giờ nhận thông báo đơn mới**; app quản trị **thử kết nối lại mỗi 4 giây, vô hạn**. Chiều ngược lại chỉ kiểm quyền lúc kết nối, không kiểm lúc đăng ký kênh → ADMIN đã bị **thu quyền đọc đơn vẫn nhận dữ liệu đơn**.
- **Tiềm ẩn:** hiện chỉ có 3 ADMIN + 1 SUPER_ADMIN, **chưa có tài khoản Quản lý cửa hàng nào** → chưa ai ảnh hưởng. Tạo một nhân viên vai trò đó là lỗi bung ngay.
- **Bằng chứng:** `WebSocketConfig.java:76-79` (chặn nếu role ≠ ADMIN/SUPER_ADMIN, không hỏi quyền `orders.read`); SHOP_MANAGER seed `orders.read+orders.write` tại `V49__create_roles_permissions_tables.sql:54`; app nối lại `adminWebSocket.js:5,101-109`; kết nối cho mọi user `App.jsx:247-249`. Docs `PERMISSION_MATRIX.md:130`.
- **Hướng xử lý:** Cổng kết nối kiểm theo **quyền `orders.read` lấy từ DB** thay vì tên vai trò cứng; thêm kiểm quyền ở bước đăng ký kênh; thêm giãn thời gian + ngừng thử lại khi bị từ chối xác thực.

#### 🟠 I-03 — Đơn có thể bị đánh dấu "Đã giao" mà không qua "Đã gửi" và không có mã vận đơn
- **Mức độ:** Trung bình · **Kiểm:** ✓ query (số liệu), chuỗi khai thác xác nhận qua code (cần thao tác ghi để chạy end-to-end).
- **Mô tả:** Máy chủ chỉ kiểm chuỗi trạng thái vận chuyển hợp lệ khi đơn **đã có** trạng thái vận chuyển. Có **1.657 đơn giao hàng cũ đang để trống** trạng thái vận chuyển. Với các đơn này, admin có thể chuyển thẳng sang **"Đã giao"**, bỏ qua "Đang xử lý → Đã gửi" và **bỏ yêu cầu nhập mã vận đơn**, rồi **hoàn tất đơn** — phá nguyên tắc "phải có mã vận đơn trước khi hoàn tất". Trong đó **503 đơn đang "Đang xử lý"** dính đủ chuỗi khai thác.
- **Bằng chứng:** `AdminOrderService.java:497-509` (kiểm chuyển trạng thái nằm trong `if (current != null)`), `:516-520` (chỉ bắt mã vận đơn ở nhánh "SHIPPED"), `:642-654` (hoàn tất chỉ kiểm "DELIVERED"). Docs `STATE_MACHINES.md:429`. **Live:** query DB → 1.657 đơn giao-hàng trạng-thái-trống, 503 đơn "Đang xử lý".
- **Hướng xử lý:** Coi trạng thái trống như "Chưa xử lý" trước khi kiểm chuyển tiếp (chạy kiểm không điều kiện), để đơn cũ cũng phải qua "Đã gửi" + mã vận đơn rồi mới "Đã giao".

#### 🟡 I-04 / I-05 — Docs mâu thuẫn hoàn tất đơn chưa thanh toán · mẩu "REFUNDED" chết trong máy chủ
- **Mức độ:** Thấp · **Kiểm:** ✓ query (không có đơn chưa-thanh-toán-đã-hoàn-tất) + code.
- **Mô tả:** **I-04:** Docs vừa nói đơn "Chưa thanh toán" **được** hoàn tất (quyết định chủ shop 2026-06-23), vừa có dòng khác nói **không được** và viện dẫn chốt chặn "không tồn tại trong code". Code thực tế cho hoàn tất. Không ảnh hưởng vận hành (605 đơn đã hoàn tất đều đã thanh toán) nhưng gây hiểu sai lần sửa sau. **I-05:** Nhánh xử lý trạng thái thanh toán còn kiểm "REFUNDED" (đã bỏ) — không bao giờ chạy tới, nhưng nếu chạy sẽ báo nhầm chữ "thất bại".
- **Bằng chứng:** I-04: `STATE_MACHINES.md:266/276` (cho) vs `:277` (cấm) + ghi chú cũ `AdminOrderService.java:639-640`; code `:642-654` không kiểm thanh toán. I-05: `AdminOrderService.java:399` (còn `|| "REFUNDED"`), enum `OrderStatus.java` không có REFUNDED; `OrderJpaRepository.java:157` comment cũ.
- **Hướng xử lý:** Sửa docs cho khớp quyết định 2026-06-23; gỡ mẩu `|| "REFUNDED"` và comment cũ.

**✅ Đã kiểm — chạy đúng (Luồng 1):**
- **Không tạo đơn trùng** khi bấm đặt hai lần: khoá chống-trùng ở DB (`uk_checkout_idempotency_flow_scope_key`) + savepoint + so mã yêu cầu; web gửi mã idempotency ổn định.
- **Cộng tiền đúng:** tổng đơn = cộng dòng hàng + phí + ship − giảm giá, trên toàn bộ đơn mới (BB-). COD → "Đang xử lý/Chưa thanh toán", chuyển khoản → "Tạm giữ", khớp docs.
- **Email hỏng không gãy đặt hàng:** gửi mail chạy nền, sau khi đơn đã lưu, nuốt lỗi — SMTP trống chỉ khiến email bị bỏ qua, đơn vẫn tạo. Link email dùng `siteBaseUrl` (mặc định `https://bigbike.vn`) — `OrderNotificationService.java:63-64`.
- **Sản phẩm nháp không lộ ra web** (trang chi tiết + snapshot đều trả 404 cho hàng nháp).

---

### Luồng 2 — Tồn kho · Sản phẩm · Giá

> Lỗi giá biến thể **I-01** ở trên cũng thuộc luồng này (hai luồng tìm độc lập cùng phát hiện — độ tin cậy cao).

#### 🟠 II-02 — Nút "còn hàng" biến thể lệch trạng thái web — 2 biến thể bật bán nhưng web báo hết
- **Mức độ:** Trung bình · **Kiểm:** ✓ query.
- **Mô tả:** Trạng thái "còn/hết" bật ở màn tồn kho và trạng thái hiển thị công khai **có thể lệch nhau**. Ở mũ fullface carbon (`wp-prod-36322`), 2 biến thể size S và L đang bật "còn hàng" nhưng trạng thái web là **"hết hàng"** → 2 size này **không bán được**, trong khi người vận hành nhìn màn tồn kho thấy nút bật ON. Nguyên nhân: trạng thái hiển thị chỉ đồng bộ khi bấm nút bật/tắt; dữ liệu nhập WordPress seed lệch sẵn, sau đó không có gì đồng bộ lại.
- **Phạm vi:** cả SP này đang "hết hàng" toàn bộ nên khách thấy đúng — thiệt hại là **hướng vận hành** (admin tưởng đang bán 2 size thực ra bán không được).
- **Bằng chứng:** **Live:** query DB → `wp-var-36422/36423` `is_available=t` nhưng `stock_state=OUT_OF_STOCK`; toàn hệ thống 16 biến thể bật vs 14 "còn hàng" (đúng 2 lệch). `InventoryPolicyService.java:20-24` chỉ chạy từ nút bật/tắt (`AdminInventoryService.java:214-245`); `AdminInventoryMapper.java:62,81-84` (anyAvailable vs aggregateState mâu thuẫn). Docs STOCK_RULE.
- **Hướng xử lý:** Chạy đồng bộ một lần (và khi nhập hàng loạt) để "còn hàng" và trạng thái hiển thị không bao giờ lệch; hoặc màn tồn kho lấy trạng thái hiển thị làm nguồn duy nhất.

#### 🟡 II-03 — Nút bật/tắt tồn kho cấp sản phẩm thiếu chốt chặn với sản phẩm có biến thể
- **Mức độ:** Thấp (tiềm ẩn) · **Kiểm:** code + query (0 trường hợp lệch hiện tại).
- **Mô tả:** Bật/tắt tồn kho cấp **sản phẩm** đặt thẳng trạng thái mà không kiểm SP có biến thể hay không. Nếu gọi nhầm trên SP có biến thể sẽ ghi đè trạng thái tổng và lệch với biến thể con. Hiện **chưa có dữ liệu lệch** (giao diện kỳ vọng chỉ hiện nút này cho SP không biến thể) → lỗ hổng qua API.
- **Bằng chứng:** `AdminInventoryService.java:248-272`; trigger DB chỉ chạy khi biến thể đổi; query DB: 0 SP lệch. Docs STOCK_RULE_006/008.
- **Hướng xử lý:** Từ chối (hoặc chuyển sang tính lại theo biến thể) khi gọi bật/tắt cấp SP trên SP có biến thể.

**✅ Đã kiểm — chạy đúng / không phải lỗi (Luồng 2):**
- **Quy tắc ảnh phải nằm trong MinIO nay đã siết đầy đủ ở đường sản phẩm** — lỗ hổng M6/M7/M8 của audit 2026-07-05 **đã được vá**: ảnh gallery, thumbnail video, link video, khối ảnh mô tả, ảnh HTML, ảnh SEO đều được kiểm (`CatalogRequestValidator.java`).
- **Khuyến mãi hết hạn tự ẩn:** hệ thống **không** có ngày bắt đầu/kết thúc khuyến mãi — giá là 2 ô (giá gốc + giá bán), chỉnh tay theo thiết kế (PRODUCT_RULE_012). Không có "sale hết hạn còn hiện" vì không có hạn. Nên ghi rõ vào tài liệu.

---

### Luồng 3 — Đổi trả · Bảo hành · Khiếu nại

> **Khoảng trống phạm vi có chủ đích**, không phải luồng đang chạy: không có bảng dữ liệu đổi trả/hoàn tiền/bảo hành, không có REFUNDED/RETURNED trong dữ liệu thật. Phần cần xử lý là **mẩu cũ còn sót** + vài **quyết định phạm vi**.

#### 🟡 III-01 — Trang chi tiết đơn ghi nhầm mã vận đơn thành "Mã RMA / RMA #"
- **Mức độ:** Thấp · **Kiểm:** code. Còn tồn từ audit admin trước, đã kiểm lại vẫn nguyên.
- **Mô tả:** Ô mã vận đơn được gắn nhãn **"Mã RMA"** (mã đổi trả) — tính năng đổi trả đã gỡ toàn hệ thống. Người vận hành thấy mã vận đơn bị dán nhãn của quy trình không còn tồn tại.
- **Bằng chứng:** `OrderDetailScreen.jsx:631-636` (key `orders.detail.colRma`) + `vi.json:1128` "Mã RMA" / `en.json:1128` "RMA #".
- **Hướng xử lý:** Đổi nhãn thành "Mã vận đơn / Tracking #".

#### 🟡 III-02 / III-04 — Mẩu "Đã hoàn tiền (REFUNDED)" và trường hoàn tiền còn sót trong quản trị
- **Mức độ:** Thấp · **Kiểm:** ✓ query (III-02: 0 đơn REFUNDED trong DB).
- **Mô tả:** **III-02:** Danh sách đơn còn cho lọc "Đã hoàn tiền" cho cả trạng thái đơn lẫn thanh toán, có huy hiệu màu, có trong bộ chuẩn hoá — máy chủ đã bỏ (đơn hoàn tiền cũ chuyển "Đã huỷ"). Chọn bộ lọc đó **luôn ra 0 kết quả**. **III-04:** Bộ chuẩn hoá đơn còn đọc 3 trường hoàn tiền (số tiền/lý do/thời điểm) mà máy chủ đã bỏ cột — luôn rỗng.
- **Bằng chứng:** III-02: `OrderListScreen.jsx:26-27`, `StatusBadge.jsx:11,19`, `contracts.js:690,693`; enum backend không có REFUNDED; **query DB:** 0 đơn REFUNDED. III-04: `contracts.js:823-825`; docs `DATA_CONTRACT.md:222`.
- **Hướng xử lý:** Gỡ REFUNDED khỏi bộ lọc/huy hiệu/chuẩn hoá và 3 trường hoàn tiền; xoá chuỗi ngôn ngữ tương ứng.

#### 🟣 III-05 — Trạng thái "RETURNED" vẫn được máy chủ chấp nhận trong khi đổi trả đã gỡ *(chủ shop chốt)*
- **Mô tả:** Máy chủ vẫn giữ "RETURNED" như một trạng thái giao hàng (cho phép "Đã gửi → RETURNED", "Đã giao → RETURNED"), còn giao diện admin **không có nút nào kích hoạt** (nhánh xử lý chết). Docs mập mờ: mục trạng thái giao hàng ghi "có hiệu lực", mục khác ghi cả tính năng đổi trả đã gỡ. Dữ liệu thật: chưa từng dùng RETURNED.
- **Cần chốt:** RETURNED là **trạng thái giao hàng bị hoàn về của bưu kiện** (giữ lại + thêm nút) hay **tàn dư của đổi trả** (gỡ hẳn).
- **Bằng chứng:** `FulfillmentStatus.java:8`, `AdminOrderService.java:117,127-130`; nhánh UI chết `OrderDetailScreen.jsx:237-239,282`. Docs mâu thuẫn: `STATE_MACHINES.md:409-423` vs `§10 :523`.

#### 🟣 III-06 — Bảo hành & Khiếu nại — khoảng trống phạm vi đã descope *(chủ shop chốt)*
- **Bảo hành:** chỉ có **trang chính sách tĩnh** (`WarrantyPolicyContent`) — không có luồng tra cứu/yêu cầu bảo hành trong hệ thống (đã gỡ V266). Đúng thiết kế, không phải lỗi.
- **Kênh khiếu nại/hỗ trợ khách:** chủ shop đã đánh dấu **ngoài phạm vi (2026-07-06)** — khách dùng hotline/Zalo/Facebook ở trang liên hệ tĩnh; không có form/ticket. Khoảng trống tuân thủ (NĐ 85/2021) chấp nhận xử lý thủ công. Xác nhận: không có endpoint liên hệ trong máy chủ.

---

### Luồng 4 — Đăng nhập · Phân quyền · Tài khoản

#### 🟠 IV-01 — Khoá / hạ quyền / thu quyền một admin không có hiệu lực ngay — trễ tới ~15 phút
- **Mức độ:** Trung bình · **Kiểm:** code (không có tài khoản bị khoá/ngưng để test live). Giải đáp mục docs `STATE_MACHINES.md:791` "cần xác minh".
- **Mô tả:** Mỗi lần gọi API, hệ thống chỉ tin vào "vé" đăng nhập (token) đã cấp: lấy vai trò từ token, không kiểm lại tài khoản còn hoạt động/còn vai trò không. Khi chủ shop **khoá/tạm ngưng một admin đang gây hại/đã nghỉ, hoặc hạ quyền họ**, người đó vẫn giữ nguyên quyền cũ (kể cả thao tác nguy hiểm) tới khi vé hết hạn — **tối đa 15 phút**. (Lần gia hạn vé mới có kiểm lại → cửa sổ rủi ro giới hạn ở 15 phút.)
- **Làm rõ:** **Thu hồi một quyền khỏi một vai trò thì có hiệu lực ngay** (bộ nhớ tạm được xoá, đọc DB trực tiếp). Lỗ hổng chỉ ở thay đổi **trạng thái/vai trò của từng người**. Phía **khách hàng không** có lỗ hổng này (kiểm trạng thái mỗi request → khách bị chặn mất quyền ngay).
- **Bằng chứng:** `JwtAuthFilter.java:36-51` (dựng người dùng từ token, không tra DB), `DevAdminAuthService.java:67-79` (quyền theo vai trò trong token, gán cứng "ACTIVE"); đăng nhập/gia hạn có kiểm `AdminAuthService.java:73,112`; vé sống 900s `application.properties:62`. Đối chiếu khách làm đúng: `CustomerSessionFilter.java:55-56`. Không có Redis/không có cơ chế thu hồi vé trong máy chủ.
- **Hướng xử lý:** Kiểm lại trạng thái tài khoản admin ở mỗi request (như phía khách), hoặc thêm cơ chế vô hiệu vé (token-version) khi khoá/hạ quyền. Cập nhật docs `:791` thành đã xác minh.

#### 🟡 IV-04 — Màn đăng nhập admin giấu thông báo khoá tài khoản
- **Mức độ:** Thấp · **Kiểm:** code.
- **Mô tả:** Sau 5 lần sai mật khẩu, máy chủ khoá tài khoản 15 phút và trả thông báo rõ ("tạm khoá do nhập sai nhiều lần, thử lại sau"). Nhưng màn đăng nhập **vứt bỏ thông báo đó**, coi mọi lỗi 401 là "sai thông tin". Người bị khoá — kể cả khi gõ **đúng** mật khẩu — vẫn chỉ thấy "sai thông tin", tưởng quên mật khẩu. Chốt chặn khoá phía máy chủ vẫn đúng — đây là lỗi trải nghiệm.
- **Bằng chứng:** `LoginScreen.jsx:32-34`; máy chủ phân biệt hai ca `AdminAuthService.java:55-59` vs `:69`; tham số khoá `AdminLoginAttemptService.java:26-28` (5 lần / 15 phút).
- **Hướng xử lý:** Hiển thị đúng thông báo khoá của máy chủ cho ca 401-bị-khoá.

#### 🟡 IV-03 — Tài liệu phân quyền thiếu quyền "Thư viện ảnh" và cổng xoá vĩnh viễn
- **Mức độ:** Thấp (docs) · **Kiểm:** code.
- **Mô tả:** Trong code, **xoá vĩnh viễn ảnh** và xoá hàng loạt yêu cầu quyền super-admin; các thao tác ảnh khác cần `media.read/write` — đúng và chặt. Nhưng **tài liệu phân quyền không có dòng "Thư viện ảnh"**, mục "chỉ super-admin" cũng không nhắc cổng xoá vĩnh viễn ảnh.
- **Bằng chứng:** `AdminMediaController.java:172` (`requirePermission("*")`), `:244` (permanent → "*"); `PERMISSION_MATRIX.md` không có dòng media.
- **Hướng xử lý:** Thêm dòng quyền "Thư viện ảnh" và ghi cổng "*" cho xoá vĩnh viễn/xoá hàng loạt vào tài liệu.

**✅ Đã kiểm — chạy đúng (Luồng 4):**
- **Phân quyền đúng chuẩn cơ sở dữ liệu** (qua `requirePermission`) — không có kiểm quyền cứng `@PreAuthorize` trong bất kỳ controller quản trị nào (cả 25 controller); mọi thao tác ghi đều có chốt quyền; khoá quyền khớp seed DB.
- **Thu hồi một quyền khỏi vai trò có hiệu lực ngay.**
- **Mật khẩu băm Argon2id** (tự nâng cấp mật khẩu cũ), **khoá chống dò mật khẩu** (5 lần/15 phút), **xoay và thu hồi vé gia hạn** khi gia hạn/đăng xuất, thông báo lỗi chống dò email; **đăng nhập Google/Facebook chống chiếm tài khoản** (chỉ tin email nhà cung cấp đã xác thực).
- **Khách bị chặn mất quyền ngay lập tức** (kiểm trạng thái mỗi request).

---

## 4. Cần chủ shop / đội dev chốt (không tự suy diễn)

- **Bán vượt tồn kho là quyết định có chủ đích:** hệ thống **không trừ kho khi bán** (quyết định 2026-06-23, tồn kho là công tắc còn/hết, không phải số lượng — `CheckoutService.java:162,269`; `BUSINESS_RULES.md:98` STOCK_RULE_007). Nhiều khách mua "món cuối" cùng lúc đều đặt thành công; web vẫn nhận đơn tới khi admin tự bấm "hết hàng". **Không phải lỗi code** — nhưng là **rủi ro vận hành lớn nhất** của luồng đặt hàng; chủ shop nên xác nhận còn chấp nhận, hay muốn thêm cơ chế giữ/trừ kho.
- **RETURNED giữ hay gỡ** (III-05) — trạng thái giao hàng bị hoàn về, hay tàn dư đổi trả.
- **Kênh khiếu nại khách** (III-06) — hiện descope, xử lý thủ công; xác nhận đủ tuân thủ NĐ 85/2021.
- **Bắt xác thực email trước khi khách đặt hàng / đánh giá?** Hiện **không bắt buộc** (đánh giá nhận ẩn danh — `PublicReviewController.java:54-71`; đăng nhập không đòi email đã xác thực). Docs không quy định rõ.
- **Ảnh cũ trỏ `media.bigbike.vn`:** ~211/217 ảnh sản phẩm + 943/959 ảnh gallery là link tuyệt đối tới host sản xuất (không phải kho MinIO môi trường này). Được chấp nhận như dữ liệu cũ (đọc-fallback §14.3). Xác nhận `media.bigbike.vn` có phải cổng MinIO chính thức; nếu có, nên đưa vào whitelist hoặc chuyển link về dạng `/media/`.
- **Hoá đơn điện tử (NĐ 123/2020):** vẫn chưa có (đã ghi trong docs từ trước) — quyết định phạm vi + nhà cung cấp.

---

## 5. Đã loại sau kiểm chứng — minh bạch

- **"Bán vượt tồn kho là lỗi"** → loại: đúng thiết kế theo docs (quyết định chủ shop); đã chuyển thành mục rủi ro cần xác nhận (mục 4).
- **"Sổ xuất-nhập kho tính sai"** → loại: 4 dòng lệch là dữ liệu cũ trước 2026-06-23, code hiện **không còn ghi** sổ này (dormant theo docs), giao diện cũng không hiển thị.
- **"Gần như toàn bộ ảnh không nằm trong MinIO"** → loại như lỗi: là dữ liệu cũ được chấp nhận (đọc-fallback §14.3); chuyển thành mục xác nhận host (mục 4).
- **"RETURNED mâu thuẫn tài liệu = lỗi"** → loại phần khung "mâu thuẫn": §10 gỡ **tính năng** đổi trả + trạng thái REFUNDED, không gỡ trạng thái giao hàng RETURNED; phần còn lại (nhánh UI chết + docs mập mờ) giữ ở III-05.

---

## 6. Phần chưa/không kiểm được — ghi rõ, không tạo cảm giác "đã phủ hết"

- **Không đặt đơn thật / không ghi dữ liệu** (nguyên tắc chỉ-đọc) → các lỗi **I-01, I-02, I-03, IV-01** xác nhận bằng **code + query**, chưa chạy end-to-end bằng thao tác ghi; đã kèm cách tái hiện chính xác.
- **Không gửi email thật** (SMTP trống — `BIGBIKE_MAIL_HOST` rỗng) → đường gửi mail xác nhận đã kiểm bằng code (chạy nền, dùng `siteBaseUrl`), chưa quan sát email thực tế.
- **Không chạy đua tương tranh (concurrency) thật** → phân tích qua code + mô hình dữ liệu (tồn kho là boolean → bán vượt là by-design).
- **Không có tài khoản Quản lý cửa hàng / bị khoá / bị ngưng để test live** → **I-02** và **IV-01** hiện tiềm ẩn trong môi trường này (chỉ 4 tài khoản ADMIN/SUPER_ADMIN, đều ACTIVE; 1951 khách đều ACTIVE).
- **Đăng nhập Google/Facebook** đã nối đủ trong code nhưng chưa chạy vòng thật (cần vòng chuyển hướng nhà cung cấp).
- **Hiển thị trạng thái tồn kho trên trình duyệt web** chưa lái trực tiếp (suy ra từ cột dữ liệu DB).
- **App mobile** ngoài phạm vi (chỉ web/admin/backend theo yêu cầu).
- **Các lỗi ngoài 4 luồng** (vd C2/C3 của audit 2026-07-05 về ẩn/hiện danh mục, thùng rác tin tức) không thuộc phạm vi lần này — chưa kiểm lại.

---

*Rà soát đa tác nhân có phản biện chéo (8 luồng tìm × kiểm chứng độc lập từng phát hiện; 30 tác nhân, ~480 lượt kiểm) + đối chiếu tài liệu chuẩn `docs/business` & `docs/engineering` + truy vấn dữ liệu thật trên stack Docker local (chỉ đọc). Mọi phát hiện kèm `file:dòng`, trích dẫn tài liệu, hoặc query thật để đội dev tái hiện & sửa. Không sửa code trong lượt audit này.*
