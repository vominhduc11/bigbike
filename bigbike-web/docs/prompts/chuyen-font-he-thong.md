# Historical prompt — đã bị thay thế

> **KHÔNG THỰC THI.** Đây là prompt lịch sử. Chính sách hiện tại đã chuẩn hóa toàn bộ
> `bigbike-web` về Arial/Helvetica và nội dung thao tác cũ không còn là checklist.

Source of truth hiện tại là [`../TYPOGRAPHY.md`](../TYPOGRAPHY.md):

- Nhóm A1–A5 dùng Arial/Helvetica và sentence case.
- Nhóm B1–B5 dùng Arial/Helvetica và IN HOA.
- Nhóm D chỉ dành cho chữ `404` trang trí bằng `clamp()`.
- Cỡ chữ chỉ đổi một lần tại `768px`.

File này chỉ lưu dấu một quyết định typography cũ; không phải spec, checklist hay
prompt có thể chạy lại.
