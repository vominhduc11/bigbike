# Historical prompt — đã bị thay thế

> **KHÔNG THỰC THI.** Prompt cũ từng đề xuất chuyển toàn bộ `bigbike-web` sang
> Arial/Helvetica. Quyết định đó không còn hiệu lực và nội dung thao tác cũ đã được
> gỡ để tránh mâu thuẫn với hệ typography hiện tại.

Source of truth hiện tại là [`../TYPOGRAPHY.md`](../TYPOGRAPHY.md):

- Nhóm A1–A5 dùng Arial/Helvetica và sentence case.
- Nhóm B1–B5 dùng Barlow Condensed và IN HOA.
- Nhóm D chỉ dành cho chữ `404` trang trí bằng `clamp()`.
- Cỡ chữ chỉ đổi một lần tại `768px`.

File này chỉ lưu dấu rằng yêu cầu “gỡ toàn bộ Barlow” từng tồn tại trong lịch sử;
không phải spec, checklist hay prompt có thể chạy lại.
