package com.bigbike.bigbike_backend.domain.catalog;

/** A single product FAQ entry — question/answer pair rendered in the PDP FAQ section. */
public record ProductFaq(String question, String answer) {
}
