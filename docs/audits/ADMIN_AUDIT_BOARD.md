# Bảng theo dõi audit bigbike-admin

Cập nhật lần cuối: 2026-08-29

| Module | Màn hình | Đợt | Trạng thái | Ai đang làm | Ngày | Vitest | E2E | Ghi chú / nợ lại |
|---|---|---:|---|---|---|---|---|---|
| Đơn hàng | OrderList + OrderDetail | 1 | ✅ Xong | codex | 2026-07-28 | ✅ 39 test; toàn bộ admin 327/327 | ✅ 2 test | Đã áp dụng đủ 3 quyết định của chủ shop; 91 kiểm thử backend riêng đạt, OpenAPI đã khớp; kiểm thử migration tự động bị bỏ qua cục bộ do Testcontainers không kết nối được Docker Desktop, nhưng đã đối chiếu dữ liệu thật chỉ đọc và giữ test cho CI |
| Sản phẩm | ProductList + ProductDetail | 1 | ✅ Xong | — | 2026-07-28 | ✅ 33 test; toàn bộ admin 335/337, 2 timeout ngoài Product | ✅ 5 test | Đã áp dụng đủ 3 quyết định dữ liệu của chủ shop; 114 kiểm thử backend theo phạm vi đạt, build/guard/hygiene đạt, dữ liệu E2E đã dọn sạch; `VariantSkuConflictTest` chưa chạy được logic do Testcontainers Npipe 400 nhưng compile và test discovery đạt; ảnh 3 viewport hoãn vì container `:4000` đang dùng image cũ không bind source |
| Khách hàng | CustomerList + CustomerDetail | 1 | ✅ Xong | — | 2026-07-28 | ✅ 31 test; toàn bộ admin 353/353 | ✅ 6 test; 8 ảnh ở 4 viewport | Đã áp dụng toàn bộ quyết định của chủ shop; 95 kiểm thử backend riêng đạt, OpenAPI/build/guard/hygiene đạt; bổ sung 2 test bảo đảm rollback không xóa nhầm ảnh đại diện và object chỉ được dọn sau commit; dữ liệu thật chỉ được đối chiếu dạng tổng hợp, không đọc PII và không ghi DB; còn nợ P2 tối ưu truy vấn chuẩn hóa số điện thoại cũ khi quy mô khách tăng |
| Đánh giá | ReviewList + ReviewDetail | 1 | ✅ Xong | — | 2026-07-28 | ✅ 35 test; toàn bộ admin 382/382 | ✅ 8 test; ảnh 5 viewport | Đã áp dụng đủ quyết định của chủ shop (`REVIEW_RULE_009`/`010`/`011`): vòng đời duyệt có kiểm soát, thùng rác khôi phục được, xóa vĩnh viễn chỉ `SUPER_ADMIN` từ thùng rác, mọi ghi đều cần `expectedVersion`, email báo duyệt chỉ gửi một lần sau commit, audit không lưu email/nội dung/URL ảnh, ảnh claim nguyên tử qua bảng claim + job dọn upload mồ côi sau 24 giờ. 113 kiểm thử máy chủ theo phạm vi đạt (81 Phase1NReviews + 14 PublicReview + 1 migration + 17 OpenAPI contract); web 10 kiểm thử đạt; build/guard/hygiene đạt |
| Danh mục | CategoryList + CategoryDetail | 2 | ✅ Xong | — | 2026-07-28 | ✅ 20 test (mới: List 8 + Detail 9 + payload +3) | ✅ 1 test; ảnh 9 tấm (chi tiết/cây/danh sách ×3 viewport) | Vá 2 lỗi thật: (1) ảnh banner desktop/mobile không bị kiểm kho ảnh nội bộ ở máy chủ (form đã kiểm định dạng nhưng máy chủ bỏ sót) — lưu được ảnh hero trỏ host ngoài; (2) không xoá được mô tả + khối giới thiệu cũ (payload bỏ khoá khi rỗng, lệch với bản EN gửi null). Bổ sung Vitest 2 màn (trước đây trống): vòng đời 2 cờ độc lập, khóa danh mục hệ thống, xem-trước-ảnh-hưởng trước xóa vĩnh viễn, khóa ghi theo Thùng rác/quyền, song ngữ bắt buộc tên EN. Đồng bộ e2e với bản dựng lại màn chi tiết (công tắc Switch, menu thao tác dòng, nhãn "Ẩn khỏi website"). Docs: `MEDIA_RULE_002` + mục Categories `API_CONTRACT.md`. 2 kiểm thử máy chủ mới đạt; toàn bộ admin 424/424 |
| Thương hiệu | BrandList + BrandDetail | 2 | ✅ Xong | — | 2026-07-28 | ✅ 20 test; toàn bộ admin 390/390 | ✅ 1 test; ảnh 6 tấm ở 3 viewport | Sửa 5 lỗi thật: (1) chữ thay-thế-ảnh (alt) của logo/banner bị xoá mỗi lần lưu; (2) không xoá được mô tả cũ; (3) ảnh banner desktop/mobile không bị kiểm kho ảnh nội bộ ở cả form lẫn máy chủ — lưu được ảnh trỏ ra ngoài dù web render làm ảnh bìa; (4) thông báo chặn xoá vĩnh viễn còn tiếng Anh; (5) dải "Vừa xem gần đây" ghi mà không màn nào đọc. Dọn 3 khoá dịch chết (banner mobile web không render). Docs: `MEDIA_RULE_002` + mục Brands của `API_CONTRACT.md`. OpenAPI: bổ sung `UpsertBrandRequest`/`BrandTranslations`, tham số `id` thiếu ở PATCH/DELETE/restore/permanent, tham số `lang`, và các mã lỗi 400/403/404/409 — kiểm thử contract 17/17 đạt. Kiểm thử máy chủ mới 3 ca đạt |
| Sản phẩm nổi bật | FeaturedProducts | 2 | ✅ Xong | — | 2026-07-29 | ✅ 14 test module; toàn bộ admin 570/570 | ✅ 1 test; ảnh 3 viewport | Áp dụng 2 quyết định của chủ shop: (1) `PRODUCT_RULE_015` — sản phẩm rời trạng thái Đang bán thì tự rời khối nổi bật (`homepageBlock=NONE`, `homepageOrder=null`) ở cả 4 đường ghi; đăng bán lại không tự quay lại; (2) giữ nguyên quyền xem (chỉ người sửa được sản phẩm). Sửa thông báo lỗi sang tiếng Việt gọi tên sản phẩm, cảnh báo dữ liệu cũ đã ngừng bán và ảnh hỏng. Kịch bản đầu-cuối mới chạy trên bản build hiện tại + máy chủ/dữ liệu thật: mở màn, thử bỏ rồi hoàn tác cục bộ, kiểm lỗi giao diện/API và chụp 1440/768/375; không gọi API Lưu nên không đổi trang chủ. Bổ sung module vào `MODULE_CATALOG.md` và ghi rõ route + endpoint trong `PERMISSION_MATRIX.md`. 2 kiểm thử máy chủ đạt; lint/guard/build/hygiene và toàn bộ admin đạt |
| Tin tức | ContentList + ContentDetail | 3 | ✅ Xong | — | 2026-07-29 | ✅ payload 6 test (mới) | ✅ 4 test đầu-cuối (tạo/sửa/đăng-web/thùng rác) | Vá 2 lỗi thật: (1) alt ảnh bìa + ảnh sản phẩm bị xoá mỗi lần lưu (payload gửi `{url}` thiếu `alt`, mà applier ghi đè alt từ request) — làm hỏng cả mục checklist "Ảnh bìa có alt text"; (2) `productImage` không bị máy chủ kiểm kho ảnh nội bộ (chỉ coverImage được kiểm) — lưu được ảnh trỏ host ngoài. Thêm: excerpt gửi luôn (kể cả rỗng) để xoá được. Đồng bộ e2e với bản dựng lại (SectionCard `<h3>`, khối "Hiển thị" chuyển sang tab Nội dung, tab SEO tách riêng) + xoá 1 ký tự NUL lẫn trong file kịch bản. Docs: `MEDIA_RULE_002` + mục Categories/Articles ngầm. 12 kiểm thử máy chủ nội dung theo phạm vi đạt (ContentP1 3 gồm 2 mới, AdminContent 7, ContentPublic 8 khi chạy riêng). Toàn bộ admin 430/430. **Nợ nhẹ:** Vitest mới ở mức payload; chưa có render-test 2 màn (màn có block editor phức tạp) — bù bằng 4 e2e sống + test máy chủ |
| Slider | SliderList | 3 | ✅ Xong | — | 2026-07-29 | ✅ 8 test payload/link (mới) | ➖ Hoãn | Không có lỗi hành vi: ảnh slider đã whitelist kho ảnh nội bộ ở máy chủ qua `SafeMediaAssetUrlPolicy` (cả tạo lẫn sửa), link ngoài validate qua `validateSafePublicLink`, không có ô alt để mất, quyền `sliders.*` + dải chỉ-xem + đủ trạng thái. Tách `buildSliderPayload`/`validateSliderLinkGroup` sang `slider-list/sliderPayload.js` để test được (ép sortOrder về số, chỉ gửi ảnh khi có URL, giữ ảnh mobile legacy, chặn `javascript:`). **Hoãn e2e:** slider vị trí `home` là hero trang chủ thật đang phục vụ khách — chạy tự động sẽ đổi hero; cần chốt cách chạy an toàn (cùng nhóm với Sản phẩm nổi bật) |
| Video trang chủ | HomeVideoList | 3 | ✅ Xong | — | 2026-07-29 | ✅ 14 test urlPolicies (mới) | ➖ Hoãn | Không có lỗi hành vi: URL video validate qua provider policy (YouTube/TikTok/Facebook/upload), ảnh thumbnail whitelist máy chủ qua `SafeMediaAssetUrlPolicy`, không có ô alt, xoá thumbnail qua `clearThumbnail`, quyền `home_videos.*`. Bổ sung bộ test cho `lib/urlPolicies.js` (trước đây 0 test dù là nguồn chân lý an toàn URL/video của nhiều màn): chặn scheme nguy hiểm, reject link rút gọn vt.tiktok/vm.tiktok/fb.watch, chỉ nhận video kho nội bộ, phân loại nguồn. E2e hoãn (video hiện trên trang chủ thật) |
| Nổi bật trang chủ | HomeHighlights | 3 | ✅ Xong | — | 2026-07-29 | ✅ 4 test màn (mới) | ➖ Hoãn | Sạch về mọi lỗi tái diễn: chỉ lưu `[{slot, productId}]` (ảnh lấy từ sản phẩm, không URL/alt tự nhập), quyền `home_highlights.*` + chỉ-xem, đủ trạng thái + guard đổi ngôn ngữ khi chưa lưu. Vá nhỏ: ảnh sản phẩm hỏng nay bị ẩn (không vỡ ô) cho nhất quán. Test màn (trước chỉ có test helper PDP highlights, không phải màn này): nạp slot, lưu đúng body, chỉ-xem khoá Lưu, lỗi tải có nút thử lại. E2e hoãn (hiện trên trang chủ thật) |
| Menu | Menu | 3 | ✅ Xong | — | 2026-07-29 | ✅ 7 test (mới) | ➖ Hoãn | Vá 1 lỗi thật: `isValidCustomUrl` phía admin dùng `new URL()` nên **coi `javascript:`/`data:`/`vbscript:`/`//` là hợp lệ** — server (`MenuSupport.validateMenuItemUrl`) chặn các scheme này, nên admin submit rồi mới nhận lỗi khó hiểu; nay client từ chối ngay cho khớp (mirror allowlist + chặn CRLF). Máy chủ vốn đã chắc: icon menu lấy từ danh mục trong DB (không phải URL tự nhập), target danh mục kiểm tồn tại, quyền `menus.read/write`. Test dựng cây/duyệt phẳng/gom hậu duệ (chống tự chọn làm cha). E2e hoãn (menu điều hướng trang chủ thật; cùng nhóm rủi ro live) |
| Thư viện ảnh | MediaLibrary | 3 | ✅ Xong | — | 2026-07-29 | ✅ 9 test constants (mới) | ➖ Hoãn | Không có lỗi hành vi: máy chủ đã hardening — `SvgSanitizer` bóc XSS khỏi SVG, `ImageCompressionService` nén raster + bỏ qua SVG/GIF fail-soft, kiểm định dạng theo cả Content-Type khai báo lẫn magic-byte Tika, **xoá vĩnh viễn gán quyền `*` (SUPER_ADMIN)** khớp `canHardDelete`; frontend kiểm định dạng + dung lượng, quyền `media.read/write` + trash/restore. Test: allowlist định dạng (không nhận pdf/html), 50MB, lọc nâng cao, chip lọc, khoảng ngày nửa-mở. E2e hoãn (chưa có spec; upload đổi kho ảnh thật) |
| Chuyển hướng | RedirectList | 3 | ✅ Xong | — | 2026-07-29 | ✅ 5 test | ➖ Hoãn | Không có lỗi hành vi: nguồn/đích cắt khoảng trắng gửi đúng, lỗi CONFLICT/SELF_LOOP hiện đúng field bằng tiếng Việt, đủ quyền `redirects.*` + chỉ-xem, bật/tắt hàng loạt. **Dọn lỗi lint tồn đọng** (`waitFor` import thừa) → full admin lint giờ sạch 0 lỗi. Thêm 2 test: tạo thành công + ẩn nút tạo khi chỉ đọc. E2e hoãn (chuyển hướng là ánh xạ đường dẫn nội bộ, rủi ro thấp, chưa có spec) |
| Cài đặt | Settings + BannerScreen | 4 | ✅ Xong | — | 2026-07-29 | ✅ 13 test mới; toàn bộ admin 507/507 | ➖ Hoãn | Sửa 4 lệch thật: (1) tab tài khoản nhận chuyển khoản bị ẩn theo quyết định cũ 03/07 dù `PAY_RULE_001` ngày 21/07 đã cho khách chọn chuyển khoản và `DATA_CONTRACT.md` xác nhận tab Thanh toán; (2) Banner trang hiện đồng thời VI+EN, lệch `SETTINGS_RULE_001` — nay dùng nút ngôn ngữ nội dung chung; (3) thiếu ô sửa alt/SEO của cả 3 banner dù contract có `hero_<page>_image_alt`; (4) còn dẫn sang banner trang Giới thiệu/Nội dung đã bị gỡ. Bổ sung nhãn truy cập cho Phân công, test Settings/Banner/Phân công; lint, guard, build và toàn bộ 507 test đạt. E2E/ảnh hoãn vì container `:4000` khỏe nhưng `Mounts=[]`, đang phục vụ image cũ không phản ánh source; không tự restart shared stack |
| Người dùng quản trị | AdminUsers | 4 | ✅ Xong | — | 2026-07-29 | ✅ 10 test mới; toàn bộ admin 519/519 | ➖ Hoãn | Vá 5 rủi ro quyền/tài khoản: (1) chặn PATCH đặt tài khoản về `INVITED` và chặn kích hoạt thủ công tài khoản đang chờ mời — chỉ luồng chấp nhận lời mời được `INVITED → ACTIVE`; (2) mật khẩu đổi trực tiếp nay bắt buộc 8–128 ký tự ở cả giao diện và máy chủ; (3) quản trị viên thường không còn thấy/cấp vai trò `SUPER_ADMIN` và không thể sửa bất kỳ trường nào của tài khoản `SUPER_ADMIN`; (4) tự sửa chỉ cho đổi tên/mật khẩu, không gửi role/status; (5) chặn bấm gửi lại lời mời nhiều lần làm vô hiệu liên kết vừa gửi. Bổ sung xác nhận gộp cho nhiều thay đổi nhạy cảm, dải chỉ-xem, thông báo lỗi nghiệp vụ tiếng Việt và màu avatar ổn định khi sắp xếp. Docs: `STATE_MACHINES.md` mục 5 + `API_CONTRACT.md` Admin Users + `PERMISSION_MATRIX.md` mục 5. 25 kiểm thử máy chủ riêng đạt; lint/guard/build và toàn bộ 519 test admin đạt. E2E/ảnh hoãn vì container `:4000` đang phục vụ image cũ (`Mounts=[]`), không tự restart shared stack |
| Vai trò | Roles | 4 | ✅ Xong | — | 2026-07-29 | ✅ 17 test mới; toàn bộ admin 536/536 | ➖ Hoãn | Vá 4 lệch thật: (1) máy chủ trước đây cho phép người có `roles.write` tự gỡ `roles.read`/`roles.write` khỏi chính vai trò đang dùng, bypass khóa giao diện và tự mất quyền — nay chặn cả API; (2) form tạo vai trò gửi được mã rỗng/1 ký tự rồi mới nhận lỗi máy chủ — nay kiểm đúng `[A-Z][A-Z0-9_]{1,49}` và mở ô mã để sửa; (3) hợp đồng cho tên 120 ký tự nhưng cột lưu chỉ 100 — đồng bộ về 100 ở docs, giao diện và DTO; (4) nhân bản từ vai trò có quyền nhạy cảm bỏ qua bước xác nhận — nay có xác nhận riêng và vẫn không cho nhân bản `SUPER_ADMIN`. Đồng thời dọn test còn đòi quyền POS đã gỡ và nhãn “bài viết & trang” sau khi module Trang đã xoá. Docs: `PERMISSION_MATRIX.md` mục Role Governance + `API_CONTRACT.md` Admin Roles. 34 kiểm thử máy chủ riêng đạt; lint/guard/build và toàn bộ 536 test admin đạt. E2E/ảnh hoãn vì container `:4000` là image cũ không bind source |
| Nhật ký hoạt động | AuditLogList | 4 | ✅ Xong | — | 2026-07-29 | ✅ 103 test theo module; toàn bộ admin 543/543 | ➖ Hoãn | Vá các lệch thật: khoảng ngày đảo ngược được chặn trước khi gọi máy chủ; thứ tự file CSV khớp bảng đang xem và dữ liệu bắt đầu bằng ký hiệu công thức được vô hiệu hóa; đường dẫn mở chi tiết sai tự được dọn; chi tiết hiển thị địa chỉ truy cập, giá trị object dễ đọc và nhãn dự phòng cho mã lạ; mọi thao tác xóa vĩnh viễn/xóa mềm/khóa truy cập đều được cảnh báo đúng. Gom nhãn và màu nhóm về một nguồn để bảng desktop, thẻ mobile và ngăn chi tiết không lệch nhau. Docs đối chiếu: `PERMISSION_MATRIX.md` mục Audit Log + `API_CONTRACT.md` mục Admin Audit Log. 11 kiểm thử máy chủ riêng đạt; lint/guard/build và toàn bộ 543 test admin đạt. E2E/ảnh hoãn vì container `:4000` là image cũ không bind source |
| Tổng quan | Dashboard | 5 | ✅ Xong | — | 2026-07-29 | ✅ 8 test mới; toàn bộ admin 551/551 | ➖ Hoãn | Vá 4 lệch vận hành: (1) không còn báo “mọi việc đều ổn” khi số liệu tồn kho vẫn đang tải hoặc tải lỗi; (2) bấm Đơn chờ xử lý mở đúng danh sách lọc `PENDING`, bấm Sản phẩm đang bán mở đúng danh sách `PUBLISHED`; (3) nhãn `todayPaidRevenue` đổi từ “Đã thu/Collected” sang “Đơn hoàn tất/Completed orders” vì số này tính theo trạng thái đơn, không phải giao dịch thanh toán; (4) mảng dữ liệu thiếu/sai dạng không làm hỏng toàn màn hình, số xu hướng được làm tròn dễ đọc và dữ liệu trống có fallback. Sửa thêm mô tả tiếng Anh từng ghi sai rằng đơn hôm nay tính mọi trạng thái và sản phẩm đang bán phải còn hàng. Docs: `DATA_CONTRACT.md` mục Dashboard KPI & Lists + `API_CONTRACT.md` Dashboard Contract + `PERMISSION_MATRIX.md` dashboard. 10 kiểm thử máy chủ riêng đạt; hygiene, lint/guard/build và toàn bộ 551 test admin đạt. E2E/ảnh hoãn vì container `:4000` là image cũ không bind source |
| Báo cáo | Reports | 5 | ✅ Xong | — | 2026-07-29 | ✅ 15 test mới; toàn bộ admin 566/566 | ➖ Hoãn | Vá 4 lệch thật: (1) preset ngày nay lấy lịch `Asia/Ho_Chi_Minh`, không lệch một ngày khi máy người quản trị ở múi giờ khác; (2) URL preset/ngày sai được chuẩn hoá về trạng thái an toàn, khoảng so sánh tính theo ngày lịch và đúng số ngày bao gồm hai đầu; (3) khoảng vượt 90 ngày không còn nút “Thử lại” có thể gọi máy chủ để lách chặn `REPORT_RULE_011`, khoảng đảo ngược vẫn có thể sửa tự động; (4) đổi “Tiền thực thu/Collected revenue” thành “Doanh thu đơn hoàn tất/Completed-order revenue” và sửa chú giải GMV/số đơn/AOV theo đúng quy tắc loại đơn huỷ. Hai ô ngày chuyển sang component Input chuẩn, có trạng thái lỗi truy cập được; xuất đơn chỉ bật khi khoảng ngày hợp lệ, còn xuất toàn bộ sản phẩm/khách hàng vẫn độc lập đúng hợp đồng. Docs: `BUSINESS_RULES.md` `REPORT_RULE_001/002/005/006/007/008/011` + `DATA_CONTRACT.md` Reports Analytics Response Shape + `API_CONTRACT.md` Reports & Analytics + `PERMISSION_MATRIX.md` Reports. 15 kiểm thử API máy chủ đạt; kiểm thử native-query PostgreSQL chưa chạy được vì Docker Desktop không cung cấp engine (Npipe 400), giữ lại cho CI. Hygiene, lint/guard/build và toàn bộ 566 test admin đạt. E2E/ảnh hoãn vì container `:4000` là image cũ không bind source |

## Phát hiện đối chiếu danh mục module

- ✅ Đã đối chiếu xong (2026-07-29): bổ sung `MODULE_CATALOG.md` cho Sản phẩm nổi bật, Slider, Video trang chủ, Nổi bật trang chủ, Người dùng quản trị, Vai trò; đồng thời ghi rõ Banner trang là màn con của Cài đặt.
- ✅ Đã bổ sung `PERMISSION_MATRIX.md` cho màn "Sản phẩm nổi bật" (`/admin/featured-products`) và endpoint lưu nguyên tử, cùng dùng quyền `products.update` theo quyết định owner 2026-07-28.
- Các module ghi `REMOVED` trong `MODULE_CATALOG.md` không nằm trong phạm vi audit và không được khôi phục.

## Việc xuyên module

- 🟡 Rà soát chữ hiển thị toàn admin (2026-08-29): đã sửa toàn bộ khóa dùng chung bị đặt nhầm dưới `richEditor`, bổ sung các bản dịch còn thiếu, thay dự phòng tiếng Việt/mã nội bộ bằng chữ đã khai báo, và nối chốt kiểm tra lời gọi chữ vào lint/build. Chốt đã đạt với 4.101 khóa mỗi bộ và 4.484 lượt gọi trong 247 tệp chạy thật; lint, 1.120 kiểm thử và production build đều đạt. Chưa chuyển sang ✅ vì chưa thể bấm thử các màn ở ba khổ trong cả hai ngôn ngữ: `bigbike-backend` đang lặp khởi động, không tự can thiệp dịch vụ dùng chung.

- 🟡 Đã hoàn tất phần mã của cả 3 đợt chuẩn giao diện admin (2026-08-29); dòng hoàn tất ngày 2026-08-23 trước đây chỉ phản ánh một phần công việc. Đợt 1 gom 35 màn về một hệ `Screen` / `DetailSection` / `FilterBar` / `AdminTable`, bỏ định nghĩa giao diện cũ, chuẩn hoá control/khoảng cách theo token và làm thanh cuộn sidebar dễ nhận biết. Đợt 2 rút mô tả dưới tiêu đề từ 28 xuống 7/33 đầu trang, gom 11 tên nhóm tự đặt về đúng 5 nhóm menu, đưa chú thích dài vào trợ giúp theo ngữ cảnh, thống nhất thao tác dòng và gom cảnh báo logo thương hiệu từ tối đa 25 dải dài còn 1 dải tổng hợp. Đợt 3 cho 12 màn lọc dùng drawer chung trên mobile (Nhật ký giữ drawer áp dụng-nháp riêng), bảo đảm 25/25 bảng dùng chung kể cả bảng con có dạng thẻ mobile, gộp nhiều thao tác đầu trang vào nút “…” trên điện thoại, giữ 5/5 cột phụ detail khi cuộn, chia lại Cài đặt và nâng Thư viện ảnh lên 6 cột. 13 danh sách vận hành có mật độ bảng nhớ riêng theo màn; Đơn hàng mặc định gọn, Sản phẩm mặc định thoáng, còn lại vừa. Số đo nền tảng: 0 màn thuộc `AdminShell` thiếu `Screen`, 0 file dùng card/filter cũ, 0 bảng tự dựng, 0 khoảng cách CSS viết cứng hoặc lớp khoảng cách nửa nấc; song ngữ 4.013/4.013 khoá, lint, 117 tệp / 1.116 kiểm thử và production build đạt. Chưa chuyển sang ✅ vì ảnh ba khổ/E2E chưa chạy được: `bigbike-backend` vẫn đang lặp khởi động, không tự can thiệp dịch vụ dùng chung.
- ✅ Đã chuẩn hoá hộp thoại xoá (2026-08-20): rà 31 luồng trên Thư viện ảnh, Banner, Video trang chủ, Sản phẩm, Tin tức, Danh mục, Thương hiệu, Menu, Chuyển hướng, Đánh giá và Vai trò. `ConfirmDialogProvider` mặc định trung tính; chỉ xoá vĩnh viễn truyền `variant: 'danger'`, với tiêu đề/nút `Chuyển vào Thùng rác` hoặc `Xoá vĩnh viễn`, nội dung luôn nêu mục/số lượng và khả năng hoàn tác. Vai trò đã chuyển sang xác nhận dùng chung nhưng vẫn giữ cảnh báo chặn khi còn nhân sự được gán; đã xoá `LegacySizeScaleManagerModal` và bảng cỡ đời cũ bị vô hiệu hoá. Đồng bộ `vi.json`/`en.json`, bổ sung kiểm tra parity i18n và test huỷ/xác nhận/màu nút. Không thay đổi API, quyền hoặc nghiệp vụ xoá.
- ✅ Đã dọn (2026-08-18): audit toàn bộ ô nhập tiền trong admin — inventory gồm giá sản phẩm `retailPrice`/`salePrice`, giá niêm yết/khuyến mãi riêng của biến thể ở desktop/mobile, bulk edit và matrix wizard; các giá trị phí/cost/discount/amount còn lại chỉ đọc hoặc không phải tiền hiện hành. Gom logic vào `bigbike-admin/src/components/MoneyInput.jsx` + `src/lib/moneyInput.js`: không format trong lúc gõ, giữ caret, nhận paste có/không dấu phân cách, blur format `vi-VN`, payload số/null. Bổ sung 14 ca regression helper/component/payload/screen; E2E chạy 1440/768/375, không mutation trước Lưu/Áp dụng. `salePrice = 0` được chuẩn hóa thành `null` theo `BUSINESS_RULES.md`/`DATA_CONTRACT.md`; không khôi phục `costPrice`/`compareAtPrice`/phí đã gỡ khỏi contract.
- ✅ Đã dọn (2026-07-29): checkbox của `MobileCardList` nhận nhãn theo đúng tên từng bài viết/chuyển hướng/đánh giá; 2 kiểm thử component đạt.
- ✅ Đã dọn (2026-07-29): `contracts.js` dùng fallback “Sản phẩm không xác định” và không còn dựng `updatedAt` ngoài hợp đồng chi tiết đơn; 2 kiểm thử contract đạt.
- ✅ Đã dọn (2026-07-29, lượt Chuyển hướng): lỗi lint `waitFor` import thừa trong `RedirectListScreen.test.jsx` — full admin lint giờ sạch 0 lỗi.
- Các gate máy chủ riêng của Vai trò, Người dùng quản trị, Nhật ký, Tổng quan, Báo cáo và Sản phẩm nổi bật đều sạch. Docker CLI + stack thật đang khỏe, nhưng Testcontainers 1.20.4 vẫn kết nối nhầm cầu nối `docker_cli` của Docker Desktop 4.78 và nhận Npipe 400 (đã thử cả pipe `dockerDesktopLinuxEngine` và API 1.44, không thay đổi shared state), nên riêng các test tự dựng PostgreSQL vẫn giữ cho CI. Các nợ test `unaccent` và cô lập dữ liệu Tin tức bên dưới không phải lỗi của các module đã chốt.
- Danh sách component chuẩn có `DateRangePicker`, nhưng thư mục component hiện không có file này. Module Đơn hàng dùng trực tiếp hai `Input` ngày theo chuẩn shadcn; chỉ bổ sung component dùng chung khi một module sau thực sự cần tái sử dụng.
- ✅ Đã xử lý ở lượt Danh mục (2026-07-28): `category.banner`/`category.mobileBanner` nay đã kiểm whitelist kho ảnh; payload luôn gửi `description`/`introContent` (kể cả rỗng) để xoá được nội dung cũ.
- 3 kiểm thử máy chủ của Sản phẩm hỏng vì môi trường thiếu hàm `unaccent` (`AdminReadApiTest`), và 1 kiểm thử Tin tức hỏng vì thiếu danh mục mặc định `tin-tuc` (`AdminMutationApiTest`) — không liên quan các module đã xong, xử lý ở đúng lượt module.
- `ContentPublicApiTest.shouldFilterArticlesByCategorySlug` fail khi chạy chung nhóm (đếm 4 thay vì 3) do **lẫn dữ liệu giữa các lớp test**: `ContentP1ApiTest.shouldSyncCategoryMapAfterArticleCategoryPatch` (test cũ) commit 1 bài PUBLISHED vào `cc_blog` không dọn, mà `@SpringBootTest` không rollback giữa các lớp. Chạy riêng `ContentPublicApiTest` đạt 8/8. Là lỗi HẠ TẦNG TEST có sẵn, không phải lỗi tính năng Tin tức; cần dọn bằng cách cho các test tạo bài tự xoá cứng cuối test hoặc gắn prefix + lọc — gom vào việc dọn test chung, không sửa trong lượt module.
- ✅ Đã thống nhất (2026-07-29): thẻ trạng thái Thương hiệu dùng “Đang hoạt động” / “Thùng rác”, khớp bộ lọc và hộp xác nhận; Danh mục vẫn giữ cờ Hiển thị độc lập theo đúng contract.
- Truy vấn chuẩn hóa số điện thoại legacy của Khách hàng hiện dùng quét toàn bảng; dữ liệu thật mới có 439 số, 14 số dạng cũ và 0 nhóm trùng nên chưa ảnh hưởng tính đúng. Cân nhắc expression index hoặc cột chuẩn hóa khi quy mô khách tăng.
