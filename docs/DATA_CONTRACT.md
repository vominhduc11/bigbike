# DATA_CONTRACT.md — bigbike.vn

Entity → producer → consumer → storage → screens, reverse-engineered from theme + SQL dump (`kd_` prefix).

Conventions
- "Core" = WordPress/WooCommerce defaults; "Custom" = project-specific code/meta.
- "Needs verification" = field visible in code but its population path, exact format or authoritative source was not confirmed.

---

## E-01 — Product

| Field | Value |
|---|---|
| Source | WooCommerce + custom postmeta + ACF |
| Producer | wp-admin (WooCommerce product editor, ACF field groups), possibly imported by `kd_pmxe_*` (WP All Export) — **Needs verification of import path** |
| Consumer | archive-product, taxonomy-product_cat, taxonomy-pwb-brand, single-product, related-products, home, search, cart, checkout, quick-buy, GTM `view_item` |
| Primary storage | `kd_posts` (type='product', 'product_variation') + `kd_postmeta` + WC lookup `kd_wc_product_meta_lookup` + `kd_wc_product_attributes_lookup` |
| Taxonomies | `product_cat`, `product_tag`, `pwb-brand` (Perfect WooCommerce Brands), `product_type`, `product_visibility` (feature flag `featured`), `pa_*` (attribute taxonomies, e.g. `pa_color`, `pa_size`) |
| Core fields (via `wc_get_product()`) | `id`, `sku`, `name` (post_title), `slug` (post_name), `status`, `type` (simple/variable/variation), `price`, `regular_price`, `sale_price`, `stock_status`, `manage_stock`, `stock_quantity`, `image_id`, `gallery_image_ids`, `permalink`, `is_in_stock()`, `is_purchasable()`, `backorders_allowed()` |
| Custom postmeta | `product_of_stock` (boolean-ish) — forces "out of stock" UI regardless of WC stock state ([woo-functions.php:181-185](files/wp-content/themes/bigbike/inc/woo-functions.php#L181-L185), [content-single-product.php:92-95](files/wp-content/themes/bigbike/woocommerce/content-single-product.php#L92-L95), [variable.php:25-32](files/wp-content/themes/bigbike/woocommerce/single-product/add-to-cart/variable.php#L25-L32)); `prouduct_of_stock` [sic] on simple.php:25 — possible typo, Needs verification; `salediscount` (int percent override); `_yoast_wpseo_primary_product_cat` (Yoast primary-term value — still read even though Yoast deactivated); `_yoast_wpseo_title`, `_yoast_wpseo_metadesc` |
| Custom ACF fields | `content_bottom` (WYSIWYG — bottom-of-page content), `rating` (float, default 4.5 in template), `rating_count` (int, default 124), `videos` (Needs verification — consumed by `content-videos-product`) |
| Taxonomies used in flows | `product_cat` (home show-on-homepage filter, related products), `pwb-brand` (shop title override, homepage carousel, search expansion), `product_visibility='featured'` (home featured products section) |
| Migration notes | Variations live as child posts (`post_parent=parent_product_id`, `post_type=product_variation`). Attributes are in two places: wc_post_term relationships for taxonomy attrs, and serialized postmeta `_product_attributes` for custom ones. The lookup tables are maintained by WC. |
| Evidence | [ajax-functions.php:465-475](files/wp-content/themes/bigbike/inc/ajax-functions.php#L465-L475), [utils-functions.php:214-245](files/wp-content/themes/bigbike/inc/utils-functions.php#L214-L245), [content-single-product.php:34-36](files/wp-content/themes/bigbike/woocommerce/content-single-product.php#L34-L36) |
| Status | Implemented |

---

## E-02 — Product variation

| Field | Value |
|---|---|
| Source | WooCommerce |
| Producer | wp-admin product editor |
| Consumer | single-product (variable.php, variation-add-to-cart-button.php, variation_radio_buttons), API-04 add-to-cart, API-07 buy_quickly, API-08 find_variation_product |
| Storage | `kd_posts.post_type=product_variation` (post_parent=parent product) + `kd_postmeta` (one `attribute_pa_<slug>` key per attribute), + WC lookup tables |
| Core fields | `variation_id`, `price`, `regular_price`, `sale_price`, `attributes` (assoc: `attribute_pa_color`→slug, `attribute_pa_size`→slug, etc.), `is_in_stock`, `max_qty`, `image.url` |
| Custom logic | Radio UI generated by `variation_radio_buttons()` that reads `get_field('color', $term)` + `get_field('image', $term)` on `pa_color` terms to colour/background the swatches ([woo-functions.php:224-353](files/wp-content/themes/bigbike/inc/woo-functions.php#L224-L353)). |
| Availability rule | `variation_check()` marks variation inactive when `!is_in_stock() && !backorders_allowed()` ([woo-functions.php:356-362](files/wp-content/themes/bigbike/inc/woo-functions.php#L356-L362)). Stock `availability_html` is stripped by `sww_wc_remove_variation_stock_display` ([woo-functions.php:393-397](files/wp-content/themes/bigbike/inc/woo-functions.php#L393-L397)). |
| Status | Implemented |

Note: backend import now materializes WordPress `attribute_pa_*` data into `Attribute` / `AttributeValue` relations and keeps variant options linked to those entities, while still preserving the flat option fields for compatibility.

---

## E-03 — Product category (`product_cat`)

| Field | Value |
|---|---|
| Source | WooCommerce taxonomy |
| Storage | `kd_terms` + `kd_term_taxonomy` (taxonomy='product_cat') + `kd_termmeta` |
| Custom termmeta keys | `thumbnail_id` (WC default — attachment id for category image), `show_on_homepage` (boolean, drives home section), `ordering` (int — used for meta_value_num order on home), `_yoast_wpseo_title`, `_yoast_wpseo_metadesc`, ACF `top_image`, `image_left`, `content_bottom` |
| Permalink | `/danh-muc-san-pham/{slug}/` (from `kd_options.woocommerce_permalinks.category_base`) |
| Consumers | home (category cards + featured categories), shop sidebar, breadcrumbs, SEO title/desc override, related-products (`getMainProductCategory`) |
| Status | Implemented |

Note: `product_cat` hierarchy must be preserved via `parent_id` self-reference during import and admin updates.

---

## E-04 — Brand taxonomy (`pwb-brand`)

| Field | Value |
|---|---|
| Source | Perfect WooCommerce Brands plugin |
| Storage | `kd_terms` + `kd_term_taxonomy` (taxonomy='pwb-brand') + `kd_termmeta` |
| Custom termmeta | `pwb_brand_image` (attachment id, read via ACF `get_field`) |
| Permalink | `/pwb-brand/{slug}/` — **Needs verification** (could be remapped by permalink-manager-pro) |
| Consumers | home brand carousel (5 items), shop-page title override when `?pwb-brand={slug}` query param is present ([woo-functions.php:459-498](files/wp-content/themes/bigbike/inc/woo-functions.php#L459-L498)), search (added to tax_query), archive `taxonomy-pwb-brand.php` |
| Status | Plugin-provided |

---

## E-05 — Cart item

| Field | Value |
|---|---|
| Source | WooCommerce cart (session) |
| Storage | `kd_woocommerce_sessions` (serialized) |
| Shape per item (from `WC()->cart->get_cart()`) | `{ key, product_id, variation_id, variation, quantity, data (WC_Product), line_total, line_subtotal, line_tax, ... }` |
| Producer | API-04 (`custom_add_to_cart`), standard WC cart form |
| Consumer | API-05 (`remove_item_from_cart`), API-06 (`update_cart_item_quantity`), cart.php, mini-cart (`template-parts/header-cart.php` — Needs verification), checkout, GTM view_cart |
| Lifetime | WooCommerce session (cookie-based). Destroyed at `WC()->session->destroy_session()` post-checkout. |
| Migration notes | Sessions are ephemeral — not source-of-truth; do not migrate session rows. |
| Status | Implemented |

---

## E-06 — Checkout / Order

| Field | Value |
|---|---|
| Source | WooCommerce |
| Producer | Checkout form submission (FE-01) OR API-07 (`buy_quickly`) OR wp-admin order creation |
| Storage — **LEGACY (authoritative)** | `kd_posts` với `post_type='shop_order'` + `kd_postmeta` + `kd_woocommerce_order_items` + `kd_woocommerce_order_itemmeta`. **825 order** verified. |
| Storage — HPOS (NOT used) | `kd_wc_orders`, `kd_wc_orders_meta`, `kd_wc_order_addresses`, `kd_wc_order_operational_data` có schema nhưng **HPOS tắt** (`woocommerce_custom_orders_table_enabled='no'`, `woocommerce_custom_orders_table_data_sync_enabled='no'`). Không migrate từ đây. |
| Core fields | `id`, `order_number`, `status` (pending/processing/on-hold/completed/cancelled/refunded/failed), `total`, `currency`="VND", `date_created`, `payment_method`, `payment_method_title`, `customer_id`, `billing_*`, `shipping_*`, `order_key`, `transaction_id` |
| Fields customised by theme | Checkout fields pruned: `billing_company`, `billing_address_2`, `billing_postcode`, `billing_country`, `billing_state`, `shipping_company`, `shipping_address_2`, `shipping_postcode`, `shipping_country` are all unset ([woo-functions.php:43-65](files/wp-content/themes/bigbike/inc/woo-functions.php#L43-L65)). Added `shipping_phone` (required, min=max=10 digits) ([functions.php:158-171](files/wp-content/themes/bigbike/functions.php#L158-L171)). `billing_phone` must match `^[0-9]{10}$` ([woo-functions.php:69-79](files/wp-content/themes/bigbike/inc/woo-functions.php#L69-L79)). |
| Required for order | `product_id`, `quantity`, shipping address (at minimum: `first_name`, `phone`, `address_1` per quick-buy code path) |
| Consumer | Thank-you page, My Account → Orders, admin Orders UI, GTM purchase event |
| Status | Implemented |

---

## E-07 — Order received

| Field | Value |
|---|---|
| URL format | `/thanh-toan/order-received/{order_id}?key={order_key}` — hard-coded in API-07 [ajax-functions.php:436](files/wp-content/themes/bigbike/inc/ajax-functions.php#L436); WC default elsewhere |
| Template | [woocommerce/checkout/thankyou.php](files/wp-content/themes/bigbike/woocommerce/checkout/thankyou.php) |
| Producer | Checkout OR quick-buy |
| Data shown | `order_number`, `date`, `email` (only if logged-in and same user), `total`, `payment_method_title` |
| GTM side-effect | `dataLayer.push({ event:'purchase', ecommerce:{ transaction_id, value, currency:'VND' } })` |
| Status | Implemented |

---

## E-08 — User / Customer profile

| Field | Value |
|---|---|
| Source | WordPress users + WooCommerce customer meta + custom meta |
| Storage | `kd_users` (login, email, pass, registered), `kd_usermeta` (wp_capabilities, session tokens, billing_*, shipping_*, `phone` custom, `gender` custom, `dob` custom, `first_name`, `last_name`, `display_name`) |
| Producer | API-01 (`custom_register_user`), API-07 (`buy_quickly` auto-create), wp-admin, WooCommerce checkout-register |
| Consumer | API-02 login, API-03 update-profile, WC My Account, admin |
| Username convention (API-01) | `user_login = phone`, `user_email = email`, `user_meta.phone = phone`, `role='subscriber'` |
| Username convention (API-07) | `user_login = user_email = phone.'@liveevil.vn'` — **Bug/design choice, see ARCH A5** |
| Default role | `default_role=subscriber` (kd_options.default_role). Public WP-native registration is **disabled** (`users_can_register=0`) — but AJAX API-01 bypasses this setting. |
| Status | Implemented |

---

## E-09 — Login / Register form (client)

| Field | Value |
|---|---|
| Login form shape | `username` (phone or email), `password`, `remember_me` (checkbox) + hidden `action=custom_login_user` |
| Register form shape | `fullname`, `email`, `phone`, `password`, `repassword` + hidden `action=custom_register_user` |
| Template | [page-login.php:30-54](files/wp-content/themes/bigbike/page-templates/page-login.php#L30-L54), [page-register.php:24-66](files/wp-content/themes/bigbike/page-templates/page-register.php#L24-L66) |
| Client validation | `jquery.validate` with `data-msg`, `data-rule-minlength=6`, `data-rule-maxlength=32` |
| Status | Implemented |

---

## E-10 — Contact form

| Field | Value |
|---|---|
| Source | Contact Form 7 (form id stored as shortcode text in ACF `contact_form` on Contact page) + contact-form-cfdb7 (stores to `kd_db7_forms`) |
| Fields | **Needs verification** — not available in snapshot since CF7 form config lives in `kd_posts.post_type=wpcf7_contact_form`. ACF `iframe_maps`, `note` visible on page. |
| Producer | CF7 submission endpoint |
| Consumer | CF7 email + `kd_db7_forms` record |
| Status | Plugin-provided |

---

## E-11 — Blog post / Category

| Field | Value |
|---|---|
| Source | WordPress core |
| Storage | `kd_posts` (post_type='post'), `kd_term_taxonomy` (taxonomy='category') |
| Permalink | `/tin-tuc/{slug}.html` (from `kd_options.permalink_structure='/tin-tuc/%postname%.html'`) |
| Home-page constants | Post category id `365` used as "Trải nghiệm cùng Bigbike" in `page-home.php:189`; category id `361` used as "Tin tức mới nhất" in `page-home.php:232` — Polylang-translated via `pll_get_term($id, pll_current_language())` |
| Consumer | category.php, single.php, page-home.php latest reviews/blogs blocks |
| Status | Implemented |

Note: blog posts preserve `post_author`, category, and `post_tag` relations; blog tags are canonical M:N entities and are not flattened into raw strings.

---

## E-12 — Home page sections

| Field | Value |
|---|---|
| Source | ACF fields on the Home page (page_id=12) + query results |
| ACF fields (page-home.php) | `sliders` (repeater: `image`, `image_mobile`, `product` (post relationship → product), `link`); `about_us` (repeater: `sub_title`, `title`, `content`); `blog_content` (repeater: `sub_title`, `title`, `content`); `content_bottom` (WYSIWYG) |
| Computed | featured products (`product_visibility=featured`, 3 items), 5 products in main carousel, product categories with `show_on_homepage=1` ordered by `ordering` desc, latest 3 posts in each Polylang-aware category, 5 videos (`post_type=video`), first 5 `pwb-brand` terms |
| Status | Implemented |

---

## E-13 — SEO / meta fields

| Field | Value |
|---|---|
| Source | RankMath (active) + Yoast (legacy meta still present and still read) |
| RankMath storage | `kd_rank_math_*` tables + postmeta keys `rank_math_*` |
| Yoast storage | `kd_yoast_indexable` + `kd_yoast_seo_meta` + `kd_yoast_seo_links` + postmeta `_yoast_wpseo_*` / termmeta `_yoast_wpseo_*` |
| Custom override | `custom_document_title`, `custom_document_title_heading`, `custom_document_description` hooked into `wpseo_title`, `woocommerce_page_title`, `wpseo_metadesc`; they read `_yoast_wpseo_title` / `_yoast_wpseo_metadesc` of the shop page or category term, then append filter context (brand, gender, price range, color, pagination) computed by `bigbike_title_override()` ([woo-functions.php:459-638](files/wp-content/themes/bigbike/inc/woo-functions.php#L459-L638)) |
| Canonical override for search | hard-coded `<meta name="canonical" content="https://bigbike.vn/">` on search pages ([layout-functions.php:297-303](files/wp-content/themes/bigbike/inc/layout-functions.php#L297-L303)) |
| Status | **Partial** — duplicated SEO path (Yoast meta + RankMath active). See ARCHITECTURE A9. |

---

## E-14 — Shipping / Payment data (inferred)

| Field | Value |
|---|---|
| Source | WooCommerce Shipping Zones + Flexible Shipping plugin |
| Storage | `kd_woocommerce_shipping_zones`, `kd_woocommerce_shipping_zone_locations`, `kd_woocommerce_shipping_zone_methods`, plus Flexible Shipping-specific options |
| Hard-coded reference | Quick-buy uses method id `flexible_shipping_single:9` ([ajax-functions.php:427](files/wp-content/themes/bigbike/inc/ajax-functions.php#L427)) — a specific Flexible Shipping method id |
| Free shipping threshold (custom, in quick-buy only) | 2,000,000 VND — hard-coded [ajax-functions.php:419-421](files/wp-content/themes/bigbike/inc/ajax-functions.php#L419-L421) |
| Tax | Disabled (`woocommerce_calc_taxes=no`, `woocommerce_prices_include_tax=no`) |
| Payment | Quick-buy forces `bacs` (Bank transfer) payment method. Regular checkout uses any enabled gateway — **Needs verification of full gateway list** (likely BACS + Cash on Delivery + possibly VNPay/OnePay — check `kd_options.woocommerce_gateway_order` or active gateway settings). |
| Ship to destination | `billing` (from options) — shipping fields mirror billing unless customer ticks "ship to a different address" |
| Ship-to countries | Empty value means WC falls back to `woocommerce_allowed_countries='all'` — **but** the checkout UI force-unsets `*_country` and `*_state` ([woo-functions.php:51-62](files/wp-content/themes/bigbike/inc/woo-functions.php#L51-L62)) which means default `country=VN` is used |
| Status | Partial — configuration not fully inspected |

---

## E-15 — Custom post types / Pods

| Field | Value |
|---|---|
| Source | Pods plugin + WP core |
| Types referenced in theme | `slider` (page-home.php:10-13 — fallback query), `video` (page-home.php:263-269, woocommerce product tab callback `videos_tab_callback`), `review` (glob shows `single-review.php` template exists) |
| Storage | `kd_posts` with matching `post_type` + Pods relationship rows in `kd_podsrel` |
| Status | **Needs verification** of Pods field configuration (stored in `kd_pods_*`-namespaced options). |

---

## Snapshot sanity counts

Các số liệu **verified** từ `sqldump.sql` (đếm qua regex kết thúc `,'post_type','',0)`):

- **1,227** product + **4,040** product_variation + **12,053** attachment + **174** post + **825** shop_order + **22** page + **62** video + **46** nav_menu_item + **2** slider + **2,522** revision + **1** shop_coupon + **1** wpcf7_contact_form (id=8895).
- **3,997** user trong `kd_users` (verified count).
- **40** row trong `kd_rank_math_redirections`.

Password hash distribution trong `kd_users`:
- `$wp$2y$12$...` (WP 6.9+ bcrypt wrapper) — áp dụng cho user được WP 6.9 tạo hoặc đã đổi password recent.
- `$P$...` (phpass legacy) — user cũ chưa đổi password.
Migration phải support verify cả 2 format và re-hash sang argon2id sau lần login đầu.
