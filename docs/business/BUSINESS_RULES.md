# Business Rules

Only rules verified from current code, config, migration, or test are documented here.

## Product gender contract

- `PRODUCT_RULE_018` (owner decision 2026-08-14): **Nhập/xuất giới tính dùng mảng `genders` tùy chọn.** File xuất JSON luôn ghi `genders` với `[]`, `["Nam"]`, `["Nữ"]` hoặc `["Nam","Nữ"]`; file nhập cũ có một khóa `gender` với `Nam`/`Nữ` vẫn được nhận, còn `Unisex` legacy được chuẩn hóa thành `genders: []`. Nếu file có cả hai khóa, `genders` canonical được ưu tiên. CSV giữ cột `gender` để tương thích: giá trị là `Nam`, `Nữ`, `Nam|Nữ` hoặc để trống. Không dùng giới tính để suy đoán từ tên hoặc danh mục. `CONFIRMED_FROM_OWNER_DECISION_2026-08-14`

## Admin system roles

- **`ROLE_RULE_001` (owner decision 2026-07-30)** — The only default system roles are `SUPER_ADMIN` (Chủ hệ thống) and `ADMIN` (Quản trị viên). Any other role, including historically seeded `SHOP_MANAGER` and `EDITOR`, is a custom role (`is_system = false`). The reclassification must preserve its assigned users and permission set; custom roles remain deletable only when no admin user is assigned. `CONFIRMED_FROM_OWNER_DECISION`

Evidence:

- `V361__retain_two_system_roles.sql`
- `AdminRoleService.java`

## Admin RBAC rules

- **`RBAC_RULE_001` — quyền đọc là quyền vào module.** Menu, trang danh sách, trang chi tiết và API đọc của một module dùng permission `*.read` tương ứng. Quyền ghi không thay thế quyền đọc và không được dùng theo dạng `read OR write` để mở module. `OWNER_CONFIRMED_2026-07-31`
- **`RBAC_RULE_002` — permission set phải dependency-closed.** Mọi quyền ghi/cập nhật/xuất dữ liệu phải đi cùng toàn bộ quyền đọc được khai báo trong permission catalog. Roles UI được tự thêm dependency để hỗ trợ người vận hành; Roles API phải từ chối payload thiếu dependency thay vì tự sửa âm thầm. Wildcard `*` thỏa mọi dependency nhưng không phải permission có thể cấp cho custom role. `OWNER_CONFIRMED_2026-07-31`
- **`RBAC_RULE_003` — workspace composite dùng phép AND rõ ràng.** Route hoặc action cần nhiều capability phải khai báo `allOf`; ví dụ Featured Products cần `products.read` và `products.update`, Product create cần thêm `catalog.read`, còn Slider full-edit cần thêm `products.read` và `media.read`. `OWNER_CONFIRMED_2026-07-31`
- **`RBAC_RULE_004` — UI hiding không phải authorization.** Ẩn menu/nút và chặn query giúp giao diện rõ ràng nhưng backend vẫn là lớp bảo mật cuối, tiếp tục kiểm tra permission trên mọi API đọc/ghi. `OWNER_CONFIRMED_2026-07-31`
- **`RBAC_RULE_005` — supporting permission chỉ gate phần phụ thuộc.** Thiếu `inventory.read`, `media.read`, `products.read`, `catalog.read` hoặc `roles.read` chỉ làm widget, picker, filter hoặc action liên quan không khả dụng; không làm hỏng toàn bộ screen nếu quyền vào module chính vẫn hợp lệ. UI phải giải thích capability còn thiếu và không gọi API chắc chắn bị từ chối. `OWNER_CONFIRMED_2026-07-31`
- **`RBAC_RULE_006` — xuất báo cáo là quyền dữ liệu nhạy cảm.** `reports.export` phụ thuộc `reports.read`, được đánh dấu sensitive và cho phép xuất dữ liệu báo cáo đa miền (đơn hàng, khách hàng, sản phẩm). Đây không phải export-only role; người có quyền xuất phải có lối vào workspace Báo cáo. `OWNER_CONFIRMED_2026-07-31`

Media Library không được tự động cấp cho mọi role có quyền cập nhật Product/Content/Catalog/Settings. Người thiếu `media.read` vẫn sửa được field không liên quan media; picker bị khóa và không gọi API. Có `media.read` nhưng thiếu `media.write` thì được chọn file hiện có nhưng không upload file mới.

Evidence:

- `docs/engineering/PERMISSION_MATRIX.md`
- `PermissionCatalog.java`
- `AdminRoleService.java`
- `bigbike-admin/src/lib/adminAccessPolicy.js`

## Catalog Availability

- Public catalog/cart/checkout only accept products with publish status `PUBLISHED`. `CONFIRMED_FROM_CODE`
- Variant add-to-cart/checkout requires the variant to be available and in stock. `CONFIRMED_FROM_CODE`
- Checkout re-syncs prices from DB before order creation and reports price changes. `CONFIRMED_FROM_CODE`

Evidence:

- `CartService.java`
- `CheckoutService.java`
- `Phase1ECartApiTest.java`
- `Phase1FCheckoutApiTest.java`

## Product catalog CSV export (`PRODUCT_RULE_016`, owner decision 2026-08-09)

The Product module's CSV export is a catalog data-out operation and requires `reports.export`. `FILTERED` follows the product-list filters; when `publishStatus` is blank/`ALL`, it exports `PUBLISHED` only by default. `includeDraft` and `includeTrash` explicitly widen that scope. An explicit `DRAFT`, `PUBLISHED`, or `TRASH` filter wins over those switches.

`SELECTED` exports only the selected product IDs but keeps the same status rule. The request accepts at most 200 unique IDs; the admin must use filtered export for a larger set. `ALL` is an explicit operator choice that ignores every screen filter and exports every product in every publish status, including `DRAFT` and `TRASH`.

The export has one product per CSV row, preserves nested product groups as JSON, streams in deterministic `id` order without a row cap, and records an audit containing the effective scope, filters, status expansion, column preset/groups, and exported row count. It is distinct from the single-product JSON round-trip export, which remains governed by `PRODUCT_RULE_009` and `products.update`.

## SKU Roles

- `product.sku` is the product-level **SKU** — always required to save (draft and publish alike), regardless of whether the product has variants. See `PRODUCT_RULE_005`. `CONFIRMED_FROM_CODE`
- `variant.sku` is the **selling SKU** — the code used in cart, checkout, and inventory to identify the actual unit being sold. `CONFIRMED_FROM_CODE`
- **`PRODUCT_RULE_SKU_001` — every variant must have a SKU, and variant SKUs must be unique.** On the admin product upsert API, each variant in the `variants[]` list must carry a non-blank `sku` (`@NotBlank` on `VariantRequest.sku`; admin form blocks save with a per-row error). Variant SKUs are **globally unique, case-insensitive** across all products: the backend rejects a save that reuses a SKU held by another variant (and the admin form flags duplicates within the same product before submit). `product.sku` has no uniqueness check regardless of variants; its own requiredness (not uniqueness) is governed by `PRODUCT_RULE_005`. `CONFIRMED_FROM_CODE`
- Uniqueness is enforced at the DB level by a partial unique index `ux_product_variants_sku_lower` on `lower(sku)` (V244), which also backfilled SKUs for legacy/WP-import variants that had none and de-duplicated pre-existing collisions. The application layer pre-validates duplicates to return a friendly error before hitting the constraint. `CONFIRMED_FROM_CODE`
- **Same-product variant replace is flush-ordered (2026-07-04 fix):** the app-layer duplicate pre-check above excludes the product's own variants (reusing your own SKU while editing is expected). `AdminCatalogMutationService.applyVariants` updates existing variants in place by `id` and treats any request row without a matching `id` as a brand-new variant — if that "new" row's SKU text matched a variant being dropped in the same save (e.g. the client omitted an existing row's `id`), Hibernate could INSERT the new row before the orphan-removal DELETE of the dropped row landed in the same flush, tripping `ux_product_variants_sku_lower` even though no other product was involved (raw `409 DATA_CONFLICT`, not a friendly field error). Fixed by removing the dropped variants from the parent's owning collection (not via a direct repository delete — that gets silently re-cascaded/resurrected while the entity is still collection-attached) and flushing before the collection is replaced, so the SKU is freed at the DB level first; verified live against a real Postgres instance (a plain repository-level delete looked correct in code but silently left the row in place — the fix must go through the collection's own orphanRemoval). `CONFIRMED_FROM_CODE`
- The `product_variants.sku varchar(100)` column stays nullable so the index ignores any future null (the requirement is a **write-time validation**, not a `NOT NULL` schema change); `products.sku varchar(100)` remains fully optional with no uniqueness. `CONFIRMED_FROM_CODE`
- When snapshotting line items into cart/order, the system uses `variant.sku` if present, otherwise falls back to `product.sku`. This fallback covers single-variant or no-variant products where the parent SKU is the selling code. `CONFIRMED_FROM_CODE`
- Inventory search reads both `p.sku` and `v.sku` so admin tools can locate stock by either code. `CONFIRMED_FROM_CODE`

Evidence:

- `ProductEntity.java` (line 34)
- `ProductVariantEntity.java` (line 29)
- `VariantRequest.java` (`@NotBlank` on `sku` — admin upsert write-time requirement)
- `ProductDetailScreen.jsx` / `schemas.js` (admin form per-row SKU required validation)
- `PosOrderService.java` (line 233 — fallback `variant.getSku() != null ? variant.getSku() : product.getSku()`)
- `CartService.java` (line 153 — same fallback)
- `CheckoutService.java` (line 723 — same fallback)
- `V1__create_catalog_content_tables.sql` (lines 65, 166 — `product_sku` / `variant_sku` columns)
- `AdminCatalogMutationService.java` (`applyVariants` — orphaned-variant delete+flush before collection replace)
- `GlobalExceptionHandler.java` (`friendlyConstraintError` — maps `ux_product_variants_sku_lower` to a field error on `variants`)

## Order Completion & Cancellation Rules

An order has one lifecycle axis: `OrderEntity.status`. Payment method and shipping metadata are reference data and do not create independent order states.

`PENDING → PROCESSING → COMPLETED`; `PENDING` and `PROCESSING` may move to `CANCELLED`. `COMPLETED` and `CANCELLED` are terminal.

- `ORDER_RULE_001` — New checkout orders start `PENDING`. Shop confirmation moves them to `PROCESSING`; the shop confirms delivery directly by moving the order to `COMPLETED`. `CONFIRMED_FROM_CODE`
- `ORDER_RULE_002` — `COD` and `BANK_TRANSFER` remain manual payment methods. No payment status field, payment-status endpoint or payment-state UI controls the order lifecycle. `CONFIRMED_FROM_CODE`
- `ORDER_RULE_003` — ~~`PROCESSING → SHIPPING` requires a non-blank tracking number and stores `shippingCarrier`/`shippedAt`.~~ **REMOVED (owner decision 2026-07-23).** The `SHIPPING` status and shipment-tracking fields (`trackingNumber`, `shippingCarrier`, `shippedAt`) were dropped entirely; `PROCESSING → COMPLETED` is now the shop's direct confirmation of successful delivery, with no tracking metadata. `REMOVED`
- `ORDER_RULE_004` — Customers may self-cancel only from `PENDING` or `PROCESSING`; they still cannot self-cancel `COMPLETED`. Admins with `orders.write` may cancel `PENDING` or `PROCESSING`. Cancellation sets `cancelledAt`, writes audit/notification/WebSocket side effects, does not restore stock, and does not trigger an automatic refund; any refund is handled manually. `CONFIRMED_FROM_CODE`
- `ORDER_RULE_005` — ~~POS auto-complete + CASH/CARD_TERMINAL paid-at-counter rule.~~ **REMOVED (owner decision 2026-06-23, online-only — see "POS Rules" banner below).** The POS create-order path no longer exists; there is no longer an order that starts `COMPLETED + PAID`. `REMOVED`
- `ORDER_RULE_006` — ~~POS completed orders cannot be cancelled directly.~~ **REMOVED (2026-06-23, online-only).** No POS order is created `COMPLETED` anymore, so this guard is moot. `COMPLETED` remains terminal in `ALLOWED_TRANSITIONS` for online orders. `REMOVED`
- `ORDER_RULE_008` — ~~POS below-cost override guard (`pos.sell_below_cost`).~~ **REMOVED (2026-06-23, online-only).** Price override only existed in the POS flow, which is gone; the `pos.*` permissions were dropped. Cost price (`cost_price`) was fully retired and dropped from the schema on 2026-07-04 as part of the pricing-model consolidation (see DATA_CONTRACT.md "Product pricing model" and `PRODUCT_RULE_012`) — it had zero remaining consumers after the POS module removal and zero populated rows in production. `REMOVED`
- `ORDER_RULE_009` — **KHÔNG có cơ chế tự hủy đơn (no auto-cancel)** (owner decision 2026-07-15, FIX_PROMPT_2026-07-15.md §2 #2). Scheduler tự hủy đơn BACS chưa thanh toán sau 72 giờ (`OrderAutoCancelService`/`OrderAutoCancelScheduler`, setting `bacs_unpaid_auto_cancel_hours`) đã bị **gỡ hẳn** — nó chưa từng được docs chuẩn hoá và trái mô hình đối soát thủ công (2026-06-23). Đơn treo/quá hạn do **admin tự xử lý bằng tay** (hủy qua màn quản trị đơn). `CONFIRMED_FROM_CODE`
- `ORDER_RULE_010` — Huỷ đơn (chuyển sang `CANCELLED`) bắt buộc phải có lý do (`cancelReason`) khi admin thực hiện. Lý do lưu vào `orders.cancel_reason`, chỉ admin nhìn thấy, không hiển thị cho khách. Backend từ chối request thiếu lý do. `CONFIRMED_FROM_CODE`
- `ORDER_RULE_011` — Bộ lọc ngày của màn Đơn hàng và file CSV dùng **ngày lịch Việt Nam** (`Asia/Ho_Chi_Minh`). `from=YYYY-MM-DD` bao gồm từ 00:00 của ngày bắt đầu; `to=YYYY-MM-DD` bao gồm hết ngày kết thúc bằng cận loại trừ 00:00 ngày kế tiếp. Không diễn giải hai giá trị này theo ngày UTC. Owner decision 2026-07-28. `CONFIRMED_FROM_CODE`
- `ORDER_RULE_012` — Nút **Xuất CSV** trong module Đơn hàng xuất **toàn bộ** đơn khớp đúng từ khoá tìm kiếm, trạng thái và khoảng ngày đang áp dụng (`q`, `status`, `from`, `to`), không chỉ trang đang xem và không được âm thầm cắt ở 10.000 dòng. Owner decision 2026-07-28. `CONFIRMED_FROM_CODE`

Evidence:

- `AdminOrderService.java` — `ALLOWED_TRANSITIONS`, cancellation guard and `cancelReason` validation
- `CheckoutService.java` — new orders start at `PENDING`
- `Phase1HAdminOrderApiTest.java` — covers the surviving rules above (happy + rejection paths)

## POS Rules

> **POS (Point of Sale / "bán tại quầy" / walk-in) was REMOVED platform-wide (owner decision 2026-06-23).** BigBike is now **online-only**: every order is placed through the storefront cart checkout and is a `DELIVERY` order. Walk-in customers are no longer recorded in the system. The endpoints `POST /admin/pos/orders` and `GET /admin/pos/products/search`, the `AdminPosController` / `PosOrderService`, the `pos.read` / `pos.write` / `pos.price_override` / `pos.sell_below_cost` permissions, and the old `POS_CUSTOMER_*` rules were all deleted. Legacy POS orders (`channel`/`fulfillmentType = IN_STORE`, `source = 'pos'`) were purged from the database. The `channel` / `fulfillment_type` / `source` columns still exist (online orders use `fulfillmentType = DELIVERY`), but `IN_STORE` / `'pos'` values are no longer written.

## Media Rules

- Media upload validation is server-side MIME/content validation using Apache Tika magic-byte detection. `CONFIRMED_FROM_CODE`
- Allowed MIME types: common raster images (`image/jpeg`, `image/png`, `image/webp`, `image/gif`), `image/svg+xml`, and MP4 video only. **Audio was removed (owner decision 2026-07-15, AUD-074)** — the backend rejects audio MIME types on upload and the admin Media Library no longer offers an Audio filter/count; legacy audio objects already in MinIO are left untouched. `CONFIRMED_FROM_CODE`
- SVG is allowed but sanitized on upload (`SvgSanitizer`): scripts, event handlers, `javascript:`/external references and CSS vectors are stripped; non-SVG content declared as `image/svg+xml` is rejected. `CONFIRMED_FROM_CODE`
- `MEDIA_RULE_007` (owner clarification 2026-07-30) — Admin Media Library uploads are accepted when the file is non-empty, within the upload-size limit, and passes declared MIME plus content detection. There is **no shared minimum pixel dimension** for raster uploads. A position-specific aspect ratio is validated when media is assigned to a screen field; the library upload itself does not reject a valid image solely because it is small. `CONFIRMED_FROM_OWNER_DECISION`
- Hard delete is blocked when a media URL is still referenced. `CONFIRMED_FROM_CODE`
- `MEDIA_RULE_002` — URL ảnh trong gallery sản phẩm (`product.gallery`) và gallery biến thể (`variant.gallery`) bắt buộc phải thuộc whitelist MinIO khi thêm mới hoặc chỉnh sửa (mediaType không phải video). Ảnh cũ (legacy) đã tồn tại từ trước vẫn được chấp nhận để đảm bảo khả năng tương thích ngược khi sửa sản phẩm cũ. **Cùng cơ chế tha ảnh cũ áp dụng cho ảnh đơn (không phải gallery): `product.image`, `category.image`/`icon`/`menuIcon`, `category.banner`, `category.mobileBanner`, `brand.logo`, `brand.banner`, `brand.mobileBanner`, `article.coverImage`, và `seo.ogImage` của cả 4 loại — so khớp với URL đang lưu trên bản ghi (`current`), khớp thì bỏ qua whitelist, chỉ URL MỚI/khác mới bị chặn** (fix 2026-07-21: trước đó nhóm field ảnh đơn này validate whitelist vô điều kiện dù không đổi, khiến ~211/229 sản phẩm + phần lớn brand/category/article còn ảnh cũ trỏ `media.bigbike.vn` không lưu được dù không đụng vào ảnh — xem `AdminMutationValidatorsTest` cho case này). **Bổ sung 2026-07-28 (audit module Thương hiệu):** `brand.banner` và `brand.mobileBanner` trước đây **không** được kiểm whitelist — admin lưu được ảnh bìa thương hiệu trỏ ra host ngoài dù web có render ảnh này làm hero trang thương hiệu; nay hai field đã đi qua cùng `validateImageAsset` như `brand.logo`, giữ nguyên cơ chế tha URL cũ. **Bổ sung cùng ngày (audit module Danh mục):** `category.banner` và `category.mobileBanner` cũng vốn không được kiểm whitelist (form quản trị đã kiểm định dạng nhưng máy chủ thì không) — nay đã đi qua cùng `validateImageAsset` như `category.image`/`icon`/`menuIcon`. `CONFIRMED_FROM_CODE`
- `MEDIA_RULE_003` (2026-07-06) — **Ảnh nhúng trong khối HTML tự do của mô tả sản phẩm và thân bài viết cũng phải thuộc kho MinIO.** Mọi `<img src>` dán trong các khối raw-HTML (`paragraph`/`callout`/`feature`) của `descriptionBlocks` sản phẩm (kể cả trong các tab sản phẩm) và `bodyBlocks` bài viết được kiểm khi lưu: ảnh **mới** trỏ ra host ngoài bị chặn (`400 INVALID_VALUE`), chỉ chấp nhận URL nội bộ `/media/`·`/media-proxy/` hoặc object MinIO. **Ảnh cũ đã có sẵn trong bản ghi được tha** (giống `MEDIA_RULE_002`) để không chặn việc sửa nội dung legacy đang nhúng ảnh host cũ (`~140 sản phẩm + ~174 bài viết` còn ảnh `bigbike.vn/wp` cũ); ảnh nhúng `data:` (base64) cũng được tha. Whitelist cho `ImageBlock`/`FeatureBlock` (ảnh có cấu trúc) nay cũng tha ảnh cũ theo cùng cơ chế. Áp dụng đồng nhất cho cả đường sản phẩm (`CatalogRequestValidator`) lẫn bài viết (`ContentRequestValidator`). **Từ V326:** mỗi khối `descriptionBlocks` sản phẩm mang cả 2 ngôn ngữ inline (`html`/`htmlEn`...) — rule này kiểm cả 2 field HTML của từng khối (`descriptionBlocks[i].html` và `descriptionBlocks[i].htmlEn`), không chỉ bản tiếng Việt. **Từ V327/V328:** `suitability`/`sizeGuide` không còn là khối trong `descriptionBlocks` — rule áp dụng riêng cho `html`/`htmlEn` của 2 field cấp cao nhất mới `suitabilitySection`/`sizeGuideSection` (cùng cơ chế tha ảnh cũ, cùng chặn ảnh ngoài mới). `CONFIRMED_FROM_CODE`
- `MEDIA_RULE_004` (owner decision 2026-06-25; cập nhật 2026-07-29) — **Mọi đường ghi video chỉ nhận đúng 2 provider: `youtube` hoặc `upload`.** `youtube` phải dùng URL video YouTube đầy đủ được parser chấp nhận; `upload` phải trỏ tới file video trong MinIO/đường media nội bộ và bao gồm cả tải MP4 mới lẫn chọn video đã có trong Media Library. Áp dụng đồng nhất cho video trang chủ, `videos[]` sản phẩm, gallery sản phẩm/biến thể và khối `video` của bài viết. Request chứa `tiktok`, `facebook`, provider lạ hoặc provider không khớp URL bị từ chối `400 INVALID_VALUE`. Dữ liệu TikTok/Facebook đã lưu **không bị xóa hay tự chuyển đổi** và đường đọc/public renderer vẫn giữ tương thích; PATCH không gửi trường video vẫn giữ nguyên dữ liệu cũ. Khi request sửa gửi lại trường video legacy, người dùng bắt buộc chọn YouTube hoặc Upload. **Mô tả chi tiết sản phẩm không còn tạo block video**; video sản phẩm là mục riêng trong Admin (`videos[]`). File JSON xuất ra có thể có `videos` đầy đủ, nhưng khi nạp JSON tạo/sửa Nháp thì hệ thống bỏ qua `videos`; muốn đổi video sản phẩm phải sửa trực tiếp trong Admin. `CONFIRMED_FROM_OWNER_DECISION`.
- `MEDIA_RULE_005` (owner decision 2026-07-21) — **Ảnh đại diện khách hàng (customer avatar)**: tối đa **5MB**, chỉ `image/jpeg`/`image/png`/`image/webp` (kiểm cả declared Content-Type lẫn Apache Tika magic-byte, cùng cơ chế review photo — `CustomerAvatarStorageService`). **Không bao giờ lưu ảnh mặc định vào MinIO** — khi khách chưa có `avatar_url`, mọi nơi hiển thị (trang tài khoản, menu trên cùng, đánh giá sản phẩm, chi tiết khách hàng trong admin) tự vẽ vòng tròn chữ cái đầu phía client, không gọi ảnh nào từ server. Chỉ **chính chủ tài khoản** upload được; admin chỉ **xem + gỡ**, không có đường nào để admin upload thay khách.
  **Bổ sung 2026-08-07, owner decision — ngoại lệ cho đăng nhập Google/Facebook:** ngoài đường tự upload (luôn qua MinIO như trên), `avatar_url` còn được điền tự động **một lần duy nhất** — thời điểm tài khoản Google/Facebook được liên kết lần đầu (tạo tài khoản mới, hoặc gắn thêm mạng xã hội vào tài khoản đã có) mà khách **chưa có** avatar nào. Trường hợp này **lưu thẳng link ảnh (hotlink) của Google/Facebook**, KHÔNG tải về lưu MinIO — chủ shop đã duyệt phá lệ quy tắc "mọi ảnh phải nằm trong MinIO" (§14.3 CLAUDE.md/AGENTS.md), phạm vi phá lệ chỉ giới hạn đúng trường `avatar_url` lấy từ OAuth, không áp dụng cho ảnh nào khác. Rủi ro đã biết: ảnh có thể hết hạn/đổi/vỡ nếu Google/Facebook thay đổi URL hoặc khách đổi quyền riêng tư ảnh — hệ thống không tự phát hiện, ảnh vỡ thì hiện lỗi vỡ ảnh phía trình duyệt cho tới khi khách tự tải avatar mới đè lên. Không chạy lại ở các lần đăng nhập sau — nên không ghi đè ảnh khách tự tải lên hoặc đã chủ động xoá. Ảnh mặc định hình người của Facebook (chưa từng đổi avatar) bị bỏ qua, không lấy. `CONFIRMED_FROM_OWNER_DECISION`
- `MEDIA_RULE_006` (2026-07-22) — **Ảnh raster được nén phía server trước khi lưu MinIO, ở cả 3 luồng upload**, để chặn đà phình dung lượng (không đổi giới hạn nhận file 5MB/8MB/200MB, không đổi định dạng chấp nhận): (1) **avatar khách hàng** — center-crop vuông rồi resize còn đúng **400×400px** (mọi nơi hiển thị chỉ 32-56px); (2) **ảnh đánh giá sản phẩm** — hạ còn tối đa **1600px** chiều rộng (khách xem phóng to trong lightbox nên giữ khá lớn); (3) **ảnh gốc thư viện media admin** — hạ còn tối đa **2000px** chiều rộng (3 biến thể thumb/medium/large sẵn có, ≤1600px, không đổi). Cả 3 dùng JPEG quality 0.85, **không upscale** ảnh đã nhỏ hơn ngưỡng, **giữ nguyên kênh alpha** cho PNG/WebP (không ép về JPEG làm mất nền trong suốt), và **fail mềm về ảnh gốc chưa nén** nếu decode lỗi hoặc SVG/GIF (không xử lý). `width`/`height`/dung lượng trả về trong response phản ánh **bản đã nén thực sự lưu trong MinIO**, không phải bản khách/admin gửi lên. **Khoảng lệch triển khai đã xác nhận 2026-07-28:** JPEG/PNG thực hiện đúng; runtime hiện chưa có ImageIO reader cho WebP nên WebP rơi vào nhánh fail-soft và được lưu nguyên bản. Đây là nợ P2 dùng chung của Media/Avatar/Review, không thay đổi rule chấp nhận WebP. `OWNER_CONFIRMED_2026-07-22; CODE_GAP_WEBP_2026-07-28`

Evidence:

- `AdminMediaService.java`
- `AdminMediaP0Test.java`
- `CatalogRequestValidator.java` (gọi `validateWhitelistedMediaUrl` cho gallery mới; `validateDescriptionBlockMediaUrls` + tập ảnh-cũ `collectBlockMediaUrls`/`suitabilitySectionMediaUrls`/`sizeGuideSectionMediaUrls` cho MEDIA_RULE_003)
- `ContentRequestValidator.java` (bodyBlocks bài viết — cùng cơ chế MEDIA_RULE_003)
- `AdminMutationValidators.java` (`validateWhitelistedMediaUrl`; `validateHtmlInlineImages`/`extractInlineImageSrcs`/`collectBlockMediaUrls`/`blockRawHtml`/`suitabilitySectionMediaUrls`/`sizeGuideSectionMediaUrls` cho ảnh trong HTML tự do)
- `CatalogRequestValidatorTest.java` (ảnh HTML ngoài mới bị chặn; ảnh cũ được tha)
- `CustomerAvatarStorageService.java`, `CustomerController.java`, `AdminCustomerController.java` (MEDIA_RULE_005 direct upload); `CustomerOAuthService.java` (MEDIA_RULE_005 OAuth hotlink exception), `next.config.ts` (`images.remotePatterns` for Google/Facebook CDN hosts)
- `ImageCompressionService.java`, `ImageCompressionServiceTest.java`, `ImageVariantService.java`, `AdminMediaService.java`, `ReviewPhotoStorageService.java`, `CustomerAvatarStorageService.java` (MEDIA_RULE_006)

## Inventory Rules

> **Serial-number tracking was REMOVED platform-wide (2026-06-23, V259).** There are no more per-unit serials, no serial lifecycle, and no serial-only selling mode. (The old `RULE-SER-*` IDs and the dedicated serial docs were deleted.)
>
> **Inventory switched to a BOOLEAN availability model (2026-06-23, V262).** There is no longer a tracked stock **quantity**. Availability is a simple **"Còn hàng / Hết hàng"** flag that the admin toggles by hand. The quantity columns are kept in the database but are **dormant** (no longer drive availability), and the `LOW_STOCK` tier no longer exists.

- Availability is a **boolean**, not a count: a variant is either available or not; a no-variant product is either in stock or out of stock. There is no on-hand number anymore. `CONFIRMED_FROM_CODE`
- Admin đặt Còn/Hết bằng tay trong form tạo/sửa sản phẩm (product-level `available` — chỉ áp dụng cho sản phẩm KHÔNG biến thể — hoặc `variants[].isAvailable`) và lưu bằng quyền `products.update`. Hai endpoint availability riêng của Inventory đã gỡ; form sản phẩm là đường ghi duy nhất. **Selling does NOT change availability** — không auto-decrement/auto-restore. `CONFIRMED_FROM_CODE`
- **Overselling is NOT auto-prevented.** When an item sells out, the admin must manually flip it to **"Hết hàng"**; until then the storefront keeps accepting orders. `CONFIRMED_FROM_CODE`
- Manual inventory movement types `IN` / `OUT` / `ADJUSTMENT` / `RETURN` are no longer written for sales or restores. (The `stock_movements` ledger is dormant for the availability model.) `CONFIRMED_FROM_CODE`
- The dormant columns `product_variants.quantity_on_hand`, `products.stock_quantity` and `products.manage_stock` are kept for compatibility but are **not** read for availability. The `low_stock_threshold` site setting was **removed (V279)**. `CONFIRMED_FROM_CODE`
- Receipt-based receiving tables were **dropped in V120** — schema-only, never implemented in Java. `REMOVED`

### Stock State Derivation Rules `CONFIRMED_FROM_CODE`

- `stockState` is a two-state badge (`IN_STOCK` / `OUT_OF_STOCK`) that **mirrors the boolean availability**. `LOW_STOCK` was **removed from the enum (V279)**. Admin cannot set `stockState` directly via the API; it is **derived on every product-form save** from the Còn/Hết toggles (`InventoryPolicyService.recomputeProductState`).
- `STOCK_RULE_001`: A new variant defaults to **available** (`is_available = true`); a new no-variant product defaults to **in stock** unless the admin flips its product-level Còn/Hết switch to "Hết hàng". `stockState` is derived to match on save.
- `STOCK_RULE_002`: Toggling availability is the only thing that changes a variant's / product's stock state. There is no quantity to recompute on sale, cancel, or return.
- `STOCK_RULE_003`: ~~Quantity thresholds (`LOW_STOCK` tier, `low_stock_threshold`).~~ **REMOVED (V262; `LOW_STOCK` enum value and `low_stock_threshold` setting fully deleted in V279).** No "sắp hết" tier exists; availability is binary.
- `STOCK_RULE_004`: **REMOVED (2026-07-19, V342).** `forceOutOfStock` — a product-level hard override that blocked purchase on web even when an item's own variant/toggle said available — was removed platform-wide (owner decision). Purchasability is now governed solely by `STOCK_RULE_005`/`STOCK_RULE_006` below; there is no separate override layer. `CONFIRMED_FROM_CODE`
- `STOCK_RULE_005`: For products **with variants**, availability is gated **per variant** by `product_variants.is_available`. The variant's `stockState` mirrors it (`IN_STOCK` if available, else `OUT_OF_STOCK`). The web variant selector (`VariantSelector.tsx`) dims unavailable options (still clickable for image preview); buying a variant requires `is_available = true`.
- `STOCK_RULE_006`: For products **without variants**, `products.stock_state` (`IN_STOCK` / `OUT_OF_STOCK`) is derived **directly from the admin toggle**, persisted as `products.available` (renamed from `force_out_of_stock` in V342; positive sense — `true` = còn hàng).
- `STOCK_RULE_007`: Sản phẩm đang **"Hết hàng"** → khách chỉ xem được, không thể đặt hàng. Không có chế độ "đặt trước" hay "HÀNG ODER" qua web. Muốn nhận đơn ODER, admin phải bật **"Còn hàng"** thì khách mới đặt được. **Lưu ý:** bán không tự chuyển sang "Hết hàng" — khi bán hết, admin phải tự tắt, nếu không web vẫn cho đặt (không tự chặn bán quá).
- `STOCK_RULE_008`: For products **with variants**, the product-level `stockState` is an **aggregate** of its variants: `IN_STOCK` if **any** variant is `is_available`, else `OUT_OF_STOCK` (only when **all** variants are unavailable). This is what the storefront product-level badge reads (`products.stock_state`) and what the admin stock list/Products filter reads. The former admin inventory grouped view was removed with the standalone screen. `CONFIRMED_FROM_CODE`
- `STOCK_RULE_009`: **Hiển thị badge tồn kho ở buy-box trang chi tiết sản phẩm (web — chỉ phần nhìn).** Cài đặt trong `WpPurchaseSection.tsx`. Badge chỉ còn **hai trạng thái**: **"Còn hàng" / "Hết hàng"** theo `stockState` (per-variant `is_available` khi đã chọn biến thể; product-level aggregate `STOCK_RULE_008` khi chưa chọn). Thông báo cũ **"Chỉ còn N sản phẩm" / "Sắp hết" đã bị gỡ** cùng với mô hình số lượng (V262). `CONFIRMED_FROM_CODE`
- `STOCK_RULE_010` (owner decision 2026-07-28): **Dữ liệu WordPress không có bằng chứng tồn kho thì nhập ở trạng thái an toàn "Hết hàng".** Importer không được suy availability từ cột số lượng đã dormant. Sản phẩm không biến thể và biến thể không có nguồn `_stock_status` tin cậy phải ghi đồng nhất `available/isAvailable = false` và `stockState = OUT_OF_STOCK`; admin rà soát rồi bật "Còn hàng" thủ công. Tuyệt đối không ghi `available=true` đồng thời `OUT_OF_STOCK`, vì admin và storefront sẽ hiểu trái nhau. `CONFIRMED_FROM_OWNER_DECISION`

Evidence:

- `AdminInventoryService.java`
- `AdminInventoryController.java` (read-only stock list/summary endpoints)
- `CheckoutService.java` (per-variant `isAvailable` gate)
- `AdminCatalogMutationService.java`
- `V120__drop_stock_receipt_tables.sql`
- `V165__aggregate_variant_product_stock_state.sql` (product-level aggregate trigger — `STOCK_RULE_008`)
- `V259__remove_serial_management.sql` (serial tracking removed — 2026-06-23)
- `V262__inventory_availability_toggle.sql` (boolean availability; backfilled `is_available` + `stock_state` from prior quantities; quantity columns kept dormant — 2026-06-23. Note: `V261` was taken by the return/refund removal, so the inventory migration is `V262`.)
- `V354__clean_legacy_product_import_artifacts.sql` (owner-approved legacy cleanup: align no-variant `available=false` with `OUT_OF_STOCK`, backfill missing variant covers from the first valid image gallery row, and remove only blank option associations — 2026-07-28)
- `V364__remove_orphan_inventory_write_permission.sql` (remove the unused standalone inventory mutation grant — 2026-07-31)
- `bigbike-web/components/wp/WpPurchaseSection.tsx` (`STOCK_RULE_009` — PDP buy-box badge display)

## Customer Administration Rules

- `CUSTOMER_RULE_001`: The only stored customer-account statuses are `ACTIVE`, `DISABLED`, `PENDING`, and `BLOCKED`. Admin status changes accept only these values; an unknown value returns validation error instead of silently yielding an empty/no-op result. `CONFIRMED_FROM_CODE`
- `CUSTOMER_RULE_002`: Setting a real customer to any non-`ACTIVE` status immediately revokes all active customer sessions. Non-`ACTIVE` accounts cannot authenticate or continue using an existing session. `CONFIRMED_FROM_CODE`
- `CUSTOMER_RULE_003`: An admin status-change `reason` is optional and limited to 1000 characters. It is retained only in the `CUSTOMER_STATUS_UPDATED` audit entry, not as a customer-profile field. `CONFIRMED_FROM_CODE`
- `CUSTOMER_RULE_004` (owner decision 2026-07-28): Admin may edit only `displayName` and `phone`. Email ownership and the separate `firstName`/`lastName` account fields are locked on the admin API. For phone, `null` (including omission) leaves the stored value unchanged, blank text clears it, and a non-blank number is normalized before validation/uniqueness comparison/storage. Common spaces, dots, parentheses and hyphens are accepted as formatting and removed during normalization; other non-phone input characters are invalid. Duplicate detection compares the normalized digit/local form in the database, covering canonical local `0…`, legacy `+84…`, and legacy formatted storage so an un-backfilled row cannot bypass uniqueness. Registration, login, profile change and password reset reuse the same normalized lookup. New WordPress customer imports also normalize phone before lookup/storage; existing database rows are not bulk-updated. Invalid input returns field-level validation; a normalized duplicate returns conflict. `CONFIRMED_FROM_OWNER_DECISION`
- `CUSTOMER_RULE_005` (owner decisions 2026-07-21 and 2026-07-28): Admin may view or remove a customer avatar but cannot upload one on the customer's behalf. Removal nulls `avatar_url`; the owned MinIO object is deleted best-effort **only after the database transaction commits**, so an audit/commit rollback never leaves the restored avatar URL pointing at an object already removed. The operation is idempotent when the customer already has no avatar. A removal that actually changes `avatar_url` writes one `CUSTOMER_AVATAR_REMOVED` audit entry; repeating the idempotent no-op does not create a false change event. `CONFIRMED_FROM_OWNER_DECISION`
- `CUSTOMER_RULE_006` (owner decision 2026-07-28): Customer purchase KPIs (`orderCount`, `totalSpent`, `avgOrderValue`, VIP qualification and the spending/order-count-based segment) exclude orders in `CANCELLED`. Order history remains complete: cancelled orders still appear in the detail history and still participate in first/last-order timestamps. `CONFIRMED_FROM_OWNER_DECISION`
- `CUSTOMER_RULE_007` (owner decision 2026-07-28): A synthetic customer imported from guest-order data may have `displayName` and `phone` corrected by an admin, but its stored account status cannot be changed to a different value. Such a status-change attempt returns conflict. `CONFIRMED_FROM_OWNER_DECISION`
- `CUSTOMER_RULE_008` (owner decision 2026-07-28): Customer-screen summary `total` and `vip` include synthetic guest-order customer rows. `active` and `newLast30Days` represent registered accounts only and therefore exclude synthetic rows. `CONFIRMED_FROM_OWNER_DECISION`
- `CUSTOMER_RULE_009` (owner decision 2026-07-28): Customer CSV export applies the same `q`, `status`, `synthetic`, and `emailVerified` filters as the Customers list and exports every matching row across all pages without a 10,000-row cap. `CONFIRMED_FROM_OWNER_DECISION`
- `CUSTOMER_RULE_010` (owner decision 2026-08-07): **Password accounts and social-login (Google/Facebook) accounts are separate identities — never merged.** A verified provider email matching an existing password account's email no longer causes an adoption/merge (this supersedes the pre-2026-08-07 behavior, where a password account with a verified email would be adopted by a matching social login). Google and Facebook can still merge with **each other** via a matching verified email (unchanged) — only merging into a *password* account is removed. When the matched account has a password, `linkOrCreate` falls through to creating a brand-new, separate account instead, with `email` left `null` on the new row to avoid the `customers_email_unique` partial-index collision with the password account's row.
  - **Known edge case:** if that same email is later used to log in with the *other* social provider too, it also fails to match the first social account (its `email` was nulled to avoid the collision above) and creates a **third**, still-separate account rather than merging with the first — so Google+Facebook merging is not guaranteed once a same-email password account already exists. Accepted trade-off, not treated as a bug; see `DATA_CONTRACT.md`.
  - **A customer with ≥1 linked Google/Facebook identity has their profile locked from self-service editing on BigBike** — `PATCH /api/v1/customer/me` (name/email/phone/password) and both avatar endpoints (`POST`/`DELETE /me/avatar`) are refused (`403 FORBIDDEN`) regardless of whether that account also happens to carry a password (e.g. a pre-existing, grandfathered account merged before this rule). Rationale: once an account authenticates via a provider, its identity fields are considered provider-owned, not BigBike-owned — the customer is directed to change name/photo on Google/Facebook instead.
  - **Every OAuth login (creation, first link, and every repeat sign-in) overwrites `display_name` and `avatar_url` with the provider's current values** (`CustomerOAuthService.syncProfileFromProvider`) — this is the *only* update path for a social account, replacing the pre-2026-08-07 "import once if empty" avatar behavior. A provider that stops returning a photo (e.g. Facebook reverting to its silhouette placeholder) clears `avatar_url` back to `null` on next login, same as removing it.
  - The customer-facing **"Tài khoản liên kết" (linked-accounts) panel is removed** from the storefront account page — with merging no longer available to password accounts, there is nothing for a self-service link/unlink UI to manage going forward. The underlying `GET /oauth/links` / `DELETE /oauth/links/{provider}` endpoints are left in place (unused by the frontend) rather than deleted.
  `CONFIRMED_FROM_OWNER_DECISION`

Evidence:

- `AdminCustomerController.java`
- `AdminCustomerService.java`
- `OrderJpaRepository.java`
- `CustomerSessionFilter.java`
- `CustomerAvatarStorageService.java`
- `Phase1IAdminManagementApiTest.java`
- `Phase1I1CustomerStatusLoginTest.java`
- `CustomerOAuthService.java` (`linkOrCreate`, `syncProfileFromProvider` — CUSTOMER_RULE_010), `CustomerAuthService.java` (`requireNotOauthManaged`), `CustomerSummary.java` (`oauthManaged` field), `CustomerOAuthApiTest.java`

## Bilingual / Auto-translation Rules

> Áp dụng cho **sản phẩm, danh mục, thương hiệu, bài viết, cài đặt (site settings)** (mọi thực thể song ngữ). **Từ 2026-07-03: không còn tự động dịch.** Tính năng tự dịch VI→EN bằng Google Gemini (và cơ chế khoá tay đi kèm `en_overrides`/`en_locked`) đã bị **gỡ bỏ hoàn toàn**. Tiếng Anh nay do admin **tự nhập tay**, dùng cùng 1 bộ ô nhập với nút chuyển VI/EN (`contentLang`) trên form — không đổi UX, chỉ đổi nguồn sinh nội dung EN.

- `TRANSLATION_RULE_001`: **Tiếng Anh nhập tay, không tự sinh.** Admin gõ trực tiếp nội dung tiếng Anh ở từng ô/khối, đổi qua nút VI/EN ở header màn chi tiết (`useContentLang()`) để xem/sửa đúng 1 ngôn ngữ tại 1 thời điểm. Không còn service dịch, không còn endpoint `POST /api/v1/admin/translate` hay `POST /api/v1/admin/translate/backfill`, không còn khoá tay theo trường (`en_overrides`/`en_locked` đã bị xoá khỏi DB). `CONFIRMED_FROM_CODE`
- `TRANSLATION_RULE_002`: **Tiếng Anh bắt buộc CHỈ khi trường tiếng Việt tương ứng đang bắt buộc — áp dụng nhất quán cho mọi thực thể song ngữ, trừ tên và slug thương hiệu là giá trị dùng chung.** Trường/khối tiếng Việt nào **không** bắt buộc thì tiếng Anh tương ứng cũng **không** bắt buộc — được lưu dở dang, admin để trống vẫn lưu được (xem `*_RULE_002` riêng từng entity: `PRODUCT_RULE_002`/`CATEGORY_RULE_002`/`BRAND_RULE_002`/`ARTICLE_RULE_002`). Trường tiếng Việt nào **đang** bắt buộc (theo validate hiện tại — request validator backend `CatalogRequestValidator`/`ContentRequestValidator` + Zod `create*Schema` phía admin) thì tiếng Anh tương ứng **cũng bắt buộc**, thiếu → chặn lưu: **Tên** (Sản phẩm/Danh mục), **Tiêu đề** (Bài viết), **Tên hiển thị site** — `site_name` (Cài đặt, xem `SETTINGS_RULE_001`). **Tên và slug thương hiệu không có bản dịch riêng**: admin nhập một lần và hệ thống dùng cùng giá trị cho VI/EN (`BRAND_RULE_001`/`BRAND_RULE_003`). Áp dụng cho **cả tạo mới lẫn sửa bản ghi cũ**, theo cơ chế **presence-flag**: tiếng Anh của field bắt buộc chỉ được kiểm khi **tạo mới**, HOẶC khi **chính field tiếng Việt tương ứng đang được đặt trong request** (admin thực sự sửa Tên/Tiêu đề đó). Một cập nhật **một phần KHÔNG chạm field bắt buộc đó** thì **không bị chặn**, kể cả khi bản ghi cũ đang thiếu tiếng Anh — ví dụ: bật/ẩn (`{visible}`) hay kéo sắp xếp (`{sortOrder}`) Danh mục, và **xuất bản/ẩn hàng loạt** Bài viết (`{publishStatus}`) đều lưu được mà không đòi tên/tiêu đề tiếng Anh. Nhưng ngay khi admin **sửa chính field tiếng Việt bắt buộc** (đặt lại Tên/Tiêu đề) thì tiếng Anh tương ứng lại bắt buộc — phải bổ sung đủ EN mới lưu được lần sửa đó. (Trước 2026-07-05 mọi lần sửa dù nhỏ đều bị chặn tới khi có EN; nới thành presence-flag để không kẹt các thao tác chỉ đổi trạng thái/hiển thị — mirror nhất quán giữa `CatalogRequestValidator` và `ContentRequestValidator`.) **Ba ngoại lệ**: `slug` (đường dẫn URL) — `slugEn` vẫn luôn tùy chọn, **backend không bao giờ tự sinh** cho sản phẩm/danh mục/bài viết (xem `PRODUCT_RULE_003`/`CATEGORY_RULE_003`/`ARTICLE_RULE_003`); riêng **màn hình quản trị Sản phẩm được phép GỢI Ý** `slugEn` từ tên tiếng Anh **chỉ khi ô đang trống** và **không bao giờ đè** giá trị admin đã nhập hoặc đã lưu — đây là tiện ích nhập liệu phía client, không phải auto-generate ở tầng lưu trữ (owner decision 2026-08-06); `body`/`bodyBlocks` (nội dung Bài viết) — bản tiếng Anh vẫn tùy chọn, coi như một khối nội dung dài tương tự specifications/FAQ của Sản phẩm, không phải field định danh cốt lõi; **brand name/slug** — dùng chung, không lấy từ `translations.en.name`/`translations.en.slug`. Fallback từng-trường về tiếng Việt khi EN trống (`COALESCE`) là **hành vi thiết kế vĩnh viễn** cho field/khối tùy chọn — không phải lưới an toàn tạm thời chờ dịch bù — và cũng là cách xử lý bản ghi cũ đang thiếu EN ở field bắt buộc cho tới khi admin tự bổ sung. `CONFIRMED_FROM_CODE`
- `TRANSLATION_RULE_003`: **Sửa tiếng Anh KHÔNG ảnh hưởng tiếng Việt.** Tiếng Việt là canonical, lưu ở cột chính; tiếng Anh ở cột `*_en` riêng. Thao tác ở chế độ EN chỉ ghi cột tiếng Anh — nay càng tuyệt đối vì không còn cơ chế tự-dịch nào có thể ghi đè ngầm. `CONFIRMED_FROM_CODE`

Evidence: `service/admin/CatalogRequestValidator.java`, `service/admin/ContentRequestValidator.java`, `service/admin/AdminMutationValidators.java` (`validateRequiredText`/`validateRequiredSlug` — nguồn xác định trường VI nào đang bắt buộc), `api/admin/dto/ProductTranslationRequest.java`/`CategoryTranslationRequest.java`/`ArticleTranslationRequest.java` (nơi validate `en.name`/`en.title` bắt buộc), `api/admin/dto/BrandTranslationRequest.java` (brand `en.name`/`en.slug` là field tương thích cũ, không bắt buộc và không điều khiển hiển thị theo `BRAND_RULE_001`/`BRAND_RULE_003`), `api/admin/dto/settings/UpdateSiteSettingRequest.java` (`valueEn`), `bigbike-admin/src/lib/schemas.js` (`create*Schema`), `bigbike-admin/src/lib/contentLang.js` (`useContentLang`). Đã gỡ (không còn trong repo): `service/integration/GeminiTranslationService.java`, `api/admin/AdminTranslateController.java`, `service/admin/TranslationBackfillService.java`, `service/admin/EnOverridesCodec.java`, `bigbike-admin/src/lib/geminiTranslate.js`, cột `en_overrides`/`en_locked` (dropped ở V312).

## Product Catalog Rules

- `PRODUCT_RULE_001`: Mỗi sản phẩm bắt buộc có **bản nội dung tiếng Việt** (canonical). **Bản tiếng Anh nhập tay** — admin tự gõ qua nút chuyển VI/EN trên form (`TRANSLATION_RULE_001`). **Tên** sản phẩm bắt buộc cả VI lẫn EN — thiếu `translations.en.name` chặn lưu (`TRANSLATION_RULE_002`). Các field/khối còn lại vẫn **tùy chọn** ở EN — để trống vẫn lưu được, web fallback về tiếng Việt
(`PRODUCT_RULE_002`): mô tả (shortDescription/descriptionBlocks), faqs, commitments, highlights
[positiveNotes/negativeNotes, gộp thành 1 field lồng — 2026-07-07], suitabilityAdvisory, seoTitle,
seoDescription, slug, và 3 khối HTML-only hiện hành `specifications`/`specStats`/`trustBadges`
(field V255-V257, cột DB `specifications_html`/`spec_stats_html`/`trust_badges_html` — **không phải**
field cấu trúc cùng tên của bảng `product_specifications`/`product_spec_stats`/`product_trust_badges`
cũ, đã backfill và DROP ở V329/V330). `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_002`: Khi đọc nội dung sản phẩm bằng tiếng Anh (`lang=en`), mỗi trường text thiếu bản tiếng Anh sẽ **tự lùi về bản tiếng Việt theo từng trường** (`COALESCE`) — hành vi **thiết kế vĩnh viễn** cho field/khối tùy chọn (`TRANSLATION_RULE_002`), đồng thời áp dụng cho bản ghi cũ đang thiếu EN ở `name` (field bắt buộc) cho tới khi admin tự bổ sung. Một sản phẩm có thể có tên tiếng Anh nhưng mô tả vẫn hiển thị tiếng Việt. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_003` (URL locale chuẩn hoá 2026-08-03): `slug` tiếng Việt là canonical tại `/product/{slug}/`; trang chi tiết tiếng Anh luôn tồn tại tại `/en/product/{slugEn hoặc slug}/`, kể cả khi `slugEn` trống. Trang danh sách tiếng Anh vẫn là `/en/products/`. `slugEn` chỉ là slug EN ưu tiên, không phải điều kiện tồn tại trang: nếu trống, URL EN dùng `slug` VI và nội dung tiếp tục fallback từng trường theo `PRODUCT_RULE_002`; nếu có, truy cập URL EN bằng slug VI phải redirect vĩnh viễn (**301 hoặc 308**) sang `slugEn`. Mỗi trang tự canonical về đúng URL locale của nó và khai `hreflang` VI/EN/`x-default` — **trừ khi bản EN chưa đạt ngưỡng đủ nội dung theo `SEO_RULE_002` hoặc bị tắt cờ theo `SEO_RULE_001`, khi đó URL EN mang `noindex`, không vào sitemap và không được khai `hreflang`** (owner decision 2026-08-06). Backend tiếp tục OR-resolve slug VI hoặc EN và các ràng buộc duy nhất hiện hành không đổi. Đổi `slugEn` sinh redirect 301 tới URL EN mới; xoá `slugEn` sinh redirect 301 tới `/en/product/{slug VI}/`, không đổi người dùng về locale VI. Route chi tiết cũ `/en/products/{slug}/` trả 301 trực tiếp tới canonical `/en/product/{slugEn hoặc slug}/`. **`slugEn` ĐƯỢC PHÉP trùng `slug` VI của CHÍNH sản phẩm đó** (owner decision 2026-08-06): sản phẩm mang tên vốn là tiếng Anh (mã máy như `komine-jk-176`, `rs-taichi-rsj345`) thì hai slug trùng nhau là hợp lệ và không được chặn lưu; màn hình quản trị chỉ hiện **cảnh báo mềm** để admin của sản phẩm tên tiếng Việt nhận ra mình nhập nhầm. Ràng buộc duy nhất **chéo sản phẩm** (`slugEn` trùng `slug`/`slug_en` của sản phẩm KHÁC) vẫn chặn cứng như cũ. `CONFIRMED_FROM_OWNER_DECISION`
- `PRODUCT_RULE_004`: **Phân biệt hành vi `lang=en` giữa WEB và ADMIN.** `PRODUCT_RULE_002` (fallback theo từng trường về tiếng Việt) áp dụng cho cả **web/public** lẫn **bigbike-admin**. Khi admin chuyển sang EN, các **danh sách** (sản phẩm, danh mục, thương hiệu, bài viết, menu, video trang chủ, Slider, Đánh giá, Highlights) **hiển thị đầy đủ toàn bộ bản ghi** — bản ghi chưa có trường tiếng Anh (`name_en`/`title_en`/`label_en` rỗng) **tự động hiển thị tên tiếng Việt làm dự phòng** (không bị ẩn). Riêng tên và slug thương hiệu không dịch theo `name_en`/`slug_en`, luôn dùng giá trị chung (`BRAND_RULE_001`/`BRAND_RULE_003`). Hành vi này đồng nhất với `PRODUCT_RULE_002`: fallback từng trường, không ẩn bản ghi. Màn **chi tiết/form soạn thảo** và **ô chọn (selector) trong form** vẫn hiện đầy đủ song ngữ để nhập liệu. Giao diện admin (menu/nút/nhãn) luôn cố định tiếng Việt. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_006`: ~~**"Hiển thị trên web" — admin bật/tắt 5 section tab của PDP, opt-in.**~~ **CHỨC NĂNG ĐÃ GỠ (2026-06-22 admin+web; 2026-07-07 gỡ luôn backend+DB).** Trước đây admin có bảng công tắc bật/tắt riêng 5 section dạng tab (`description, specifications, faqs, videos, reviews`); sản phẩm mới mặc định **tắt hết** (opt-in) → dễ gây "nhập nội dung mà không hiện". Owner chốt **bỏ hẳn**: nay **mọi khối PDP hiện thuần theo nội dung** (giống các khối ngoài tab vốn đã vậy) — không còn bật/tắt từng phần. Đã gỡ ở **admin** (ô "Hiển thị trên web" + `SectionVisibilityEditor` + `SECTION_VISIBILITY_KEYS`/`resolve…Form`) và **web** (gating trong `ProductView.tsx` + xoá `lib/utils/section-visibility.ts`) từ 2026-06-22. **Hệ quả lúc đó:** sản phẩm trước đây bị tắt một phần (nhất là SP tạo mới mặc định tắt) hiện phần đó nếu có nội dung. **Từ 2026-07-07, backend + DB cũng gỡ hẳn (không còn ngủ yên):** cột `products.section_visibility`, `products.product_tabs`, `products.promotion_content(_en)`, `products.installation_guide(_en)` bị **DROP** (`V325__drop_dead_product_fields.sql`), cùng `Product.sectionVisibility`/`tabs`/`promotionContent`/`installationGuide` (record), `ProductTab`/`ProductTabRequest`/`ProductTabsConverter`, và field tương ứng trên `UpsertProductRequest`. **Đính chính:** tài liệu bản trước ghi "`ProductView` truyền `tabs={[]}`" — kiểm tra lại code không đúng; `tabs` đơn giản chưa từng được `ProductView.tsx` đọc/tham chiếu (chết từ đầu, không phải bị set rỗng tường minh). Mục "Tùy chỉnh tab" (V231) — vốn dormant, chưa từng có UI quản lý trên admin — đã xóa hẳn cùng đợt. *(Bối cảnh cũ: bảng từng quản 16 section rồi thu hẹp về 5 tab; `suitability`/`sizeGuide`/`prosCons` đã tách thành khối riêng — nay tất cả gate theo "có nội dung".)* `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_007`: ~~**Đăng sản phẩm MỚI phải nhập đủ 10 ô bắt buộc, khác quy tắc khi sửa.**~~ **HỢP NHẤT VÀO `PRODUCT_RULE_005` (2026-07-07).** Quy tắc cũ chỉ ép field bắt buộc lúc **tạo mới**; sửa sản phẩm cũ thì lỏng hơn (SKU/Đường dẫn chỉ là nhắc nhở, không chặn lưu) — đây chính là lỗ hổng đã sửa. Nay **tạo mới và sửa chịu cùng một bộ điều kiện bắt buộc**, không còn phân biệt theo `isCreate`; xem bảng đầy đủ tại `PRODUCT_RULE_005`. Gender hiện là trường tùy chọn theo owner decision 2026-08-13. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_008`: **Tên biến thể (variant name) không do admin nhập tay — luôn tự sinh từ thuộc tính.** Backend tính `variant.name` bằng cách nối giá trị các thuộc tính của biến thể theo đúng thứ tự (ví dụ `"Đen bóng - XL"`), ưu tiên nhãn hiển thị trong từ điển thuộc tính (`attribute_values.label`) khi biến thể liên kết được, nếu không thì dùng nguyên văn giá trị đã gửi. Trường `variants[].name` **đã bị gỡ khỏi request API** (`VariantRequest` không còn field này) — admin form cũng không còn ô nhập, chỉ hiển thị tên tự tính làm preview. Biến thể không có thuộc tính nào (hiếm — hiện tại 0 bản ghi) dùng tên dự phòng `"Biến thể N"` theo vị trí trong sản phẩm. Dữ liệu cũ đã được backfill 1 lần sang quy ước mới. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_009` (2026-07-04; **chuyển JSON-only 2026-07-06** — nhánh CSV đã gỡ hẳn; **shape lồng song ngữ 2026-07-07; draft-only import + export từng sản phẩm 2026-07-15; export toàn catalog gỡ 2026-07-19; NHẬP FILE KHÔNG CÒN QUẢN LÝ BIẾN THỂ 2026-08-08**): **Nhập sản phẩm hàng loạt từ file JSON — hành vi bắt buộc.** File là **mảng object `ProductImportRow`** (mỗi object = 1 sản phẩm) — mỗi cột song ngữ gộp thành 1 object lồng ngay tại cột đó (vd `name: {nameVI, nameEN}`, `seo: {titleVI, titleEN, descriptionVI, descriptionEN, canonicalUrl}`), không còn khối `translations` rời. `ProductImportRowMapper` chuyển đổi mỗi dòng sang đúng shape `UpsertProductRequest`+`ProductTranslationRequest` mà API sửa 1 sản phẩm đã dùng, trước khi vào chung 1 luồng xử lý — 2 DTO đó không đổi. Danh mục/thương hiệu trong file ghi bằng **slug**, backend tra `findBySlug` — không tồn tại, đã ẩn hoặc nằm trong Thùng rác thì báo lỗi dòng đó, **không tự tạo**. **Danh mục/thương hiệu không bắt buộc khi dòng nhập TẠO SẢN PHẨM MỚI (owner chốt 2026-07-22):** dòng tạo mới bỏ hẳn `categorySlugs`/`categoryIds`/`categoryId` và/hoặc `brandId` sẽ **tự gán vào danh mục/thương hiệu hệ thống "Chưa phân loại"** (`uncategorized`/`uncategorized-brand`) thay vì báo lỗi thiếu — `ProductImportService.resolveCategoryAndBrand` mặc định về 2 id này khi dòng đang tạo mới (`isCreate`) và không có giá trị nào được cung cấp, đồng thời (chỉ riêng cho đúng id `uncategorized`) bỏ qua điều kiện "phải đang hiển thị, không nằm trong Thùng rác" ở cả tầng import lẫn `CatalogRequestValidator.validateAndResolveCategories` — xem thêm `CATEGORY_RULE_005`/`BRAND_RULE_004`. Đây là carve-out **riêng cho luồng import**, cùng nhóm với ảnh/video/quan hệ sản phẩm đã nêu dưới đây — **không đổi** `PRODUCT_RULE_005` của form tạo/sửa 1 sản phẩm trên trang quản trị (form đó vẫn luôn bắt buộc cả 2) và **không đổi** cổng đăng bán (`updateProductPublishStatus` vẫn chạy `validateProductFieldsRequired` đầy đủ) — sản phẩm nhập thiếu danh mục/thương hiệu vẫn chỉ ở Nháp, không đăng bán được cho tới khi admin vào trang quản trị gán danh mục/thương hiệu thật. **Dòng nhập CẬP NHẬT sản phẩm đã có** (tìm thấy theo SKU/slug) bỏ hẳn 2 khoá này thì **giữ nguyên** danh mục/thương hiệu hiện có như trước — hành vi mặc định-về-"Chưa phân loại" ở trên chỉ áp dụng khi tạo mới, không áp dụng khi cập nhật. Đối chiếu sản phẩm đã có: **SKU sản phẩm ưu tiên, fallback slug**; SKU sản phẩm trùng nhiều bản ghi → lỗi dòng đó (không đoán). **NHẬP FILE KHÔNG BAO GIỜ TẠO/SỬA/XOÁ BIẾN THỂ (owner chốt 2026-08-08).** `ProductImportRowMapper.toUpsertRequest` **không** copy `variants` của file sang `UpsertProductRequest`, nên `request.getVariants()` luôn `null` và `ProductMutationService.applyVariants` bị bỏ qua hoàn toàn: dòng **CẬP NHẬT** giữ nguyên 100% biến thể hiện có của sản phẩm (id, SKU, giá riêng, options, ảnh màu/gallery màu, mối gắn `attribute_values`, lịch sử tồn kho), dòng **TẠO MỚI** ra sản phẩm có 0 biến thể. Hệ quả bắt buộc: dòng tạo mới **luôn phải có `retailPrice` cấp sản phẩm `> 0`** (sản phẩm không biến thể → nhánh `!hasVariants` của `PRODUCT_RULE_005` đòi giá; `PublishGateException` kế thừa `ApiException` nên `processRow` bắt và báo lỗi đúng dòng đó, không hỏng cả tệp — và bước xem trước không bắt được, lỗi hiện lúc bấm Xác nhận). File cũ hoặc file Export vẫn còn mảng `variants` **vẫn nạp được**: khoá vẫn khai báo trên `ProductImportRow` (bộ đọc JSON chặn khoá lạ, xoá trường sẽ làm cả tệp không đọc được), `parseJson` gắn **cảnh báo** `variants` / `IGNORED` cho dòng đó ("Tệp có phần biến thể — hệ thống đã bỏ qua") thay vì im lặng như nhóm ảnh/video. Toàn bộ máy móc đối chiếu biến thể theo SKU (`resolveVariantIdentities`, `checkBatchVariantSkuDuplicates`, `computeVariantDiff`, phần giữ ảnh biến thể của `preserveExistingMedia`) đã gỡ khỏi code cùng ngày, kèm 5 trường đếm biến thể của `ImportRowResult` và cột "Biến thể" trên bảng kiểm tra ở Admin. Thêm/sửa/xoá biến thể chỉ còn làm ở màn hình chi tiết sản phẩm. **Dòng nhập TẠO SẢN PHẨM MỚI luôn lưu ở `DRAFT`**, bất kể `publishStatus` trong file — không tự động đăng bán sản phẩm mới qua nhập file; đăng bán lần đầu là thao tác thủ công riêng trong Admin và là nơi chạy điều kiện đủ ảnh/nội dung. **Dòng nhập CẬP NHẬT sản phẩm đã có thì `publishStatus` bị bỏ qua hoàn toàn — trạng thái đăng bán hiện tại (Nháp/Đang bán/Thùng rác) giữ nguyên, không đổi, không báo lỗi, không báo cảnh báo, bất kể file có ghi `publishStatus` gì hay không** (sửa lỗi 2026-08-08: bản trước ép cả dòng cập nhật về `DRAFT` giống dòng tạo mới, khiến `ProductMutationService.updateProduct` từ chối với lỗi "Product status changes must use the dedicated lifecycle endpoint" ngay khi sản phẩm đang cập nhật ở trạng thái Đang bán/Thùng rác — mâu thuẫn với chính mục "Cập nhật nội dung cho sản phẩm ĐÃ CÓ sẵn" của `product-template/HUONG-DAN.md`, vốn hướng dẫn shop sửa giá/nội dung sản phẩm **đang bán** bằng cách nạp lại file JSON; owner chốt: cập nhật qua nhập file giữ nguyên trạng thái đăng bán, không tự ẩn sản phẩm đang bán về Nháp). Bước xem trước (validate) gọi thẳng `CatalogRequestValidator.validateProductRequest(..., preview=false, ...)` — **không** dùng `previewProduct` (chế độ preview cố tình bỏ qua bắt buộc tên tiếng Anh và kiểm tra trùng SKU/slug liên sản phẩm, nên không phản ánh đúng những gì lưu thật sẽ chặn). Thiếu tên tiếng Anh → lỗi dòng đó, không tự lấy tên tiếng Việt thay thế. **Ảnh/video không bắt buộc trong file nhập**: dòng tạo mới vì sản phẩm còn ở Nháp, dòng cập nhật vì ảnh/gallery/video hiện có của sản phẩm được tự động giữ nguyên (`preserveExistingMedia`). Nếu file nhập có các nhóm không dùng cho Nháp thì vẫn cho nạp và hệ thống tự lược bỏ trước khi kiểm tra/lưu: `publishStatus`, ảnh đại diện, gallery ảnh, video, `relatedProductIds`, `accessoryProductIds`, và toàn bộ mảng `variants` (nhóm cuối kèm cảnh báo dòng, xem trên). Muốn đổi ảnh/video/liên quan/bộ bảo hộ hoặc đăng bán phải sửa trực tiếp ở trang quản trị, không qua nạp file. **Export JSON từng sản phẩm (`GET /import/export/{id}`)** vẫn trả về cùng shape `ProductImportRow[]` nhưng chỉ gồm 1 sản phẩm, category/brand là slug, related/accessory là SKU, có cả trạng thái, ảnh, gallery, video, ảnh/gallery biến thể, sản phẩm liên quan và hoàn thiện bộ bảo hộ để file có thể xem/sửa/nạp lại cho chính sản phẩm đó. Không còn endpoint/nút tải JSON toàn bộ catalog. `descriptionBlocks` khi nhập hoặc export từng sản phẩm chỉ nhận **khối `feature`** với `side=left|right`, đúng hai preset ảnh-trái/chữ-phải và ảnh-phải/chữ-trái của Admin; block loại khác bị từ chối khi nhập và không được phát ra khi export. Dữ liệu cũ `side=auto` được export thành `left/right` luân phiên một cách xác định, không ghi ngược vào DB, để file vừa tải có thể nạp lại. Các khoá bỏ trống khi nạp lại giữ nguyên dữ liệu cũ (presence-flag). **Ảnh chèn trong nội dung (2026-07-19, owner chốt cùng ngày): mọi ảnh trong `descriptionBlocks`/`suitabilitySection`/`sizeGuideSection` LUÔN bị xoá vô điều kiện khi nạp file — kể cả ảnh đã đúng nguồn kho MinIO của shop, khớp cách ảnh đại diện/gallery/video/ảnh biến thể đã bị lược bỏ ở trên.** Với `FeatureBlock`, chỉ mất phần ảnh, chữ vẫn giữ; `<img>` chèn trong `html`/`htmlEn` của các khối trên và của `suitabilitySection`/`sizeGuideSection` bị gỡ khỏi markup, giữ nguyên chữ xung quanh. Vì hành vi này luôn xảy ra 100% (không còn điều kiện theo nguồn ảnh hợp lệ/không hợp lệ như bản trước đó cùng ngày), hệ thống **không báo cảnh báo** cho từng dòng — im lặng như cách ảnh đại diện/gallery/video vốn đã không báo. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_010` (2026-07-22): **Một sản phẩm thuộc một hoặc nhiều danh mục; thứ tự gán là dữ liệu nghiệp vụ.** Quan hệ lưu trong `product_category_map(product_id, category_id, sort_order)`; phần tử đầu (`sort_order = 0`) là **danh mục chính** cho breadcrumb, SEO và các luồng legacy. Response sản phẩm luôn trả đầy đủ `categories[]` theo `sort_order` ổn định và vẫn trả `category` là phần tử đầu để tương thích. Create bắt buộc ít nhất một danh mục; PATCH không gửi `categoryIds` thì giữ nguyên, còn gửi mảng rỗng bị chặn. Chỉ danh mục `visible = true` và `deleted = false` được gán mới — **ngoại lệ duy nhất: danh mục hệ thống `uncategorized`, luôn được phép gán khi dòng nhập JSON tạo sản phẩm mới không cung cấp danh mục nào (`PRODUCT_RULE_009`, `CATEGORY_RULE_005`)**. `categoryId` singular chỉ là alias tạm thời cho mảng một phần tử trong một bản phát hành; không được gửi cùng `categoryIds`. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_011`: **Đơn vị tiền tệ chỉ VND.** `UpsertProductRequest.currency` chỉ chấp nhận đúng chuỗi `"VND"` nếu có gửi lên (regex `^(VND)$`), nhưng tầng lưu trữ **luôn ghi cứng `"VND"`** bất kể client gửi gì (`AdminCatalogMutationService.applyProductPatch`) — không có đường nào tạo được sản phẩm với tiền tệ khác. `CONFIRMED_FROM_CODE`
- `ATTRIBUTE_RULE_001` (2026-07-02): **Admin có thể tạo loại thuộc tính biến thể mới và xóa loại/giá trị thuộc tính — nhưng xóa bị CHẶN khi đang được sử dụng.** Trước rule này, danh sách loại thuộc tính (Color, Size, Bo, Dungtich, Gender, Model...) là dữ liệu di trú từ WordPress, chỉ đổi tên/thêm giá trị được, không tạo mới hay xóa được. Owner chốt: (1) **Tạo mới** một loại thuộc tính (vd "Chất liệu") qua `POST /admin/attributes` — `code` (mã máy, bất biến) tự sinh từ tên theo đúng quy tắc kebab-case bỏ dấu như slug sản phẩm; trùng mã → `409 CONFLICT`. (2) **Xóa** một loại thuộc tính (`DELETE /admin/attributes/{id}`) hoặc một giá trị (`DELETE /admin/attribute-values/{id}`) — **chặn với `409 CONFLICT`** nếu còn **bất kỳ** `product_variant_options` nào (biến thể của sản phẩm bất kỳ) đang liên kết tới nó; thông báo lỗi cho admin biết, **không tự động gỡ khỏi sản phẩm**. Xóa một loại thuộc tính không còn dùng sẽ cascade xóa các giá trị con (cũng đang không dùng) ở tầng DB. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_012` (2026-07-04): **Mô hình giá sản phẩm rút gọn còn đúng 2 trường: Giá niêm yết (`retailPrice`, bắt buộc, `> 0` để đăng bán — xem `PRODUCT_RULE_005`) và Giá sale (`salePrice`, tùy chọn).** `compareAtPrice` (Giá gốc/gạch ngang) và `costPrice` (Giá vốn) đã bị **xoá hoàn toàn** khỏi schema (cột DB, entity, DTO, admin UI) — không giữ ngủ yên. Nếu có nhập `salePrice`, giá trị này **phải nhỏ hơn** `retailPrice` (validate cả frontend `createProductSchema` lẫn backend `AdminMutationValidators.validateSalePriceRule`), báo lỗi rõ ràng nếu vi phạm. **Quy tắc hiển thị trên web (PDP + card sản phẩm + mọi nơi hiện giá):** có `salePrice` hợp lệ (`< retailPrice`) → hiện `retailPrice` gạch ngang + `salePrice` làm giá bán chính + badge phần trăm giảm giá; không có `salePrice` → chỉ hiện `retailPrice` làm giá bán, KHÔNG gạch ngang, KHÔNG badge/% giảm giá. **Dữ liệu lịch sử:** các bản ghi trước đây có `compareAtPrice > retailPrice` (giảm giá thật đang hiển thị — 2 sản phẩm + 69 biến thể tại thời điểm migrate) đã được chuyển 1 lần sang mô hình mới **trước khi xoá cột** (`V317__consolidate_product_pricing.sql`): `retailPrice` mới = `compareAtPrice` cũ, `salePrice` mới = `COALESCE(salePrice cũ, retailPrice cũ)`. `costPrice` không còn dữ liệu nào tại thời điểm xoá (0 sản phẩm/biến thể từng dùng, module POS từng cần nó đã gỡ — xem `ORDER_RULE_008`). `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_013` (2026-07-06; **mở rộng 2026-07-07 — giá cấp sản phẩm thành "giá chung"**): **Sản phẩm có biến thể → mỗi biến thể tự có giá riêng SẼ dùng đúng giá riêng đó cho cả hiển thị lẫn tính tiền; biến thể KHÔNG có giá riêng sẽ tự dùng "giá chung" cấp sản phẩm làm giá mặc định.** Sản phẩm **không có biến thể nào** → giá bán = giá của chính sản phẩm đó (`retailPrice`/`salePrice` cấp sản phẩm), không đổi so với trước.
  Sản phẩm **có từ 1 biến thể trở lên**:
  - **Biến thể có `retailPrice` riêng** → "tự có giá" — dùng đúng `retailPrice` riêng, và `salePrice` riêng nếu có; **không** rơi về `salePrice` chung dù chưa tự khai giá sale riêng (đây là lựa chọn chủ đích, xem quyết định chủ shop 2026-07-07: tránh nhầm lẫn 2 nguồn giá sale cho cùng 1 biến thể).
  - **Biến thể KHÔNG có `retailPrice` riêng** → dùng trọn "giá chung" của sản phẩm (`product.retailPrice`/`product.salePrice`) làm giá hiệu lực, cho cả hiển thị (trang sản phẩm, danh sách) lẫn tính tiền (giỏ hàng, thanh toán) — `VariantPricing.regularPrice`/`salePrice`/`resolveUnitPrice`.
  - **Bắt buộc phải có giá ở đâu đó:** nếu biến thể không có `retailPrice` riêng **và** sản phẩm cũng không có `retailPrice` cấp sản phẩm hợp lệ (`> 0`) → vẫn báo lỗi khi lưu/đăng (không có giá nào để dùng).
  - **Biến thể có `salePrice` riêng nhưng KHÔNG có `retailPrice` riêng → bị từ chối khi lưu** (validate ở cả `CatalogRequestValidator`/`AdminMutationValidators` lẫn `bigbike-admin/src/lib/schemas.js`) — vì biến thể đó chưa "tự có giá" nên `salePrice` riêng sẽ bị bỏ qua âm thầm nếu không chặn, dễ khiến admin tưởng đã áp dụng khuyến mãi.
  **Trước 2026-07-07:** mỗi biến thể bắt buộc phải tự khai `retailPrice` riêng vô điều kiện một khi sản phẩm có biến thể — giá cấp sản phẩm hoàn toàn không dùng được nữa (chỉ có 1 ngoại lệ hẹp, không chính thức: dữ liệu cũ trước rule apply được `VariantPricing` âm thầm fallback về giá sản phẩm cha khi tính tiền, để không chặn đơn hàng). Từ 2026-07-07, fallback này trở thành quy tắc chính thức, áp dụng cả ở đường đọc/hiển thị (`JpaCatalogReadRepository`), không chỉ lúc thanh toán — và validate được nới lỏng tương ứng thay vì chỉ vá cho dữ liệu cũ. `CONFIRMED_FROM_CODE`
- `PRODUCT_RULE_015` (owner decision 2026-07-28): **Sản phẩm rời trạng thái Đang bán thì tự động rời khối "Sản phẩm nổi bật" trên trang chủ.** Khi `publishStatus` chuyển khỏi `PUBLISHED` (về `DRAFT` hoặc `TRASH`, qua bất kỳ đường nào: đổi trạng thái, xóa mềm, hay lần lưu sản phẩm có đổi trạng thái), hệ thống đặt lại `homepageBlock = NONE` và `homepageOrder = null`. **Đăng bán lại KHÔNG tự đưa sản phẩm trở lại khối nổi bật** — admin phải chủ động thêm lại ở màn "Sản phẩm nổi bật". Trước quyết định này, sản phẩm ngừng bán vẫn giữ cờ `FEATURED_GRID`: trang chủ âm thầm hiện ít hơn số ô đã chọn (đường đọc public lọc `PUBLISHED`), và vì `POST /admin/products/homepage-blocks` từ chối mọi mã không ở `PUBLISHED` nên **mọi thay đổi sau đó của danh sách nổi bật đều bị chặn** cho tới khi admin tự tay bỏ dòng đó ra. Màn quản trị vẫn hiện cảnh báo + nhãn trạng thái cho dữ liệu cũ còn sót lại trước khi rule này có hiệu lực. Evidence: `ProductMutationService.dropFeaturedPlacementIfNotPublished`, `HomepageBlockMutationService.setHomepageBlocks`, `FeaturedProductsScreen.jsx`. `CONFIRMED_FROM_OWNER_DECISION`
- `PRODUCT_RULE_017` (owner decision 2026-08-13): **Ngừng bán là cờ độc lập với vòng đời đăng bán.** `discontinued = true` chỉ được đặt trên sản phẩm vẫn có `publishStatus = PUBLISHED` để giữ trang lịch sử công khai; sản phẩm đó bị loại khỏi danh sách/facet/tìm kiếm/sitemap/feed và không được mua. URL lịch sử `/sp/{slug}.html` vẫn trả trang 200, không `noindex`, phát `Product` JSON-LD với `offers.availability = https://schema.org/Discontinued`, và không render nút mua. Không tạo URL `/product/` mới cho bản ngừng bán; URL canonical của trang lịch sử vẫn là chính URL `/sp/` cũ. Sản phẩm `DRAFT`/`TRASH` tiếp tục tuân thủ state machine hiện hành. `CONFIRMED_FROM_OWNER_DECISION`
- `PRODUCT_RULE_019` (owner decision 2026-08-17): **Nhập HTML trong bảy khối nội dung PDP phải an toàn, khoan dung và có xác nhận.** Bản HTML đang gõ/dán chỉ là bản nháp cục bộ; không được ghi vào dữ liệu sản phẩm theo từng phím. Hệ thống phải đọc được HTML thông thường (không phụ thuộc duy nhất vào class do hệ thống sinh), báo số mục đọc được và phần bỏ qua, rồi chỉ ghi sau khi người quản lý bấm nhận và xác nhận. Nếu không đọc được mục nào, dữ liệu cũ phải giữ nguyên và phải có cảnh báo rõ ràng. Với tiếng Việt, nội dung đã nhận có thể thay đổi số mục; với tiếng Anh, chỉ cập nhật các mục tương ứng, không tự thêm/bớt hoặc làm thay đổi bản tiếng Việt. HTML tùy biến chỉ được lưu nguyên trạng qua thao tác xác nhận riêng ở các field HTML là nguồn render; FAQ và Ưu/Nhược điểm tiếp tục lưu theo shape mảng hiện hành, không thêm field HTML mới. `CONFIRMED_FROM_OWNER_DECISION`
- `PRODUCT_RULE_020` (owner decision 2026-08-17): **Bốn nguồn của bộ template nhập sản phẩm phải dùng đúng một khuôn trình bày canonical.** Khuôn `specifications` là `<table class="shop_attributes"><tbody>...<th scope="row">Tên thông số</th><td>Giá trị</td>...</tbody></table>` không có `style` inline ở table/th/td; website tự trình bày bằng class và token web. Giá trị trong ô được phép có inline HTML an toàn như `<strong>`/`<em>` và khi sửa bằng biểu mẫu phải giữ định dạng đó. Khuôn `sizeGuideSection` là bảng `<thead>`/`<tbody>` dùng đúng toàn bộ `var(--bb-...)` style token hiện hành; khuôn `suitabilitySection` là `ul.suitability-list` dùng style token hiện hành ở wrapper và `strong` tên đối tượng, đồng thời cho phép `<strong>`/`<em>` trong nội dung tên. Serializer biểu mẫu, lệnh gợi ý AI, `product-template/HUONG-DAN.md` và `product-template/mau-day-du.json` phải khớp khuôn này; không đổi shape JSON/API hay cách web hiển thị. Mẫu không được còn URL ảnh WordPress/hotlink: URL ảnh minh họa phải là đường dẫn kho media hiện hành đã kiểm chứng. `CONFIRMED_FROM_OWNER_DECISION_AND_RUNTIME_DATA`
- `CATALOG_RULE_001` (owner decision 2026-08-13): **Bộ lọc kích cỡ công khai dùng query `kich-co`.** Giá trị được chuẩn hóa không phân biệt hoa/thường; `XXXL` là alias của `3XL`, các giá trị khác được giữ theo dạng chuẩn đang có trong biến thể. Bộ lọc áp dụng ở backend trên tùy chọn kích cỡ của biến thể, không tái sử dụng tham số `size` vốn là số dòng mỗi trang. `CONFIRMED_FROM_OWNER_DECISION_AND_RUNTIME_DATA`
- `CATALOG_RULE_002` (owner decision 2026-08-14, refined 2026-08-14): **Kích cỡ công khai phải dựa trên bảng cỡ được khai báo, không suy đoán từ giá trị số.** Mỗi sản phẩm có option kích cỡ phải được gắn đúng một bảng cỡ. Bảng cỡ có tên tiếng Việt, một nhóm lọc và danh sách cỡ theo thứ tự nhập. Bốn nhóm hiện hành là đồ mặc (chữ), giày, quần (eo inch) và quần (EU). Các bảng cỡ mũ, găng tay và đồ mặc dùng chung namespace chữ để `M`/`L` không bị tách sai; giày `42`, vòng eo `42` và cỡ EU được phân biệt bằng namespace riêng. Giá trị lặp nghĩa được chuẩn hóa `2XL → XXL`, `XXXL → 3XL`, chữ thường/khoảng trắng được làm sạch; `BM`, `BL`, `BXL`, `2BM`, `2BL` và cỡ ghép vẫn là các giá trị riêng. Nhãn tiếng Anh của cỡ bằng đúng ký hiệu người dùng nhập; nhóm phụ không còn là dữ liệu hiển thị. `CONFIRMED_FROM_OWNER_DECISION_AND_RUNTIME_DATA`
- `CATALOG_RULE_003` (owner decision 2026-08-14): **Facet kích cỡ phải tính theo đúng context đang xem.** Context gồm trạng thái `PUBLISHED`, `discontinued=false`, category và toàn bộ tìm kiếm/thương hiệu/màu/giới tính/khoảng giá đang bật; bộ lọc kích cỡ hiện tại được loại khỏi mẫu tính count để hỗ trợ multi-select. Count là số sản phẩm duy nhất; bucket không có sản phẩm bị ẩn. URL cũ không có namespace (ví dụ `kich-co=42`) tiếp tục khớp mọi bảng cỡ chứa giá trị đó; URL mới dùng token namespace để phân biệt chính xác. `CONFIRMED_FROM_OWNER_DECISION_AND_RUNTIME_DATA`
- `CATALOG_RULE_004` (owner decision 2026-08-14): **Quản lý scale kích cỡ chỉ có ba ô nhập.** Admin nhập tên thang, chọn nhóm lọc và nhập danh sách cỡ phân cách bằng dấu phẩy. Mã scale, namespace, thứ tự scale, nhãn tiếng Anh, mã giá trị, thứ tự giá trị và nhóm phụ do hệ thống tự quản lý; chúng không xuất hiện trên màn hình. Cỡ trùng sau chuẩn hóa bị chặn với thông báo nêu rõ cỡ bị lặp. Scale hoặc cỡ đang được sản phẩm sử dụng không được xóa/đổi mã/làm mất liên kết. `CONFIRMED_FROM_OWNER_DECISION`
- `CATALOG_RULE_005` (owner decision 2026-08-14, refined 2026-08-15): **Bộ lọc giá công khai dùng một thanh trượt hai đầu theo dữ liệu thật.** `priceRange.minPrice` và `maxPrice` là giá hiệu lực thấp nhất/cao nhất của đúng context đang xem — gồm category descendants, tìm kiếm, thương hiệu, màu, giới tính và kích cỡ — và `priceRange` là `null` khi không có sản phẩm hoặc chỉ có một mức giá khác nhau. Giá hiệu lực dùng `salePrice` khi `salePrice > 0` và thấp hơn `retailPrice`, nếu không dùng `retailPrice`. Trục và cột mật độ luôn tính trên tập trước khi áp dụng `min_price`/`max_price`, nên không co lại sau thao tác kéo. `step: 50.000` vẫn giữ trong wire contract để tương thích; frontend không được đếm nấc từ `minPrice` và không được bẻ số khách gõ về nấc đó. Trục hiển thị dùng các mốc tròn theo dải giá (dưới 1 triệu: 50.000đ; từ 1–5 triệu: 100.000đ; trên 5 triệu: 500.000đ), tự co thưa khi cần để thanh không quá dày. Ngọn trục được chặn theo ngưỡng mật độ 90% của histogram; nấc cuối có nghĩa là "trở lên" khi vẫn còn giá ngoài ngưỡng. Khách gõ tay vẫn lọc đúng số nguyên đã nhập, kể cả ngoài trục hiển thị; giá âm/qua giới hạn URL an toàn được kẹp, hai đầu đảo được đổi lại, không hiện lỗi đỏ. Hai ô để trống khi chưa lọc, chỉ có placeholder/hint theo dải giá; nhãn trên thanh và ô nhập dùng cùng một số đầy đủ, không dùng số rút gọn khác nhau. Nhập bằng tay chỉ áp dụng sau nút `Áp dụng`/`Apply` (hoặc Enter), không áp dụng khi chỉ rời một ô; mobile chỉ ghi vào bản nháp cho đến nút xem sản phẩm, để một khoảng giá tạo tối đa một bước lịch sử. Giao diện chỉ có thanh trượt, histogram và hai ô số; bảy mức giá cố định cũ bị loại bỏ. `CONFIRMED_FROM_OWNER_DECISION`
- `CATALOG_RULE_006` (owner decision 2026-08-15): **Bộ lọc danh sách hỗ trợ chọn nhiều thương hiệu, màu, nước sơn và kích cỡ.** Nhiều giá trị trong cùng một nhóm dùng phép OR; các nhóm khác nhau dùng phép AND. Giới tính giữ một giá trị. Mỗi facet tính count sau khi áp dụng toàn bộ context khác nhưng bỏ chính nhóm đang tính, để lựa chọn OR còn khả dụng; bucket không được chọn và chắc chắn ra 0 bị ẩn. `PUBLISHED + discontinued=false` là gate chung cho cả danh sách, count và `resultCount`. `CONFIRMED_FROM_OWNER_DECISION`
- `CATALOG_RULE_007` (owner decision 2026-08-15): **Màu công khai là dữ liệu cấu hình, không phải danh sách raw của biến thể và không được hardcode trong mã.** Mười hai màu gốc hiện hành là Đen, Trắng, Xám, Bạc, Đỏ, Cam, Vàng, Nâu, Xanh dương, Xanh lá, Khaki/Rêu và Camo. Một alias có thể thuộc nhiều màu, nên màu ghép như Đen đỏ xuất hiện ở cả Đen và Đỏ. Từ chỉ màu tiếng Anh được quy về màu tương ứng; tên thương mại/họa tiết không nhận ra màu bị bỏ khỏi facet và phải xuất hiện trong báo cáo rà dữ liệu, không tự sinh bucket riêng hoặc bucket mặc định. Link màu raw cũ có alias tiếp tục được nhận và chuẩn hóa về màu gốc; link chứa giá trị đã chủ động bỏ khỏi bộ lọc được xóa điều kiện màu thay vì đưa khách tới trang trắng. Thêm màu/alias mới là thay đổi dữ liệu cấu hình, không yêu cầu sửa/triển khai lại mã. `CONFIRMED_FROM_OWNER_DECISION`
- `CATALOG_RULE_008` (owner decision 2026-08-15): **Nước sơn là facet riêng, cấu hình cùng alias màu.** Bốn giá trị hiện hành là Bóng, Nhám, Carbon và Phản quang. Khi đồng thời lọc màu và nước sơn, cùng một option màu raw của sản phẩm phải thỏa cả hai điều kiện; không được ghép màu từ option này với nước sơn ở option khác. Các tên nước sơn carbon đang có thật trong kho được cấu hình vừa thuộc màu gốc Đen vừa thuộc nước sơn Carbon (và Bóng/Nhám nếu tên có chỉ rõ), nhờ đó số Đen của Mũ bảo hiểm phản ánh đủ 15 sản phẩm mà vẫn lọc Carbon riêng được. Nhóm Màu sắc/Nước sơn không có dữ liệu trong context hiện tại phải ẩn hoàn toàn. `CONFIRMED_FROM_OWNER_DECISION_AND_RUNTIME_DATA_2026-08-15`
- `CATALOG_RULE_009` (owner decision 2026-08-15): **"Chỉ hiện hàng còn" lọc theo `product.stockState = IN_STOCK`.** Giá trị này là tổng hợp từ biến thể theo `STOCK_RULE_008`, nên sản phẩm có ít nhất một biến thể còn bán vẫn được giữ. Count còn hàng dùng chung toàn bộ context catalog và không tạo trạng thái tồn kho mới. `CONFIRMED_FROM_OWNER_DECISION`
- `CATALOG_RULE_010` (owner decision 2026-08-15, updated 2026-08-17): **Facet danh mục cha đếm toàn bộ cây con theo `CATEGORY_RULE_006`; facet thương hiệu không trả hãng có count 0.** Website khách không render dải ô danh mục con trên bất kỳ trang lưới sản phẩm nào; khách đi vào danh mục con qua menu đầu trang hoặc URL trực tiếp. Cây danh mục không phải là một nhóm lọc trong sidebar. `CONFIRMED_FROM_OWNER_DECISION`
- `CATALOG_RULE_011` (owner decision 2026-08-15): **Catalog chỉ công bố bốn kiểu sắp xếp có kết quả khác nhau:** Mới nhất (mặc định), Bán chạy, Giá tăng và Giá giảm. Bán chạy xếp theo tổng số đơn vị trong line item của đơn không `CANCELLED`, dùng khóa sản phẩm theo `REPORT_RULE_007`/`REPORT_RULE_009`; hòa thì ưu tiên sản phẩm mới hơn rồi id ổn định. Giá trị URL cũ `date`/`menu_order` được nhận như Mới nhất; `popularity` nay thực sự là Bán chạy. `CONFIRMED_FROM_OWNER_DECISION`
- `PRODUCT_RULE_014` (owner decision 2026-07-28): **Biến thể WordPress không hỗ trợ wildcard tùy chọn trống.** Importer bỏ association option có tên/giá trị trống thay vì tạo một lựa chọn không thể chọn trên web; không tự đoán màu/size và không tự xóa biến thể. Khi biến thể màu thiếu ảnh đại diện riêng nhưng có gallery, ảnh đầu gallery được dùng làm ảnh đại diện màu; không có gallery thì vẫn để trống và cổng đăng bán tiếp tục chặn theo `PRODUCT_RULE_005`. `CONFIRMED_FROM_OWNER_DECISION`
- `PRODUCT_RULE_005` (viết lại 2026-07-07; cập nhật owner decision 2026-08-13 — giới tính chuyển thành tùy chọn và không còn giá trị Unisex): **Điều kiện bắt buộc theo trạng thái nháp/xuất bản và có/không biến thể — áp dụng như nhau cho tạo mới lẫn sửa.**
  - **Sản phẩm KHÔNG có biến thể** — bắt buộc cả **Lưu nháp** lẫn **Đăng**: Tên, Đường dẫn (slug), Danh mục, Thương hiệu, SKU (cấp sản phẩm), Giá niêm yết (`retailPrice > 0`). **Giới tính là tùy chọn**, có thể để trống/null. Riêng **Ảnh đại diện** chỉ bắt buộc khi **Đăng** (không bắt buộc lúc Lưu nháp). Khi đăng, danh mục/thương hiệu phải là dữ liệu thật: `uncategorized` và `uncategorized-brand` chỉ là chỗ chờ xử lý cho file nhập, không đạt cổng đăng.
  - **Sản phẩm CÓ ≥1 biến thể** — bắt buộc cả Lưu nháp lẫn Đăng: Tên, Đường dẫn, Danh mục, Thương hiệu, SKU (cấp sản phẩm); **Giới tính là tùy chọn**, có thể để trống/null; và với **mỗi biến thể**: SKU. Khi đăng, phải có ít nhất một danh mục thật và thương hiệu không phải `uncategorized-brand`. Giá niêm yết **cấp sản phẩm** KHÔNG bắt buộc khi đã có biến thể. **Giá niêm yết mỗi biến thể** (từ 2026-07-07, xem `PRODUCT_RULE_013`) chỉ bắt buộc khi sản phẩm **không có** Giá niêm yết cấp sản phẩm hợp lệ (`> 0`) để làm "giá chung" thay thế — có giá chung hợp lệ thì biến thể được phép bỏ trống Giá niêm yết riêng (tự dùng giá chung). **Ảnh đại diện sản phẩm** chỉ bắt buộc khi **Đăng**. **Ảnh đại diện màu của biến thể** cũng chỉ bắt buộc khi **Đăng**, nhưng (sửa 2026-07-11) **CHỈ áp dụng cho biến thể CÓ thuộc tính màu** (`color`/`màu sắc`…) — biến thể **không có màu** (ví dụ chỉ có `Size`) **không bắt buộc ảnh**, khớp đúng với form (chỉ hiện ô "Ảnh đại diện màu" cho biến thể có màu) và với `applyVariants` (luôn đặt `imageUrl=null` cho biến thể không màu). Trước bản sửa này gate đòi ảnh cho **mọi** biến thể, nên sản phẩm một-màu-nhiều-size (biến thể chỉ có `Size`) bị khoá cứng không đăng bán được.
  - **Giới tính là cờ tùy chọn độc lập, không phải một lựa chọn đơn.** Sản phẩm có thể có 0, 1 hoặc 2 cờ: `Nam`, `Nữ`; `0` cờ là trạng thái hợp lệ và không chặn lưu, `2` cờ nghĩa là dành cho cả hai giới. Dữ liệu cũ `Nam`, `Nữ` và để trống được giữ nguyên tương ứng; giá trị legacy `Unisex` không được khôi phục thành lựa chọn và được quy về không gắn giới tính. Không suy diễn giới tính từ tên, danh mục hay dữ liệu khác. API canonical dùng `genders: []` theo thứ tự `Nam`, `Nữ`; bộ lọc admin chỉ có `Tất cả/Nam/Nữ`, còn storefront chỉ chọn một giới tính tại một thời điểm. `CONFIRMED_FROM_OWNER_DECISION_2026-08-14`
  - Mọi trường khác (mô tả ngắn, mô tả chi tiết, dải tin cậy, cam kết, ô số liệu nổi bật, quick answer, ưu/nhược điểm, phù hợp với ai, bảng size, thông số kỹ thuật, FAQ, SEO, mọi bản dịch tiếng Anh...) **không bao giờ bắt buộc**, dù nháp hay xuất bản. Modal checklist khi bật **Xuất bản** ở màn danh sách sản phẩm (xem cập nhật 2026-07-19 bên dưới) vẫn liệt kê một số phần này (SEO, bộ sưu tập ảnh, ô số liệu nổi bật, ưu/nhược điểm, phù hợp với ai, thông số kỹ thuật, FAQ) dưới nhãn **"Nên bổ sung"** (✓/⚠, không chặn đăng) — thuần nhắc nhở UX, không phải điều kiện bắt buộc.
  - **Thực thi ở cả 2 tầng, tạo mới lẫn sửa như nhau:** Frontend `bigbike-admin/src/lib/schemas.js` (`createProductSchema.superRefine`, không còn phân biệt `isCreate`) VÀ backend `bigbike-backend/.../service/admin/AdminMutationValidators.java` (`validateProductFieldsRequired`), gọi từ **cả 3 điểm ghi**: `AdminCatalogMutationService.createProduct`, `updateProduct`, và `updateProductPublishStatus`. **Đây là điểm vá lỗ hổng:** trước đây backend chỉ enforce tại endpoint publish-status riêng (`PATCH /products/{id}/publish`), trong khi nút "Lưu & đăng" từng có trên trang chi tiết gọi thẳng `createProduct`/`updateProduct` — nghĩa là một sản phẩm có thể được lưu ở `PUBLISHED` mà thiếu brand/ảnh mà backend không hề chặn, chỉ dựa vào checklist client-side. `previewProduct` (live preview) **không** chạy validate readiness — cố tình, để không phá UX xem trước khi form chưa hoàn chỉnh. `CONFIRMED_FROM_CODE`
  - **Cập nhật spec 2026-07-30 — đổi trạng thái ngay tại Danh sách hoặc Chi tiết:** trang chi tiết vẫn tách **Lưu nội dung** khỏi **Đổi trạng thái xuất bản**. Nút Lưu giữ nguyên hành vi: sản phẩm đang **Đã xuất bản** thì lưu nội dung và giữ `PUBLISHED`; sản phẩm mới/đang Nháp thì lưu `DRAFT`; sản phẩm trong Thùng rác dùng flow khôi phục hiện có. Với sản phẩm đã lưu ở `DRAFT` hoặc `PUBLISHED`, cả `ProductListScreen.jsx` và `ProductDetailScreen.jsx` đều được phép kích hoạt hành động riêng `PATCH /api/v1/admin/products/{id}/publish` để chuyển `DRAFT ↔ PUBLISHED`; không thêm endpoint và không gộp publish vào request lưu form. Khi `DRAFT → PUBLISHED`, hai màn hình phải dùng chung `getPublishReadiness` và `PublishChecklistModal`; còn blocker bắt buộc thì không được xuất bản. Khi `PUBLISHED → DRAFT`, phải xác nhận rõ ràng và không cần checklist. Trên trang chi tiết, nếu form đang có thay đổi chưa lưu thì action đổi trạng thái bị khóa cho tới khi người dùng lưu xong, để không xuất bản dữ liệu cũ hoặc làm mất phần đang sửa. Sau khi thành công, badge và action trên cùng trang cập nhật ngay; không điều hướng và không tải lại toàn trang. Backend giữ đúng ranh giới: create chỉ nhận `DRAFT`; update thường giữ nguyên state hiện có; chỉ endpoint `/publish` được đổi `DRAFT ↔ PUBLISHED`. `OWNER_CONFIRMED_2026-07-30`

Evidence:

- `ProductEntity.java`, `ProductSpecificationEntity.java`, `ProductFaqEntity.java` (các cột `*_en`, gồm `suitability_advisory_en` thêm ở V237; `quick_answer_summary_en` từng gỡ ở V253, thêm lại ở V300)
- `JpaCatalogReadRepository.java` (resolve locale + fallback cho web; predicate `name_en` non-blank khi `locale=en` ở `buildProductSpec`/`findCategoriesPaged`; overload `findAllCategories(locale, strictEnglish)` cho admin; brand read luôn dùng tên chung theo `BRAND_RULE_001`)
- `AdminCatalogReadService.java` (truyền `strictEnglish = "en".equals(locale)` cho tree/brands)
- `AdminContentReadService.java` + `JpaContentReadRepository.java` (`findArticlesByFilter`/`findPagesByFilter` lọc `title_en` khi `locale=en`)
- `HomeHighlightsService.java` (`listHighlights(lang, strictEnglish)` — admin ẩn slot chưa có `name_en`)
- `CatalogController.java` (`lang` param)
- `AdminCatalogMutationService.java` (`applyProductPatch` ghi cột `_en` + `slug_en`; validate uniqueness `slugEn`; auto-301 khi `slugEn` đổi)
- `ProductJpaRepository.java` (`findBySlugOrSlugEn`, `findBySlugEn`)
- `V136__add_product_bilingual_content.sql`, `V214__add_product_slug_en.sql`
- `DATA_CONTRACT.md` — "Product bilingual content"
- `bigbike-admin/src/screens/ProductListScreen.jsx` và `bigbike-admin/src/screens/ProductDetailScreen.jsx` — cùng kích hoạt endpoint `/publish`; trang chi tiết khóa action khi form dirty và cập nhật trạng thái tại chỗ; `bigbike-admin/src/screens/product-detail/constants.js` — `getPublishReadiness` (publish gate: required theo bảng draft/publish × có/không biến thể; "Nên bổ sung" = warning); `bigbike-admin/src/screens/product-detail/Modals.jsx` — `PublishChecklistModal` (ẩn nút đăng khi còn blocker)
- `bigbike-admin/src/screens/product-detail/constants.js` — `getPublishReadiness` (checklist "required" khớp `hasVariants`/`publishStatus`, không còn theo `isCreate`); `bigbike-admin/src/lib/schemas.js` — `createProductSchema` (`.superRefine` ép theo `hasVariants`/`publishStatus`, áp dụng như nhau tạo mới lẫn sửa); `bigbike-backend/.../service/admin/AdminMutationValidators.java` (`validateProductFieldsRequired`, gọi từ `createProduct`/`updateProduct`/`updateProductPublishStatus`) (PRODUCT_RULE_005; PRODUCT_RULE_007 hợp nhất vào đây). **Ảnh biến thể chỉ đòi cho biến thể có màu (sửa 2026-07-11):** `AdminMutationValidators.validateVariantFieldsRequired` (kiểm tra `ProductFieldApplier.isColorAttributeName` trên options trước khi đòi `imageUrl`), `schemas.js` (`superRefine` — `variantHasColor` gate ảnh), `constants.js` (`getPublishReadiness` — checklist `variantImages` chỉ tính biến thể có màu qua `getVariantColorValue`).
- PRODUCT_RULE_008: `VariantRequest.java` (không còn field `name`), `AdminCatalogMutationService.java` (`applyVariants` / `deriveVariantName`), `V297__derive_variant_name_from_options.sql`, `bigbike-admin/src/screens/product-detail/VariantEditors.jsx` (`deriveVariantName`, không còn `<Input>` tên biến thể trong `VariantCard`).
- PRODUCT_RULE_009: `service/admin/ProductImportService.java` (`parseJson` + pipeline import dùng chung; `applyPublishStatusRule` — set `DRAFT` chỉ khi `isCreate`, dòng cập nhật để `publishStatus` null (không đổi, `ProductMutationService.updateProduct` đọc null là giữ nguyên trạng thái hiện có) — sửa 2026-08-08, bản trước set `DRAFT` vô điều kiện kể cả cập nhật, đụng validator "chỉ đổi trạng thái qua endpoint lifecycle"; `discardExportOnlyImportFields` — bỏ qua publish/media/relations khi nhập Nháp; `resolveCategoryAndBrand` — reorder `isCreate` trước khi gọi (đọc trong `processRow`) để mặc định `categorySlugs`/`brandId` trống về `uncategorized`/`uncategorized-brand` CHỈ khi đang tạo mới, giữ nguyên khi đang cập nhật; `checkImportDescriptionBlocks` — chỉ nhận block mô tả sản phẩm trong template; `preserveExistingMedia` — giữ ảnh/gallery/video cấp sản phẩm khi cập nhật; `ProductImportRowMapper.toUpsertRequest` — CỐ TÌNH không copy `variants` sang request, đây là chốt chặn duy nhất khiến nhập file không đụng biến thể (2026-08-08); `exportProductAsTemplateJson`/`toExportRow`/`writeExportJson` cho xuất JSON từng sản phẩm — nhánh đọc/xuất CSV đã gỡ hẳn 2026-07-06, export toàn catalog đã gỡ 2026-07-19), `service/admin/CatalogRequestValidator.java` (`validateAndResolveCategories` — bỏ qua điều kiện `visible`/`deleted` riêng cho id `uncategorized`, vì `ProductMutationService.createProduct`/`updateProduct` re-resolve category đã gán ở bước import qua đúng method này), `api/admin/AdminProductImportController.java` (`/api/v1/admin/products/import/validate|commit|export/{id}`, không còn tham số `type`), `persistence/repository/catalog/ProductJpaRepository.java` (`findAllBySkuIgnoreCase`), `api/admin/dto/ImportRowResult.java` (không còn 5 trường đếm biến thể từ 2026-08-08), `bigbike-admin/src/components/ImportProductsDialog.jsx` (bảng kiểm tra không còn cột "Biến thể"; cảnh báo `variants/IGNORED` hiện ở cột Chi tiết), `V292__ensure_uncategorized_category.sql`/`V304__ensure_uncategorized_brand.sql` (nguồn 2 id fallback), bộ kit `product-template/` (`mau-day-du.json` + `HUONG-DAN.md`).
- PRODUCT_RULE_010: `ProductEntity.java` (`categories` + `@OrderColumn`), `V348__restore_product_category_map.sql`, `CatalogRequestValidator.java`, `ProductMutationService.java`, `JpaCatalogReadRepository.java`.
- PRODUCT_RULE_011: `UpsertProductRequest.java` (field `currency`), `AdminCatalogMutationService.java` (`applyProductPatch`, dòng `entity.setCurrency("VND")`).
- PRODUCT_RULE_012: `ProductEntity.java`/`ProductVariantEntity.java` (2 cột `compare_at_price`/`cost_price` bị xoá), `domain/catalog/ProductPrice.java` (record còn `retailPrice`/`salePrice`/`currency`), `ProductSnapshotResponse.java`, `service/admin/AdminMutationValidators.java` (`validateSalePriceRule`), `service/admin/CatalogRequestValidator.java`, `bigbike-admin/src/screens/ProductDetailScreen.jsx` (SectionCard "Giá & trạng thái" còn 2 ô), `bigbike-admin/src/lib/schemas.js` (`createProductSchema`), `bigbike-web/lib/pricing.ts` (`derivePricing`), `V317__consolidate_product_pricing.sql`.
- PRODUCT_RULE_013: `service/pricing/VariantPricing.java` (`resolveUnitPrice`/`regularPrice`/`salePrice`/`hasOwnPrice`), `service/checkout/CheckoutSupport.java`, `service/cart/CartService.java` (cả hai gọi qua `VariantPricing`), `repository/catalog/JpaCatalogReadRepository.java` (`toVariant`/`toVariantForListing` gọi `VariantPricing` — fallback áp dụng cả lúc hiển thị, từ 2026-07-07), `service/admin/CatalogRequestValidator.java` + `service/admin/AdminMutationValidators.java` (`validateVariantFieldsRequired` — Giá niêm yết biến thể chỉ bắt buộc khi sản phẩm không có giá chung hợp lệ; chặn `salePrice` riêng khi biến thể chưa có `retailPrice` riêng), `bigbike-admin/src/lib/schemas.js` (mirror cùng ma trận phía client), `DATA_CONTRACT.md` — "Checkout/charging rule".
- ATTRIBUTE_RULE_001: `AdminAttributeController.java` (`createAttribute`, `deleteAttribute`, `deleteAttributeValue`), `AdminAttributeService.java` (cùng tên method + guard `variantOptionRepo.countByAttribute_Id`/`countByAttributeValue_Id` ném `ConflictException`), `ProductVariantOptionJpaRepository.java`, `CreateAttributeRequest.java`, `API_CONTRACT.md` — "Attribute value management — admin catalog endpoints", `bigbike-admin/src/screens/product-detail/VariantEditors.jsx` (`CreateAttributeModal`, nút xóa trong `AttributeRenameModal`/`AttributeValueManagerModal`), `bigbike-admin/src/lib/adminApi.js` (`createAttribute`, `deleteAttribute`, `deleteAttributeValue`).
- PRODUCT_RULE_006 (CHỨC NĂNG GỠ 2026-06-22 admin+web; 2026-07-07 gỡ backend+DB): admin gỡ ô "Hiển thị trên web" + `SectionVisibilityEditor` + `SECTION_VISIBILITY_KEYS`/`sectionHasContent`/`parse|resolveSectionVisibilityForm` (`ProductDetailScreen.jsx`, `product-detail/ContentEditors.jsx`, `product-detail/constants.js`, `lib/contracts.js`, locales); web gỡ gating trong `components/catalog/ProductView.tsx` + xoá `lib/utils/section-visibility.ts` + bỏ field `lib/contracts/public.ts`. **2026-07-07: xoá luôn backend + DB** — `V325__drop_dead_product_fields.sql` drop `products.section_visibility`/`product_tabs`/`promotion_content(_en)`/`installation_guide(_en)`; xoá `Product.sectionVisibility`/`tabs`/`promotionContent`/`installationGuide` (record), `ProductTab`/`ProductTabRequest`/`ProductTabsConverter`, field tương ứng trên `UpsertProductRequest` + apply logic trong `AdminCatalogMutationService`.

### Admin price-entry semantics (2026-08-18)

- Khi nhập trên `bigbike-admin`, `salePrice = 0` được hiểu là **để trống** và được chuẩn hóa thành `null` trong payload. Đây chỉ là quy ước của giao diện admin; không thay đổi quy tắc giá sale phải thấp hơn giá niêm yết, không thêm trần giá và không thay đổi shape API/backend.

## Review And Rating Display Rules

- `REVIEW_RULE_001`: Chỉ review trạng thái **APPROVED** được tính vào điểm trung bình và số lượng đánh giá hiển thị. Review `PENDING` / `SPAM` / `TRASH` không bao giờ xuất hiện trên web và không được tính. `CONFIRMED_FROM_CODE`
- `REVIEW_RULE_002`: Điểm hiển thị = **trung bình cộng** điểm của tất cả review đã duyệt, làm tròn **1 chữ số thập phân, half-up** (ví dụ `[5, 4, 3]` → `4.0`; `[5, 2]` → `3.5`). Quy ước này thống nhất ở 3 nơi: cache `products.rating` (`AdminReviewService.toCachedRating` — `RoundingMode.HALF_UP`), `avgRating` của API public reviews (`PublicReviewService.roundAverage`), và SQL backfill `V63`. Giá trị hiển thị trên web phải khớp giữa `rating` (denormalized trên Product), `avgRating` (API reviews) và trung bình cộng thực tế. `CONFIRMED_FROM_CODE`
- `REVIEW_RULE_003` (cập nhật owner decision 2026-08-15): **Public aggregate-rating widget trên trang chi tiết/khối đánh giá chỉ hiển thị đúng hai phần theo thứ tự: hàng 5 sao rồi số lượt đánh giá dạng `({n})`; riêng thẻ sản phẩm trên trang danh sách chỉ render cụm này khi có ít nhất một review `APPROVED`.** Vì vậy product card không có review phải để trống hoàn toàn, không hiện 5 sao rỗng, không hiện `(0)` và cụm rating trên card không phải nút mở form viết đánh giá. Ở các widget chi tiết còn lại, khi `ratingCount = NULL` hoặc `0` vẫn hiển thị 5 sao viền/rỗng trung tính và `(0)`. Khi có ít nhất 1 review `APPROVED` và điểm hợp lệ, hiển thị sao tô một phần theo điểm trung bình đã làm tròn 1 chữ số `HALF_UP` và `(n)`. Khi có lượt đánh giá nhưng điểm thiếu/không hợp lệ, hiển thị sao viền/rỗng trung tính và `(n)` với số lượt thật; không tạo điểm giả. Không hiển thị nhãn trạng thái hoặc điểm trung bình dạng chữ như `4.5`/`4.5/5`. Aria-label đầy đủ của từng trạng thái vẫn phải được giữ cho trình đọc màn hình. Số lượt chỉ hiển thị một lần trong mỗi widget; tại PDP, `reviewCount` và `ratingValue` của schema.org dùng thẻ `<meta>` ẩn. Microdata/schema.org `aggregateRating` chỉ xuất khi count ≥ 1 và điểm hợp lệ. `OWNER_CONFIRMED_2026-08-15`
- `REVIEW_RULE_005`: Một review có thể kèm **tối đa 10 ảnh thực tế của khách** (`photos`). Ảnh do khách tải lên phải nằm trong **MinIO** đúng namespace URL nội bộ `/media/reviews/...`; cấm URL `/media/...` thuộc module khác và cấm link ngoài. Mỗi ảnh chỉ nhận định dạng `image/jpeg|png|webp`, ≤ 8MB. Ảnh **chỉ hiển thị công khai khi review ở trạng thái `APPROVED`** — duyệt chung với review theo `REVIEW_RULE_001`, không có moderation riêng cho từng ảnh. Ảnh **không** ảnh hưởng điểm trung bình, `ratingCount`, hay `aggregateRating` (gate hiển thị sao vẫn theo `REVIEW_RULE_003`). Mỗi object ảnh upload chỉ được claim nguyên tử cho **một** review; submit phải chứng minh object đã được endpoint upload ảnh Review tạo cho đúng sản phẩm, không chỉ tin URL từ client. `OWNER_CONFIRMED_2026-07-28` (field `title` bị gỡ bỏ hoàn toàn — `V298__drop_review_title.sql`, 2026-07-01)
- `REVIEW_RULE_004`: `ratingCount`/`totalReviews` là tín hiệu có review; không dùng riêng `rating > 0` để suy ra review. **Importer WP đã được sửa để không còn tạo rating ảo**: `WordPressProductMapper` không default `4.5` khi WP meta thiếu (để `null`), `ProductImporter` không seed `rating` từ meta sản phẩm, và `ReviewImporter` recompute `rating` / `rating_count` từ review **APPROVED** sau khi import (0 review duyệt → `rating = NULL`, `rating_count = 0`; trung bình HALF_UP 1 decimal — `REVIEW_RULE_002`). Cache rating do đó chỉ phản ánh review thật. Bản ghi tồn dư hoặc payload không nhất quán phải dùng trạng thái sao trung tính, không được hiển thị điểm giả. `ratingCount` không được dùng để suy ra số lượt mua; nếu có dữ liệu bán hàng thì phải có nhãn riêng. `CONFIRMED_FROM_CODE`
- `REVIEW_RULE_006` (owner decision 2026-07-21): **Ảnh đại diện tác giả đánh giá là ảnh tài khoản thật, không phải bản chụp đóng-đinh.** Khi review được gửi lúc khách đang có session hợp lệ (`reviews.customer_id` khác null), danh sách đánh giá công khai resolve `authorAvatarUrl` **trực tiếp từ `avatar_url` hiện tại** của khách tại thời điểm đọc (không lưu bản sao đóng-đinh lúc gửi) — khách đổi/xoá avatar sau đó thì các đánh giá đã đăng cập nhật theo ở lần đọc tiếp theo. Đánh giá gửi lúc chưa đăng nhập (`customer_id` null) luôn có `authorAvatarUrl: null` → web tự vẽ chữ cái đầu, giống hệt cách xử lý cho khách đã đăng nhập nhưng chưa có avatar (không phân biệt màu nền theo trạng thái đăng nhập, tránh danh sách đánh giá bị lộn xộn). `CONFIRMED_FROM_CODE`
- `REVIEW_RULE_007` (owner decision 2026-07-21; backend enforcement chốt 2026-07-28): **Khách đã đăng nhập không tự nhập tên/email khi đánh giá.** Form đánh giá ẩn hẳn 2 ô `authorName`/`authorEmail` khi có session hợp lệ, thay bằng dòng xác nhận danh tính (ảnh đại diện + `displayName`, fallback phần trước `@` của email). Backend **bỏ qua hoàn toàn** `authorName`/`authorEmail` trong body khi có session và đọc cả hai từ tài khoản hiện tại; client gửi tên/email giả không được làm thay đổi review. Khách chưa đăng nhập giữ nguyên form cũ: tên bắt buộc, email tùy chọn và phải hợp lệ nếu có. Email gõ tay **không bao giờ** được dùng để suy ra tài khoản — chỉ session cookie mới xác định `customer_id`. `OWNER_CONFIRMED_2026-07-28`
- `REVIEW_RULE_008` (owner decision 2026-07-22): **Khách được chọn nửa sao khi gửi đánh giá.** `reviews.rating` nhận giá trị thập phân bước **0,5**, từ `1.0` đến `5.0` (**9 mức**: 1, 1.5, 2, 2.5, 3, 3.5, 4, 4.5, 5) — DB check constraint và validation request đều enforce bước 0,5, không nhận giá trị lẻ khác (VD `4.2`). Cách tính điểm trung bình + làm tròn 1 chữ số thập phân **không đổi** (`REVIEW_RULE_002` áp dụng y nguyên trên nguồn dữ liệu thập phân). **Biểu đồ phân bố đánh giá** (`ratingBreakdown`, hiển thị trên PDP web và tab tổng quan admin) tách riêng dòng cho từng mức nửa sao — 9 dòng: `5, 4.5, 4, 3.5, 3, 2.5, 2, 1.5, 1` (không gộp `4.5` vào nhóm `4` hay `5`), key dạng chuỗi số không có số 0 thừa (`"4.5"`, `"5"`, không phải `"5.0"`). **Bộ lọc theo sao** (web: bấm dòng biểu đồ để lọc danh sách; admin: dropdown lọc review) áp dụng cùng 9 mức thập phân này, thay vì chỉ 5 mức nguyên như trước. Dữ liệu review cũ (nhập từ WordPress, luôn là số nguyên 1-5) không bị ảnh hưởng — vẫn là các giá trị hợp lệ trong tập 9 mức trên.
- `REVIEW_RULE_009` (owner decision 2026-07-28): **Vòng đời kiểm duyệt có kiểm soát.** Review public mới luôn bắt đầu `PENDING`. Chỉ cho `PENDING → APPROVED|SPAM|TRASH`; từ `APPROVED`, `SPAM` hoặc `TRASH` chỉ được đưa về `PENDING`; chọn đúng trạng thái hiện tại là no-op (không đổi version, không ghi audit, không tính lại điểm, không gửi email). UI phải xác nhận trước thao tác duyệt công khai, đánh spam/đưa vào thùng rác và xóa vĩnh viễn. Email báo duyệt chỉ gửi **một lần duy nhất trong đời review**, ở lần đầu chuyển vào `APPROVED`, được đánh dấu bền vững trong DB và chỉ dispatch sau khi transaction commit. `OWNER_CONFIRMED_2026-07-28`
- `REVIEW_RULE_010` (owner decision 2026-07-28): **`TRASH` là thùng rác có thể khôi phục; xóa vĩnh viễn chỉ từ `TRASH` và chỉ `SUPER_ADMIN`.** Người xóa vẫn phải có `reviews.write`. Mutation đơn và từng mục bulk phải gửi `expectedVersion`; dữ liệu stale trả xung đột thay vì ghi đè quyết định mới hơn. Bulk xử lý best-effort theo từng item, khử ID trùng, `affected` chỉ đếm ID distinct thực sự đổi/xóa và trả `skipped[]` với lý do máy đọc được (`DUPLICATE_ID`, `NOT_FOUND`, `VERSION_CONFLICT`, `INVALID_TRANSITION`, `NOT_IN_TRASH`, `NO_CHANGE`). `OWNER_CONFIRMED_2026-07-28`
- `REVIEW_RULE_011` (owner decision 2026-07-28): **Giới hạn dữ liệu nhạy cảm và vòng đời ảnh.** Email tác giả chỉ có trong API chi tiết Review, không có ở danh sách. Audit Review không lưu email, nội dung nhận xét hoặc URL ảnh; chỉ lưu metadata an toàn như ID, trạng thái, điểm và số ảnh. Ảnh chỉ nhận URL `/media/reviews/...`; upload được ghi nhận trong bảng claim bền vững, claim nguyên tử đúng một review. Upload chưa claim quá 24 giờ phải được job tự dọn. Xóa review chỉ dọn object sau commit và chỉ khi không còn tham chiếu legacy khác; review bị cascade khi xóa sản phẩm cũng được job dọn qua claim mồ côi. `OWNER_CONFIRMED_2026-07-28`
- `REVIEW_RULE_012` (owner decision 2026-08-08): **Kiểm duyệt tự động bằng AI — chỉ được CHẶN, không bao giờ được DUYỆT.** Đây là lớp lọc chạy thêm *sau* `REVIEW_RULE_009`, không thay thế nó.
  - **Điểm vào:** review public mới **vẫn luôn được ghi ở `PENDING`** đúng như `REVIEW_RULE_009`. Kiểm duyệt tự động chạy **sau khi transaction gửi đánh giá đã commit**, bất đồng bộ (`@Async`), rồi mới thực hiện chuyển trạng thái qua đúng đồ thị `PENDING → SPAM|TRASH` đã cho phép. Khách không phải chờ AI; thời gian trả lời form không đổi.
  - **AI không có quyền đăng bài.** Kết luận `CLEAN` **giữ nguyên `PENDING`** để người duyệt tay — không tồn tại đường nào cho phép kiểm duyệt tự động chuyển sang `APPROVED`, kể cả khi bật hết tuỳ chọn. Hệ quả: máy móc này không bao giờ tự đẩy nội dung lên web, và email báo duyệt (`REVIEW_RULE_009`) vẫn chỉ phát sinh từ thao tác của người thật.
  - **Hai lớp lọc, chạy theo thứ tự, dừng ở lớp đầu tiên bắt được:** (1) **danh sách từ cấm** do shop tự quản trong Cài đặt — so khớp cục bộ, tức thì, không tốn phí, không gọi mạng; (2) **AI** đọc hiểu nội dung. Lớp (1) bắt được thì **không gọi AI** (tiết kiệm phí). Nhận xét rỗng (khách chỉ chấm sao) **không gọi AI**. **Hai lớp có phạm vi điều khiển khác nhau:** 4 công tắc loại vi phạm chỉ chi phối lớp AI; **từ cấm luôn chặn vào `TRASH` kể cả khi tắt hết 4 loại đó**, vì shop đã tự tay ghi từ ấy vào danh sách — đó là một chỉ thị tường minh, không phải một phán đoán theo loại. Muốn ngừng chặn theo từ cấm thì xoá từ khỏi danh sách, không phải tắt công tắc.
  - **Bốn loại vi phạm và nơi đến, bật/tắt độc lập từng loại trong Cài đặt:** `PROFANITY` (chửi tục, thô tục) → **`TRASH`**; `HARASSMENT` (xúc phạm, công kích, kỳ thị) → **`TRASH`**; `SENSITIVE` (18+, chính trị, tôn giáo, rác vô nghĩa không liên quan sản phẩm) → **`TRASH`**; `ADVERTISING` (quảng cáo shop khác, link, số điện thoại, Zalo, mã giới thiệu) → **`SPAM`**. Một review dính nhiều loại thì loại có mặt trong danh sách `TRASH` **thắng** (an toàn hơn). Loại bị tắt trong Cài đặt thì AI vẫn có thể nhận diện nhưng **không** dùng để chặn — chỉ ghi lại làm ghi chú cho người duyệt.
  - **Fail-safe: lỗi thì không chặn gì cả.** Chưa cấu hình khoá dịch vụ AI, tắt công tắc trong Cài đặt, AI lỗi mạng/quá hạn chờ/trả về sai định dạng → review **ở nguyên `PENDING`** và được đánh dấu `moderation_source = SKIPPED` kèm lý do máy đọc được. Không bao giờ chặn nhầm vì hệ thống hỏng, và không bao giờ ném lỗi ngược ra form của khách.
  - **Không giẫm lên quyết định của người.** Trước khi chuyển trạng thái, hệ thống đọc lại review: chỉ chuyển khi **vẫn còn đúng ở `PENDING`**. Người duyệt tay xong trước AI thì kết quả AI chỉ được ghi lại làm ghi chú, không đổi trạng thái. Kiểm duyệt tự động chỉ chạy **một lần duy nhất trong đời review**, tại thời điểm khách gửi — khôi phục từ Thùng rác về `PENDING` **không** kích hoạt AI chạy lại, nên người luôn là tiếng nói cuối cùng.
  - **Ghi nhật ký:** mỗi lần chặn tự động ghi một dòng audit `REVIEW_AUTO_MODERATED` với `actorType = SYSTEM`, `actorId = null` — phân biệt rõ với `REVIEW_STATUS_CHANGED` do người thật bấm. Nội dung audit tuân thủ nguyên `REVIEW_RULE_011`: chỉ metadata an toàn (id, trạng thái trước/sau, điểm, số ảnh, nguồn chặn, danh sách loại vi phạm) — **không** lưu email, **không** lưu nội dung nhận xét, **không** lưu câu giải thích của AI.
  - Kiểm duyệt tự động **không** tính lại điểm trung bình theo cách riêng: nó đi qua đúng `recomputeProductReviewAggregate` như mọi chuyển trạng thái khác, mà `PENDING → SPAM|TRASH` vốn không đổi tập review `APPROVED` nên điểm sao thực tế không nhúc nhích (`REVIEW_RULE_001`/`002`). `OWNER_CONFIRMED_2026-08-08`
- `REVIEW_RULE_013` (owner decision 2026-08-08): **Cấu hình, chi phí và dữ liệu gửi ra ngoài của kiểm duyệt AI.**
  - **Công tắc và tuỳ chọn nằm trong Cài đặt (nhóm `review_moderation`), shop tự đổi, không cần lập trình viên:** `review_moderation_enabled` (mặc định **tắt** — phải có khoá dịch vụ AI rồi mới bật), 4 công tắc loại vi phạm `review_moderation_block_profanity|harassment|advertising|sensitive` (mặc định **bật cả 4**), và `review_moderation_banned_words` (danh sách từ cấm, ngăn bằng dấu phẩy hoặc xuống dòng, mặc định rỗng). Cả 6 khoá đều **không** public — không lộ ra API công khai.
  - **Khoá dịch vụ AI là bí mật hạ tầng, không phải cài đặt trong app:** đặt ở biến môi trường `GEMINI_API_KEY` (trên VPS là file `.env.vps`, local là `.env`) (kèm `BIGBIKE_REVIEW_MODERATION_MODEL`, `BIGBIKE_REVIEW_MODERATION_TIMEOUT_SECONDS`). **Cấm** đưa khoá vào bảng `site_settings` hay bất kỳ API admin nào — registry cài đặt vốn đã chặn mọi khoá chứa `api_key` khỏi endpoint public, nhưng ở đây quyết định là không lưu trong DB chút nào.
  - **So khớp từ cấm phải bỏ dấu và bỏ ký tự chèn** trước khi so, để `đm`, `dm`, `đ.m`, `d-m` cùng bắt được một mục trong danh sách; so khớp không phân biệt hoa thường và chỉ tính khi khớp **trọn từ**, tránh chặn oan từ chứa chuỗi con vô hại.
  - **Dữ liệu gửi sang dịch vụ AI được giới hạn tối thiểu:** chỉ **nội dung nhận xét** (cắt còn tối đa 4.000 ký tự) và **số sao**. **Không** gửi email, **không** gửi tên khách, **không** gửi ảnh, **không** gửi `customer_id`, **không** gửi thông tin đơn hàng. Nhận xét dài hơn ngưỡng bị cắt trước khi gửi, không bị từ chối.
  - **Chi phí và trần chi tiêu:** mỗi đánh giá tốn đúng **một** lượt gọi AI, chỉ khi bật công tắc và lớp từ cấm không bắt được. Lượt gọi có giới hạn thời gian chờ; hết giờ tính là `SKIPPED` (giữ `PENDING`), không thử lại — không có vòng lặp thử lại nào có thể nhân chi phí lên. Ngoài ra có **trần cứng theo ngày** `review_moderation_daily_limit` (mặc định **200**, tính theo ngày dương lịch giờ Việt Nam): chạm trần thì ngừng gọi AI tới hết ngày, mọi đánh giá sau đó là `SKIPPED` với lý do `DAILY_LIMIT_REACHED` và nằm ở `PENDING`; **lớp từ cấm vẫn chạy** vì miễn phí. Đặt `0` = tắt hẳn lớp AI nhưng giữ lớp từ cấm. Trần này cần thiết vì chặn tần suất của web chỉ là **5 lượt gửi/phút mỗi IP** — bó được một IP ở ~7.200 lượt/ngày và không bó được nhiều IP. Bộ đếm không dùng bảng riêng: mỗi lượt gọi AI để lại đúng một dòng `moderation_source = 'AI'`, nên đếm số dòng trong ngày là ra và số liệu sống sót qua khởi động lại. `OWNER_CONFIRMED_2026-08-08`

Evidence:

- `AdminReviewService.java` (`updateStatus`, `deleteReview` → `recomputeProductReviewAggregate`: 0 approved → `rating = NULL`, `rating_count = 0`; `toCachedRating` HALF_UP 1 decimal)
- `PublicReviewService.java` (`getProductReviews` — chỉ APPROVED; `roundAverage`)
- `ReviewJpaRepository.java` (`findAggregateByProductIdAndStatus`)
- `WordPressProductMapper.java` (không còn default 4.5), `ProductImporter.java` (không seed rating từ meta), `ReviewImporter.java` (`recomputeRatingCache` từ review APPROVED sau import — `REVIEW_RULE_004`)
- `bigbike-web/lib/rating.ts` (`resolveRatingDisplay`/`hasApprovedReviews` — state và gate dùng chung phía web)
- `bigbike-web/components/ui/RatingStars.tsx`, `RatingDisplay.tsx`, `ProductCard.tsx`, `RatingBlock.tsx`, `RatingSummary.tsx`, `ReviewCard.tsx`, `ReviewsSection.tsx` (các nơi render rating theo `REVIEW_RULE_003`)
- `API_CONTRACT.md` — "Public Reviews Contract"; `DATA_CONTRACT.md` — "Product rating denormalization"
- `PublicReviewController.java` (`optionalCustomerId`), `PublicReviewService.java` (danh tính tài khoản authoritative + batch avatar resolve), `CustomerJpaRepository.java`
- `bigbike-web/components/catalog/reviews/WriteReviewForm.tsx` + `api.ts` (`signedInProfile` — ẩn ô tên/email; mutation gọi thẳng API public với cookie host API để backend tự lấy danh tính: `REVIEW_RULE_006`/`007`)
- `service/review/ReviewModerationService.java` (điều phối 2 lớp lọc + fail-safe), `ReviewBannedWordMatcher.java` (bỏ dấu, khớp trọn từ), `ReviewModerationSettings.java` (đọc 6 khoá cài đặt), `ReviewModerationVerdict.java` (`REVIEW_RULE_012`/`013`)
- `service/review/AiReviewModerationClient.java` (gọi Google Gemini qua `RestClient` — cùng lối bản tích hợp Gemini cũ; chỉ gửi nhận xét + số sao, có hạn chờ, không thử lại — `REVIEW_RULE_013`)
- `AdminReviewService.applyAutoModeration` (chuyển trạng thái với actor `SYSTEM`, đọc lại `PENDING` trước khi ghi — `REVIEW_RULE_012`), `PublicReviewService.submitReview` (kích hoạt sau commit)
- `V380__add_review_ai_moderation.sql` (5 cột kiểm duyệt trên `reviews` + seed 6 khoá cài đặt nhóm `review_moderation`)

## Category Catalog Rules

- `CATEGORY_RULE_001`: Mỗi danh mục bắt buộc có **bản nội dung tiếng Việt** (canonical). **Bản tiếng Anh nhập tay** — admin tự gõ qua nút chuyển VI/EN trên form (`TRANSLATION_RULE_001`). **Tên** danh mục bắt buộc cả VI lẫn EN — thiếu `translations.en.name` chặn lưu (`TRANSLATION_RULE_002`). Các field còn lại (mô tả, introContent, seoTitle, seoDescription, slug) vẫn tùy chọn ở EN, web fallback về tiếng Việt (`CATEGORY_RULE_002`). `CONFIRMED_FROM_CODE`
- `CATEGORY_RULE_002`: Khi đọc danh mục bằng tiếng Anh (`lang=en`), mỗi trường thiếu bản tiếng Anh sẽ **tự lùi về bản tiếng Việt theo từng trường** — hành vi thiết kế vĩnh viễn cho field tùy chọn, đồng thời áp dụng cho bản ghi cũ đang thiếu EN ở `name` (field bắt buộc) cho tới khi admin tự bổ sung. `CONFIRMED_FROM_CODE`
- `CATEGORY_RULE_003` (URL locale chuẩn hoá 2026-08-01): `slug` tiếng Việt là canonical tại `/danh-muc/{slug}/`; trang tiếng Anh luôn tồn tại tại `/en/categories/{slugEn hoặc slug}/`. Thiếu `slugEn` thì giữ slug VI trên URL EN và fallback nội dung từng trường theo `CATEGORY_RULE_002`; có `slugEn` thì URL EN dùng slug đó và URL EN mang slug VI redirect vĩnh viễn (**301 hoặc 308**) về URL chuẩn. Mỗi locale tự canonical và khai `hreflang` VI/EN/`x-default` — **trừ khi bản EN chưa đạt ngưỡng `SEO_RULE_002` hoặc bị tắt cờ `SEO_RULE_001`** (owner decision 2026-08-06). Đổi/xoá `slugEn` giữ locale EN khi redirect. Trang danh sách sản phẩm VI có canonical `/sp/`; EN có canonical `/en/products/`. Các alias VI cũ `/san-pham/`, `/danh-muc/`, `/danh-muc-san-pham/` và `/danh-muc-san-pham/{slug}/` tiếp tục 301 về URL VI hiện hành. `CONFIRMED_FROM_OWNER_DECISION`
- `CATEGORY_RULE_004`: **Xóa mềm danh mục** (đưa vào Thùng rác) đặt cột `deleted = true` và **không đổi** liên kết product-category; danh mục đó không còn được chọn mới. Khi **Xóa vĩnh viễn** một cây danh mục từ Thùng rác, hệ thống chỉ gỡ các liên kết nằm trong cây bị xóa. Sản phẩm còn ít nhất một liên kết danh mục khác giữ nguyên các liên kết còn lại (và danh mục đầu còn lại trở thành danh mục chính); chỉ sản phẩm mất toàn bộ liên kết mới được gán danh mục hệ thống `uncategorized`. Admin hiển thị riêng số sản phẩm bị gỡ liên kết và số sản phẩm phải chuyển `uncategorized` trước khi thực thi. `CONFIRMED_BACKEND_ENFORCED` (2026-07-22)
- `CATEGORY_RULE_005`: Danh mục hệ thống **"Chưa phân loại"** (`id`/`slug` = `uncategorized`, tên VI "Chưa phân loại", EN "Uncategorized") là fallback khi hard-delete làm sản phẩm mất toàn bộ liên kết (`CATEGORY_RULE_004`) **và** là giá trị mặc định khi tạo sản phẩm mới qua nhập file JSON không kèm danh mục (`PRODUCT_RULE_009`, từ 2026-07-22). Nó bị khóa, **không được gán thủ công qua ô chọn danh mục trên form sản phẩm** (dropdown không liệt kê nó vì `is_visible = false`), nhưng **được phép gán bằng 2 con đường tự động nói trên** — hard-delete cascade và fallback lúc import — cả hai đều đi qua tầng service, không qua form. Luôn `is_visible = false`; sản phẩm trong nhóm này vẫn truy cập/tìm kiếm bình thường nhưng không có breadcrumb danh mục công khai. `CONFIRMED_BACKEND_ENFORCED` (2026-07-22)
- `CATEGORY_RULE_006`: **Lọc/duyệt sản phẩm theo danh mục gồm cả danh mục con và mọi liên kết product-category.** Khi lọc theo một danh mục (admin: tham số `categoryId`; web khách: slug danh mục), kết quả gồm sản phẩm gắn với chính danh mục đó hoặc bất kỳ danh mục con nào bên dưới (đệ quy toàn bộ cây con). Một sản phẩm có nhiều liên kết vẫn chỉ xuất hiện một lần trong kết quả/phân trang/facet. `CONFIRMED_BACKEND_ENFORCED` (2026-07-22)
- `CATEGORY_RULE_007`: `showOnHomepage` là thuộc tính vị trí **độc lập** với `isVisible` (ẩn/hiện trên web) và `deleted` (Thùng rác). Danh mục mới mặc định `false`; form tạo mới không gửi field khi giá trị mặc định chưa bị bật, còn form sửa chỉ gửi boolean khi admin thực sự đổi vị trí Trang chủ. `showOnHomepage = true` không tự làm danh mục hiển thị, không khôi phục khỏi Thùng rác và không thay đổi quan hệ sản phẩm. `CONFIRMED_PRODUCT_DECISION` (2026-07-22)
- `CATEGORY_RULE_008` (owner decision 2026-08-18): **`introContent` HTML là bản gốc duy nhất.** Thẻ biểu mẫu chỉ là bảng điều khiển sửa nhanh: phần nào nhận diện được thì hiển thị, phần thiếu hoặc phần HTML tự do đều hợp lệ. Khi sửa bằng biểu mẫu, hệ thống chỉ vá đúng node được sửa trong HTML hiện có; bảng, ảnh, layout, block lạ và thứ tự cũ phải được giữ nguyên. Bộ đọc phải ghép các câu hỏi với câu trả lời theo thứ tự xuất hiện toàn cục, không phụ thuộc wrapper FAQ. Nút AI chỉ sao chép hướng dẫn HTML kèm hồ sơ danh mục lấy mới tại thời điểm bấm; không còn ô dán kết quả AI dạng nhãn và không được gọi AI từ hệ thống. `CONFIRMED_FROM_OWNER_DECISION`

Evidence:

- `CategoryEntity.java` (các cột `name_en`, `description_en`, `seo_title_en`, `seo_description_en`, `deleted`, `show_on_homepage`)
- `AdminCatalogMutationService.java` (`softDeleteCategory`, `restoreCategory`, `hardDeleteCategory` — gom cây con, chuyển sản phẩm sang `uncategorized` rồi xoá leaves-first; khóa danh mục `uncategorized`)
- `ProductJpaRepository.java` (`findIdsByCategory_IdIn`, `reassignCategory` — bulk chuyển danh mục sản phẩm)
- `V292__ensure_uncategorized_category.sql` (bảo đảm danh mục hệ thống "Chưa phân loại" tồn tại, `is_visible = false`)
- `V293__add_category_deleted.sql` (bổ sung cột `deleted` cho categories)
- `AdminCatalogController.java` (`DELETE /admin/categories/{id}` để xóa mềm, `POST /admin/categories/{id}/restore` để khôi phục, `DELETE /admin/categories/{id}/permanent` để xóa vĩnh viễn)
- `ProductEntity.java` (`categories` ordered many-to-many qua `product_category_map`; phần tử đầu là danh mục chính)
- `JpaCatalogReadRepository.java` (resolve locale + fallback cho category, lọc bỏ `deleted = true`; `resolveCategoryIdWithDescendants`/`resolveCategorySlugWithDescendants` — BFS trong bộ nhớ trên toàn bộ cây danh mục để mở rộng 1 category thành chính nó + mọi danh mục con, dùng cho cả `buildProductSpec` (admin) và `buildPublicListingSpec` (web khách))
- `CatalogController.java` (`lang` param trên category endpoints)

## Brand Catalog Rules

- `BRAND_RULE_001`: Mỗi thương hiệu bắt buộc có **tên dùng chung** (tên riêng/brand name, không dịch theo VI/EN). Admin nhập tên một lần; khi chuyển VI/EN trên form, ô tên vẫn hiển thị và sửa cùng một giá trị. Backend không yêu cầu `translations.en.name`; nếu client cũ còn gửi field này thì hệ thống không dùng nó để hiển thị tên thương hiệu. Các field thật sự có bản tiếng Anh riêng (mô tả, seoTitle, seoDescription) vẫn tùy chọn ở EN, web fallback về tiếng Việt (`BRAND_RULE_002`). `CONFIRMED_FROM_CODE` (2026-07-22)
- `BRAND_RULE_002`: Khi đọc thương hiệu bằng tiếng Anh (`lang=en`), **tên thương hiệu luôn lấy từ field tên dùng chung**. Các trường còn lại thiếu bản tiếng Anh sẽ **tự lùi về bản tiếng Việt theo từng trường** — hành vi thiết kế vĩnh viễn cho field tùy chọn. `CONFIRMED_FROM_CODE`
- `BRAND_RULE_003`: `slug` thương hiệu là **giá trị dùng chung** cho VI/EN, giống tên thương hiệu: admin nhập một lần; URL VI là `/brands/{slug}/`, URL EN là `/en/brands/{slug}/`. Backend không yêu cầu và không ghi `translations.en.slug`; nếu client cũ còn gửi field này thì hệ thống bỏ qua. Cột `brands.name_en`/`brands.slug_en` từng là dữ liệu legacy, chưa từng khác giá trị chính và đã bị **xoá hẳn khỏi bảng `brands`** (`V352`, 2026-07-24) — thương hiệu giờ chỉ còn đúng 1 cột tên (`name`) và 1 cột slug (`slug`). `CONFIRMED_FROM_OWNER_DECISION` (2026-08-01)
- `BRAND_RULE_004`: **Xóa mềm thương hiệu** (gộp với tính năng Ẩn) đặt cột `isVisible = false` (đưa vào Thùng rác). Khôi phục thương hiệu đặt lại `isVisible = true`. Chỉ được **Xóa vĩnh viễn thương hiệu** từ Thùng rác (`isVisible = false`), nếu đang hoạt động thì chặn lại (409). Khi xóa cứng thương hiệu còn sản phẩm, toàn bộ sản phẩm liên kết được **tự động chuyển sang thương hiệu hệ thống "Chưa phân loại"** (`uncategorized-brand`) thay vì để trống — mirror `CATEGORY_RULE_004`. API trả về số sản phẩm đã chuyển (`reassignedProductCount`) để admin biết. Thương hiệu "Chưa phân loại" **bị khoá** (không cho sửa/xoá/khôi phục), **ẩn hoàn toàn khỏi web** (`isVisible = false` vĩnh viễn — không có trang riêng, không menu/bộ lọc, PDP sản phẩm bên trong không hiện mục thương hiệu nào) và **ẩn khỏi danh sách quản lý thương hiệu trong admin** (chỉ hiện tên "Chưa phân loại" trên chính sản phẩm để admin biết cần gán lại). **Từ 2026-07-22, cũng là giá trị mặc định** khi tạo sản phẩm mới qua nhập file JSON không kèm `brandId` — xem `PRODUCT_RULE_009`. `CONFIRMED_FROM_CODE` (2026-07-03)
- `BRAND_RULE_005`: `showOnHomepage` là thuộc tính độc lập, mặc định `true`, chỉ điều khiển thương hiệu có xuất hiện ở dải logo thương hiệu trên trang chủ hay không. Khi `showOnHomepage = false`, thương hiệu vẫn xuất hiện ở trang danh sách thương hiệu, trang chi tiết, bộ lọc thương hiệu và brand nhúng trong chi tiết sản phẩm nếu `isVisible = true`. `isVisible` vẫn là cờ xóa mềm/Thùng rác duy nhất của Brand; không dùng `showOnHomepage` để thay thế hoặc thay đổi transition xóa mềm/khôi phục. `GET /api/v1/brands?showOnHomepage=true` là đường đọc riêng cho carousel trang chủ; các luồng đọc Brand khác không truyền bộ lọc này. `CONFIRMED_FROM_CODE` (2026-07-22)

Evidence:

- `BrandEntity.java` (các cột `description_en`, `seo_title_en`, `seo_description_en`, `isVisible` — không còn `name_en`/`slug_en`, xoá ở `V352`)
- `AdminCatalogMutationService.java` (`deleteBrand` đặt `isVisible = false`, `restoreBrand` đặt `isVisible = true`, `hardDeleteBrand` chuyển sản phẩm sang `uncategorized-brand` rồi xóa khỏi DB, khoá sửa/xoá/khôi phục thương hiệu hệ thống)
- `ProductJpaRepository.java` (`reassignBrand` bulk-update)
- `AdminCatalogReadService.java` (`listBrands` loại `uncategorized-brand` khỏi kết quả)
- `JpaCatalogReadRepository.java` (resolve locale + fallback cho brand; `toBrandSummary` trả `null` khi `publicView && !isVisible`)
- `CatalogController.java` (`lang` param trên brand endpoints)
- `AdminCatalogController.java` (`DELETE /admin/brands/{id}` để xóa mềm, `POST /admin/brands/{id}/restore` để khôi phục, `DELETE /admin/brands/{id}/permanent` để xóa vĩnh viễn, trả `reassignedProductCount`)
- Migration `V304__ensure_uncategorized_brand.sql` (seed thương hiệu hệ thống, mirror `V292` của category)
- `BrandEntity.showOnHomepage`, `BrandMutationService.applyBrandPatch`, `CatalogReadService.listBrands`, `CatalogController` (`showOnHomepage`), `bigbike-web/app/page.tsx` (carousel trang chủ)
- Migration `V349__add_brand_show_on_homepage.sql`

## Article (Blog) Rules

- `ARTICLE_RULE_001`: Mỗi bài viết bắt buộc có **bản nội dung tiếng Việt** (canonical). **Bản tiếng Anh nhập tay** — admin tự gõ qua nút chuyển VI/EN trên form (`TRANSLATION_RULE_001`). **Tiêu đề** bài viết bắt buộc cả VI lẫn EN — thiếu `translations.en.title` chặn lưu (`TRANSLATION_RULE_002`). Các field còn lại — gồm cả **`body`/`bodyBlocks`** (nội dung bài viết), dù bắt buộc ở tiếng Việt, EN vẫn **tùy chọn theo ngoại lệ business riêng** (coi như khối nội dung dài, không phải field định danh) — cùng excerpt, seoTitle, seoDescription, slug đều tùy chọn ở EN, web fallback về tiếng Việt (`ARTICLE_RULE_002`). `CONFIRMED_FROM_CODE`
- `ARTICLE_RULE_002`: Khi đọc bài viết bằng tiếng Anh (`lang=en`), mỗi trường thiếu bản tiếng Anh sẽ **tự lùi về bản tiếng Việt theo từng trường** (title, excerpt, body, seoTitle, seoDescription) — hành vi thiết kế vĩnh viễn cho field/khối tùy chọn (kể cả `body`/`bodyBlocks`), đồng thời áp dụng cho bản ghi cũ đang thiếu EN ở `title` (field bắt buộc) cho tới khi admin tự bổ sung. `CONFIRMED_FROM_CODE`
- `ARTICLE_RULE_003` (URL locale chuẩn hoá 2026-08-03): `slug` tiếng Việt là canonical tại `/tin-tuc/{slug}/`; danh sách và chi tiết tiếng Anh nằm tại `/en/tin-tuc/` và `/en/tin-tuc/{slugEn hoặc slug}/`. Thiếu `slugEn` thì dùng slug VI dưới `/en/tin-tuc` và fallback nội dung từng trường theo `ARTICLE_RULE_002`; có `slugEn` thì URL EN mang slug VI redirect vĩnh viễn (**301 hoặc 308**) sang slug EN. Canonical, `hreflang` VI/EN/`x-default` và redirect khi đổi/xoá slug luôn giữ đúng locale — `hreflang` và sitemap **chỉ khai URL EN khi bản EN đạt ngưỡng `SEO_RULE_002` và cờ `SEO_RULE_001` đang bật** (owner decision 2026-08-06). Các route cũ `/en/news...` trả 301 trực tiếp sang `/en/tin-tuc...`. Các ràng buộc duy nhất và cơ chế OR-resolve của backend không đổi. **Chỉ áp dụng cho bài viết**; trang thông tin/chính sách tĩnh dùng bảng route locale của web. `CONFIRMED_FROM_OWNER_DECISION`
- `ARTICLE_RULE_004`: Admin có thể đánh dấu một bài viết là **nổi bật** (`featured`). Web hiển thị các bài `featured` ở khu **"Tin nổi bật"**; nếu **không có bài nào** được đánh dấu nổi bật, web **fallback sang các bài viết mới nhất**. `featured` chỉ áp dụng cho bài viết. `CONFIRMED_FROM_CODE`
- `ARTICLE_RULE_005` (owner decision 2026-07-29): Màn danh sách Tin tức mặc định và lựa chọn **"Tất cả" không bao gồm bài ở `TRASH`**. Thùng rác là một view riêng, chỉ được tải khi admin gửi rõ `publishStatus=TRASH`. Quy tắc này áp dụng ở backend, không chỉ ẩn ở giao diện, để phân trang và tổng số bản ghi luôn chính xác. `CONFIRMED_FROM_OWNER_DECISION`
- `ARTICLE_RULE_006` (owner decision 2026-08-03): Chức năng danh mục nội dung bị xóa hoàn toàn; bài viết không còn `categoryId`, `category` hay `categories`, API list không còn lọc theo category và storefront không còn sidebar/drawer/nhãn danh mục. Việc này không ảnh hưởng danh mục sản phẩm hoặc bảng `article_tags`. Form bài viết không cho nhập canonical thủ công; canonical tiếng Việt tự sinh từ `slug` theo dạng `https://bigbike.vn/tin-tuc/{slug}/` (localhost/127.0.0.1 được phép ở môi trường local). `slugEn` vẫn tùy chọn và URL/canonical trang tiếng Anh theo `ARTICLE_RULE_003`. `CONFIRMED_FROM_OWNER_DECISION`
- `ARTICLE_RULE_007` (owner decision 2026-08-16): Bài viết có trường tác giả tùy chọn, lưu riêng cho tiếng Việt và tiếng Anh (`authorName` / `authorNameEn`). Admin được nhập từng ngôn ngữ; bản tiếng Anh trống fallback về tiếng Việt. Khi cả hai bản trống, storefront không hiện dòng tác giả và JSON-LD không sinh trường `author` rỗng. Khi có giá trị, trang bài viết hiển thị dòng tác giả và JSON-LD khai báo `Person` với đúng tên đang hiển thị. `CONFIRMED_FROM_OWNER_DECISION`

Evidence:

- `ArticleEntity.java` (các cột `title_en`, `excerpt_en`, `body_en`, `seo_title_en`, `seo_description_en`; `featured`, `seo_no_index`)
- `JpaContentReadRepository.java` (resolve locale + fallback cho article)
- `ContentController.java` (`lang` param trên article endpoints; `listArticles` query param `featured`)
- `AdminContentMutationService.java` (`applyArticlePatch` ghi cột `_en` + `slug_en` + `featured`/`seo_no_index`; validate uniqueness `slugEn`)
- `ArticleJpaRepository.java` (`findBySlug`, `findBySlugEn`)
- `UpsertArticleRequest.java` (`featured`), `SeoMetaRequest.java` (`noIndex`)
- `V138__add_article_page_bilingual_content.sql`, `V216__add_article_slug_en.sql`, `V222__add_article_featured_and_seo_no_index.sql`
- `DATA_CONTRACT.md` — "Article bilingual content", "Article featured + seo_no_index (V222)"

## Homepage Slider Rules

- `SLIDER_RULE_001` (owner decision 2026-07-30): Banner/slider tạo mới hoặc sửa toàn phần phải có `productId` hợp lệ và chỉ điều hướng tới trang chi tiết sản phẩm. Không còn luồng nhập hoặc validate `externalLink`. Giá trị `externalLink` lịch sử vẫn được giữ trong dữ liệu/response để tương thích, nhưng không hiển thị trong Admin và không được dùng làm đích đến trên storefront. Patch một phần chỉ đổi trạng thái/thứ tự không yêu cầu gửi lại sản phẩm. `OWNER_CONFIRMED_FROM_USER_REQUEST`

Evidence:

- `SliderListScreen.jsx`, `sliderPayload.js`
- `AdminSliderService.java`, `UpsertSliderRequest.java`, `PatchSliderRequest.java`
- `SliderReadService.java`, `HeroSlider.tsx`

## Site Settings Rules

- `SETTINGS_RULE_001` (cập nhật 2026-07-03 — bỏ tự động dịch): **Cài đặt (site settings) nhập song ngữ bằng nút VI/EN, không còn 2 ô xếp chồng, không còn tự động dịch.** Mỗi setting dịch được (`isTranslatableSetting()` — nhóm `general`/`public_hero`/`seo`, kiểu chữ) hiện **1 ô tại 1 thời điểm**, đổi qua nút VI/EN ở header admin (`useContentLang()`); setting **không** dịch được (ảnh/số/điện thoại/boolean/ngân hàng…) luôn hiện đúng 1 giá trị tiếng Việt, không đổi theo nút ngôn ngữ. Không còn tự động dịch VI→EN khi lưu, không còn endpoint `/admin/translate`, không còn cơ chế khoá `en_locked` (đã xoá khỏi DB cùng Gemini — `TRANSLATION_RULE_001`) — admin **tự gõ** tiếng Anh cho từng setting dịch-được. Setting nào **vừa dịch-được vừa bắt buộc ở VI** (hiện chỉ `site_name`, cờ `.required()` trong `SettingDefinitionRegistry`) thì bản tiếng Anh (`valueEn`) cũng bắt buộc — để trống chặn lưu (`TRANSLATION_RULE_002`). Setting dịch-được khác (không bắt buộc VI) vẫn tùy chọn ở EN, web fallback về tiếng Việt khi trống. `CONFIRMED_FROM_CODE`

Evidence:

- `SiteSettingEntity.java` (không còn cột `en_locked` — dropped V312, trước đó thêm ở V309)
- `AdminSiteSettingResponse.java` / `UpdateSiteSettingRequest.java` / `BatchUpdateSettingsRequest.BatchSettingUpdate` (field `valueEn`; không còn `enLocked`)
- `SettingDefinitionRegistry.java` (`.required()` — hiện chỉ `site_name`), `SettingValueValidator.java` (bắt buộc `valueEn` khi setting translatable + required)
- `AdminSettingsService.java` (`updateSetting`/`batchUpdateSettings` — không còn nhánh xử lý `enLocked`)
- `bigbike-admin/src/screens/settings/SettingField.jsx` (ô đơn theo `useContentLang()`, không còn hint khoá)
- `bigbike-admin/src/screens/SettingsScreen.jsx` (`handleSave` lưu trực tiếp `batchUpdateSettings`, không còn gọi dịch/khoá)
- `screens/settings/constants.js` (`TRANSLATABLE_GROUPS`, `isTranslatableSetting()` — không đổi phạm vi field nào dịch được)

- `SETTINGS_RULE_002` (thêm 2026-07-05; quyền đọc mở rộng 2026-07-29): **Banner "Phân công" trên màn tạo/sửa sản phẩm VÀ màn tạo/sửa bài viết dùng chung 1 nguồn dữ liệu** (`GET /api/v1/admin/product-assignment`) — không phải 2 cấu hình độc lập, sửa ở Settings cập nhật cả 2 nơi. Banner báo cho nhân sự nội bộ biết ai phụ trách phần nào của sản phẩm/bài viết (mặc định Content/SEO/Quản lý, nhưng tùy chỉnh được). Số vai trò **tối thiểu 1, tối đa 6** (`product_assign_roles`, kiểu JSON, validate ở `SettingValueValidator`) — giới hạn trên để tránh vỡ layout banner (grid tối đa 3 cột, tự xuống dòng khi nhiều hơn). Người có ít nhất một trong hai quyền `products.read` hoặc `content.read` được xem banner; chỉ SUPER_ADMIN thêm/sửa/xóa vai trò (`superAdminOnly`). 3 vai trò gốc (Content/SEO/Quản lý) giữ id ổn định (`content`/`seo`/`manager`) qua migration `V318` — nếu Super Admin xóa hẳn 1 trong 3 vai trò gốc, các nhãn nhỏ gắn cố định trong form sản phẩm (không đổi theo số vai trò, ví dụ nhãn cạnh khối SEO) vẫn hiện tên mặc định ban đầu thay vì biến mất. `CONFIRMED_FROM_OWNER_DECISION`
- `SETTINGS_RULE_003` (owner decision 2026-08-16; đảo quyết định V337): Ba setting SEO trang chủ `seo_home_title`, `seo_home_description` và `seo_home_h1` được mở lại trong tab **SEO website**, đều nhập song ngữ và công khai cho website. Website dùng lần lượt cho tiêu đề trang, mô tả Google và đúng một tiêu đề chính trang chủ. Tiêu đề và tiêu đề chính để trống thì fallback về `site_name`; mô tả để trống thì fallback về mô tả trang chủ hiện hành theo ngôn ngữ. Không tự thêm tên shop nếu giá trị đã chứa BigBike; không có setting ảnh chia sẻ mặc định trong đợt này. `CONFIRMED_FROM_OWNER_DECISION`

Evidence:
- `SettingDefinitionRegistry.java` (`product_assign_roles`, kiểu JSON, `.superAdminOnly()`), `SettingValueValidator.java` (`validateProductAssignRoles` — giới hạn 1-6 + field bắt buộc)
- `V318__consolidate_product_assignment_roles.sql` (gộp 6 key cũ thành `product_assign_roles`, giữ id `content`/`seo`/`manager`)
- `AdminProductAssignmentController.java` (`GET /api/v1/admin/product-assignment`, gate một-trong-hai `products.read` / `content.read`)
- `bigbike-admin/src/screens/product-detail/Layout.jsx` (`useRoleLabel` — fallback tên mặc định khi id không còn tồn tại), `bigbike-admin/src/screens/content-detail/ContentAssignmentBanner.jsx` (đọc chung endpoint/query key)
- `DATA_CONTRACT.md` — "`product_assign` keys + super-admin-only write"; `PERMISSION_MATRIX.md` — "Super-admin-only settings (`product_assign`)"

## Static Page Rules — REMOVED (2026-06-24)

> **REMOVED / Deprecated 2026-06-24.** Module "Trang tĩnh CMS" (pages) đã gỡ khỏi toàn stack. 8 route thông tin/chính sách hiện hành (Giới thiệu, Liên hệ, Hướng dẫn + 2 trang con, 3 trang chính sách) nay **đóng cứng trong `bigbike-web`** (nguồn `static-pages.json` + component web, song ngữ VI/EN cố định trong code). Không còn bảng `pages` (drop ở `V271`), không còn endpoint hay màn admin quản lý trang. Các rule dưới đây **không còn áp dụng** — giữ lại làm lịch sử.
>
> - ~~`PAGE_RULE_001`~~: (cũ) Mỗi trang tĩnh bắt buộc có bản nội dung tiếng Việt; bản tiếng Anh tùy chọn. → Nay nội dung song ngữ cố định trong code web.
> - ~~`PAGE_RULE_002`~~: (cũ) Đọc trang `lang=en` lùi về VI theo từng trường. → Không còn — web tự chọn bản theo locale.
> - ~~`PAGE_RULE_003`~~: (cũ) `slug` của trang dùng chung 1 bản. → Slug nay là route cố định trong web.
>
> Lý do gỡ: các trang này có nội dung ổn định, không cần admin sửa thường xuyên → chuyển thành tĩnh để đơn giản hoá vận hành. Bài viết (ARTICLE_RULE_*) **vẫn còn** quản lý động qua module Nội dung (Tin tức).

## Contact Page Rules

Trang `/lien-he` là **trang tĩnh hoàn toàn**: bố cục, nhãn, tiêu đề và SEO cố định trong code web (i18n `Contact`/`StaticPage`). Không có hero. Admin **không quản lý** trang này — không trang CMS, không trình dựng khối, không tab Cài đặt. (Đảo bỏ trình dựng `contact_page_layout` trước đây — bảng đã drop ở `V270`.)

- `CONTACT_PAGE_RULE_001`: Bố cục cố định trong code (`bigbike-web/components/contact/ContactPageContent.tsx`): cột thông tin (hotline / địa chỉ / giờ làm việc) + cột kênh trực tuyến (Zalo / Facebook / hotline) + bản đồ Google nhúng dựng từ địa chỉ cửa hàng. `CONFIRMED_FROM_CODE`
- `CONTACT_PAGE_RULE_002`: Số điện thoại / địa chỉ / giờ / URL mạng xã hội **không** hardcode trong code — là dữ liệu CHUNG ở `site_settings` nhóm `contact` (single source dùng chung header/footer/trang Giới thiệu), web đọc qua `GET /api/v1/settings/public`. Vì admin không quản lý trang Liên hệ, nhóm `contact` hiện không có UI sửa (tab Cài đặt ẩn qua `HIDDEN_GROUPS`); muốn cho sửa lại thì bỏ `CONTACT` khỏi `HIDDEN_GROUPS`. `CONFIRMED_FROM_CODE`
- `CONTACT_PAGE_RULE_003`: Nhãn/tiêu đề song ngữ VI/EN qua i18n; không còn endpoint admin/public riêng cho trang (`GET/PUT /api/v1/admin/contact-page` và `GET /api/v1/contact-page` đã gỡ). `CONFIRMED_FROM_CODE`

Evidence:

- `bigbike-web/app/lien-he/page.tsx`, `bigbike-web/components/contact/ContactPageContent.tsx` (trang tĩnh)
- `bigbike-web/messages/{vi,en}.json` namespace `Contact` (nhãn/tiêu đề song ngữ)
- `V270__drop_contact_page_layout.sql` (drop bảng; gỡ controller/service/entity/DTO/converter contact-page + whitelist `SecurityConfig`)
- `bigbike-admin/src/screens/settings/constants.js` (`CONTACT` trong `HIDDEN_GROUPS`)

## Guide Page Builder Rules — REMOVED (2026-06-24)

> **REMOVED / Deprecated 2026-06-24.** Trang Hướng dẫn `/huong-dan` nay là **nội dung tĩnh trong `bigbike-web`** (nguồn `static-pages.json`). Trình dựng trang Hướng dẫn (GuidePageBuilder) trong admin đã gỡ; bảng `guide_page_layout` drop ở `V271`; endpoint admin/public guide-page không còn. Các rule dưới đây **không còn áp dụng** — giữ lại làm lịch sử.
>
> **Bộ trang con hiện hành (2026-07-15, AUD-012):** đúng **2 trang con tĩnh** — `/huong-dan/size-mu/` (size mũ bảo hiểm) và `/huong-dan/size-trang-phuc/` (size trang phục bảo hộ, đã bao gồm găng tay/giày/áo/quần). Các URL cũ 301 trong `next.config.ts`: `/huong-dan/mua-hang/` (+ biến thể `/huong-dan-mua-hang(.html)`) → `/huong-dan/`; `/huong-dan/size-gang-tay/` → `/huong-dan/size-trang-phuc/`. Không có nội dung "hướng dẫn mua hàng" riêng — muốn có lại thì phải soạn nội dung mới (quyết định owner).
>
> - ~~`GUIDE_PAGE_RULE_000`~~..~~`GUIDE_PAGE_RULE_003`~~: (cũ) Lưới ô + hero do admin dựng qua trình dựng nhúng trong module Nội dung, lưu ở `guide_page_layout`, web dựng lưới/sidebar từ entries. → Toàn bộ chuyển thành tĩnh trong web.

## Policy Page Rules

Trang chính sách `/chinh-sach/{slug}` là nội dung cố định trong `bigbike-web`; admin không quản lý thân bài, danh sách hay thứ tự sidebar (owner decision 2026-07-15, AUD-041).

- `POLICY_PAGE_RULE_001`: Chỉ có đúng 3 slug: `chinh-sach-bao-mat-thong-tin`, `chinh-sach-bao-hanh`, `chinh-sach-doi-tra-hang`. Slug khác trả 404. `CONFIRMED_FROM_CODE`
- `POLICY_PAGE_RULE_002`: Sidebar luôn hiển thị cố định theo thứ tự **Bảo mật thông tin → Bảo hành → Đổi trả hàng**; web tự dựng từ danh sách trong route, không gọi `GET /api/v1/menus/policy`. `CONFIRMED_FROM_CODE`
- `POLICY_PAGE_RULE_003`: Admin không quản lý trang chính sách và không có menu slot `policy`; hệ thống menu chỉ còn slot `primary`. Muốn thay đổi danh sách/nội dung chính sách phải cập nhật source web và canonical docs trong cùng thay đổi. `CONFIRMED_FROM_CODE`

Evidence:

- `bigbike-web/app/chinh-sach/[slug]/page.tsx` (`POLICY_SLUGS`, `buildStaticSidebarItems`)
- `bigbike-web/lib/content/static-pages.json`
- `MenuLocations.java` (`PRIMARY` là system slot duy nhất)

## WebSocket Rules

- WebSocket STOMP connect must include native header `Authorization: Bearer <token>`. `CONFIRMED_FROM_CODE`
- The token must belong to an `ACTIVE` admin account. CONNECT is not restricted to hardcoded role names. `CONFIRMED_FROM_CODE`
- `WEBSOCKET_RULE_001`: Subscribing to the admin order topic requires the `orders.read` permission. The rule is permission-based, so built-in roles such as `SHOP_MANAGER` and custom roles can receive order events when granted `orders.read`; `ADMIN`/`SUPER_ADMIN` is not a hardcoded allowlist. Account status and current DB-backed permission are rechecked on every SUBSCRIBE. `CONFIRMED_FROM_CODE`
- Current confirmed topic is `/topic/admin/orders`. `CONFIRMED_FROM_CODE`
- Confirmed event type in the checkout flow is `NEW_ORDER`; `ORDER_STATUS_CHANGED` is declared in the event record comment but needs a live sender check before relying on it. `NEEDS_VERIFICATION`

Evidence:

- `WebSocketConfig.java`
- `AdminOrderWsService.java`
- `OrderWsEvent.java`
- `adminWebSocket.js`

## Redirect And Integration Rules

- `REDIRECT_RULE_001`: **Chống tự trỏ (self-redirect).** `sourcePattern` và pathname của `targetUrl` không được trùng nhau sau khi chuẩn hoá (case-sensitive, bỏ mọi dấu `/` cuối trừ root). Query/fragment không làm thành một trang khác đối với bộ dò redirect; URL tuyệt đối cùng domain cũng được quy về pathname trước khi so sánh. Tạo/sửa vi phạm bị chặn `400 VALIDATION_ERROR`. `CONFIRMED_FROM_RUNTIME_CONTRACT_2026-08-07`
- `REDIRECT_RULE_002`: **Chống vòng lặp nhiều bước (A→B→A...).** Khi tạo/sửa một redirect, hệ thống dò theo chuỗi pathname đích tối đa **20 bước**; phát hiện chu trình quay lại nguồn ban đầu → chặn lưu. URL tuyệt đối cùng domain vẫn tham gia chuỗi; chỉ dừng sớm khi đích thực sự ra domain ngoài. Web có thêm chốt an toàn trực tiếp để không phục vụ self-loop từ dữ liệu legacy chưa qua API. `CONFIRMED_FROM_RUNTIME_CONTRACT_2026-08-07`
- `REDIRECT_RULE_003`: **`sourcePattern` phải duy nhất** trong toàn bộ bảng — phân biệt hoa/thường, đã chuẩn hoá bỏ dấu `/` cuối (nên `/foo` và `/foo/` được coi là cùng 1 nguồn, không tạo được 2 lần; `/Foo` và `/foo` vẫn là 2 nguồn khác nhau, được phép). Chặn ở tầng ứng dụng (`409 CONFLICT`) và tầng DB (`uq_redirects_source_pattern`, lưới an toàn khi có 2 request ghi đồng thời). `CONFIRMED_FROM_CODE`
- `REDIRECT_RULE_004`: **Chống trỏ ra ngoài (open-redirect).** `targetUrl` chỉ được là path nội bộ (`/...`, không bắt đầu bằng `//`) hoặc URL tuyệt đối `http(s)://` có domain **trùng đúng** `bigbike.site.base-url` đang cấu hình. URL protocol-relative (`//...`) hoặc scheme khác `http`/`https` bị từ chối. `CONFIRMED_FROM_CODE`
- `REDIRECT_RULE_005`: **Redirect quản trị chỉ dùng HTTP 301 hoặc 410.** Mặc định là `301 Permanent`; `410 Gone` là trạng thái kết thúc dành cho URL sản phẩm đã bị gỡ hoặc không còn đích hợp lệ. `410` không được chuyển tiếp sang `targetUrl`, không tăng hit count như một redirect thành công, và trang đích phải phát `X-Robots-Tag: noindex, nofollow`. `redirectType` không còn là thuộc tính được lưu; status code được quản trị chọn trong tập `{301, 410}`. Các redirect tĩnh riêng trong `bigbike-web/next.config.ts` nằm ngoài bảng admin. `OWNER_DECISION_2026-08-13`
- `REDIRECT_RULE_006`: **Đếm lượt truy cập (hit tracking).** Mỗi lần web áp dụng thành công một redirect cho khách, hệ thống tăng `hitCount`/`lastHitAt` của rule đó (ghi nhận kiểu "cứ gửi, không chờ phản hồi" — không làm chậm redirect nếu ghi log thất bại). `CONFIRMED_FROM_CODE`
- `REDIRECT_RULE_007`: **Nguồn là đường dẫn nội bộ khớp chính xác.** `sourcePattern` không nhận URL tuyệt đối, protocol-relative, query hay fragment; hệ thống tự thêm `/` đầu nếu admin bỏ quên và bỏ mọi `/` cuối trừ root. Quy tắc này khớp đúng dữ liệu mà `bigbike-web/proxy.ts` gửi sang API lookup (pathname, không gồm query/fragment). `CONFIRMED_FROM_RUNTIME_CONTRACT_2026-08-07`
- `REDIRECT_RULE_008`: **Bảng `redirects` quản lý trong admin là nguồn DUY NHẤT của luật chuyển hướng.** Không được thêm luật vào `redirects()` trong `bigbike-web/next.config.ts` cho các URL mà admin quản lý được: Next.js chạy luật viết cứng **trước** proxy, nên luật viết cứng luôn đè bảng admin — chủ shop sửa trong admin sẽ không có tác dụng và `hitCount` đứng yên ở 0 (đúng triệu chứng của 4 URL `/mu-bao-hiem`, `/ao-quan-bao-ho`, `/phu-kien-khac`, `/san-pham-khuyen-mai`, đã gỡ 2026-08-07). `next.config.ts` chỉ còn giữ luật hạ tầng không thuộc phạm vi admin (chuẩn hoá locale, alias route tĩnh). `CONFIRMED_FROM_CODE`
- `REDIRECT_RULE_009`: **Nguồn và đích không cần dấu `/` cuối — luôn ra đúng 1 bước.** Proxy tra bảng `redirects` **trước** khi chuẩn hoá dấu `/` cuối, và tự thử cả biến thể bỏ dấu `/`; đích cũng được chuẩn hoá qua `translatePath`. Nên `sourcePattern` `/mu-bao-hiem` và `targetUrl` `/danh-muc/mu-bao-hiem` vẫn cho **một** `301` thẳng tới `/danh-muc/mu-bao-hiem/`, không chèn thêm `308`. Trước 2026-08-07 proxy chuẩn hoá `/` trước khi tra bảng nên mọi nguồn không kèm `/` cuối tốn 2 bước. `CONFIRMED_FROM_CODE`
- `REDIRECT_RULE_010`: **Tự động thu gọn về đúng 1 bước khi lưu — không còn khái niệm "số bước"/cảnh báo đi vòng.** Khi tạo redirect mới, hoặc sửa MỘT redirect có gửi kèm `targetUrl` mới, nếu giá trị đó trùng đúng `sourcePattern` của một redirect KHÁC đang bật (chuỗi A→B→C), hệ thống **tự động lưu `targetUrl` bằng đích cuối cùng** (C) thay vì đích trung gian (B) — admin không cần biết hay xử lý gì thêm, redirect luôn ra đúng 1 bước ngay từ lúc lưu. Vòng lặp thật (A→B→A, kể cả đi qua 1 luật đã tắt) vẫn bị **chặn cứng** như cũ và luôn được kiểm tra lại mỗi lần lưu, kể cả khi chỉ đổi nguồn hoặc bật/tắt (`REDIRECT_RULE_002`, không đổi). **Chỉ tính luật đang bật** khi dò chuỗi để thu gọn — luật đã tắt không chuyển hướng ai nên không tham gia (khớp đúng hành vi thật của proxy). **Phạm vi:** việc thu gọn chỉ chạy khi request có gửi `targetUrl` (sửa riêng trạng thái bật/tắt hay ghi chú không kích hoạt); và chỉ áp dụng cho đúng bản ghi đang được lưu tại thời điểm đó — không quét/sửa lại các bản ghi khác đã có sẵn nếu sau đó một luật mới ra đời khiến chúng vô tình thành chuỗi. Trường hợp này hiếm; admin có thể tự sửa lại (nhập lại) đúng ô "Địa chỉ mới" của bản ghi đó để hệ thống thu gọn lại. Không còn field `chainHops`/`finalTarget` trên response, không còn endpoint `GET /admin/redirects/resolve`, không còn filter `chained` trên danh sách — tất cả bị bỏ vì không còn cần thiết. `OWNER_DECISION_2026-08-08`, thay thế hành vi "chỉ cảnh báo" trước đó.

- `REDIRECT_RULE_011` (owner decision 2026-08-14): **Đích 301 thuộc catalog phải còn hợp lệ.** Nếu đích là `/product/{slug}` hoặc `/en/product/{slugEn}` thì sản phẩm phải tồn tại theo slug VI hoặc EN, có `publishStatus = PUBLISHED` và `discontinued = false`. Cờ `available` chỉ mô tả tồn kho, không làm mất hiệu lực của sản phẩm. Nếu đích là `/danh-muc/{slug}` hoặc `/en/categories/{slugEn}` thì danh mục phải tồn tại, `visible = true` và `deleted = false`. Luật `410` là terminal nên bỏ qua kiểm tra đích. Nguồn legacy sản phẩm `/sp/...` không được chuyển vào trang danh sách/danh mục; trường hợp không còn PDP hợp lệ dùng trang lịch sử hoặc `410`.
- `REDIRECT_RULE_012` (owner decision 2026-08-14): **Proxy đọc snapshot redirect đang bật.** `bigbike-web` tải `/api/internal/redirects/active` theo cơ chế single-flight, TTL 30 giây; cache L1 chỉ lưu rule dương. Timeout, lỗi mạng và HTTP 5xx/không thành công không được ghi thành “không có rule”; proxy thử lookup từng nguồn như fallback. Endpoint clear phải xóa cả snapshot và L1.
- `REDIRECT_RULE_013` (owner decision 2026-08-15, display superseded by owner decision 2026-08-18): **Trang lịch sử sản phẩm ngừng bán là URL legacy ổn định và quản lý được từ admin.** Sản phẩm hiện hữu có `products.discontinued = true` tiếp tục dùng chính bản ghi sản phẩm đó. Hàng lịch sử không còn bản ghi sản phẩm phải nằm trong bảng `legacy_discontinued_products`, do nhân viên bật/tắt và sửa tên VI/EN, thương hiệu, nhóm hàng, ảnh đã kiểm chứng trong admin — **không** giữ danh sách cứng trong mã web và không tạo sản phẩm giả có giá/tồn kho. URL `/sp/{slug}.html` trả `200`, có tên hàng, ảnh khi đã có ảnh nguồn, nhãn ngừng bán, không có nút mua và hiển thị 4–8 sản phẩm đang bán phù hợp nếu có đủ. Gợi ý ưu tiên cùng thương hiệu, rồi cùng kiểu hàng suy ra từ tên, sau đó bổ sung hàng khác trong nhóm; nếu nhóm rỗng thì dùng nhóm cha hoặc thứ tự bán chạy hiện hành. Trang dùng chính URL `/sp/` làm canonical, không tạo PDP `/product/` mới. Ảnh nguồn chưa kiểm chứng phải hiện trạng thái thiết kế sẵn thay vì thay bằng ảnh của sản phẩm khác. Owner ngày 18/08/2026 chốt thêm breadcrumb đủ cây nhóm, link thương hiệu/nhóm hàng, dải cam kết, nút Zalo, ô tìm kiếm, bỏ khung ảnh khi thiếu ảnh và đưa cảnh báo an toàn xuống cuối trang. Đây là quyết định hiển thị mới, không phải lỗi cần khôi phục.
- `PRODUCT_RULE_021` (owner decision 2026-08-18): **Sản phẩm PUBLISHED bị gắn cờ `discontinued` vẫn giữ nguyên toàn bộ nội dung PDP tại URL lịch sử hiện có.** Web chỉ thay vùng giá, chọn biến thể, nút mua/thêm giỏ và thanh mua dính đáy bằng khối trạng thái ngừng bán; mọi đường checkout/add-to-cart đều không được render. Không thêm trường dữ liệu, không đổi URL, không chuyển hướng tự động. `CONFIRMED_FROM_OWNER_DECISION_2026-08-18`
- `REDIRECT_RULE_014` (owner decision 2026-08-15): **Archive size trỏ thẳng vào catalog filter.** Các URL `/size/...` được chuyển đúng một bước tới `/sp/?kich-co=...`; `XXXL` chuẩn hóa thành `3XL`. URL phân trang cũ giữ đúng trang bằng tham số `page`; query `?paged=` cũ được đổi thành `page` trong cùng một lần chuyển. Khi không có kết quả, catalog phải giải thích rõ size đang lọc và cho phép xóa bộ lọc để quay về danh sách.
- `REDIRECT_RULE_015` (owner decision 2026-08-15): **Không khôi phục bộ lọc màu cũ.** Các archive legacy `/color/...` không được tạo filter màu mới và không được báo gỡ hẳn chỉ vì taxonomy màu cũ đã mất. Chúng chuyển đúng một bước về danh sách sản phẩm `/sp/`, là nhóm hàng hiện còn phù hợp với archive màu toàn catalog; không mang query màu cũ sang đích.

> **`notes`/`legacyId` đã bị bỏ khỏi bảng `redirects` (owner decision 2026-08-08, migration `V379`).** `legacyId` (mã tham chiếu dòng redirect WordPress gốc, chỉ dùng đối chiếu di trú) và `notes` (ghi chú tự do của admin) không còn là cột trong schema, không còn field trên `CreateRedirectRequest`/`UpdateRedirectRequest`/`AdminRedirectResponse`, không còn ô nhập trên form admin. Toàn bộ nội dung ghi chú/mã tham chiếu đã lưu trước đó bị xoá vĩnh viễn theo migration này — hai field này chưa từng được `bigbike-web/proxy.ts`/`InternalRedirectController` đọc nên việc xoá không ảnh hưởng hành vi chuyển hướng thực tế trên web.
- Internal redirect endpoints require the `X-Internal-Token` shared-secret header (`BIGBIKE_INTERNAL_TOKEN` backend / `INTERNAL_API_TOKEN` web) in addition to being blocked at the nginx layer (`deploy/nginx/api.bigbike.vn.conf`) in production — see PERMISSION_MATRIX.md "Internal Redirect Caveat" for the full mechanism. `CONFIRMED_FROM_CONFIG`
- `PAY_RULE_001`: **Storefront customers choose exactly one of two manual payment methods: `COD` or `BANK_TRANSFER`** (owner decision 2026-07-21, partially reversing the COD-only decision of 2026-07-15). The checkout page sends the customer’s selection; the API normalises an omitted value to `COD` and rejects any explicit code outside that enum. `BANK_TRANSFER` is a new code and must not reuse `BACS`, whose legacy auto-mark-paid behavior is reserved for imported WooCommerce orders. `GET /api/v1/checkout/options` remains removed: the two fixed methods are owned by the checkout contract and web, so there is no dynamic options list to fetch. Legacy `BACS`/`null` values remain readable only. There is no automatic payment gateway — all payment is reconciled manually by the admin. `CONFIRMED_FROM_CODE`
- `PAY_RULE_002`: Manual-confirm reconciliation is operational, not a second order state. New online orders start `PENDING`; staff confirm the order, then move it to `PROCESSING`. For `BANK_TRANSFER`, staff confirm receipt externally, then confirm delivery by moving the order directly to `COMPLETED`. No payment status is stored on the order and no payment-status endpoint exists. `CONFIRMED_FROM_CODE`
- `PAY_RULE_003`: `payments.status` is a read-only transaction snapshot shown in order detail, not an editable order state. Its only canonical values are `PENDING`, `SUCCEEDED`, `FAILED`, `CANCELLED`. Legacy `UNPAID`/`PARTIALLY_PAID` map to `PENDING`, `PAID` maps to `SUCCEEDED`, and removed `REFUNDED`/`PARTIALLY_REFUNDED` map to `CANCELLED`; existing canonical values remain unchanged. Unknown values must stop migration for manual classification instead of being guessed. New checkout/import code must write only this vocabulary and the database must reject any other value. This does not add a payment-state endpoint or change the order transition rules. Owner decision 2026-07-28. `CONFIRMED_FROM_CODE`
- `SHIP_RULE_001`: Shipping-method choice and shipping fee removed (owner decision 2026-06-23). Online checkout no longer asks the customer to pick a shipping method, and online orders carry **no shipping fee** (`shippingAmount = 0`, `totalAmount = subtotal − discount`). The whole admin shipping-management module (shipping zones + shipping methods, `/api/v1/admin/shipping/*`, the admin "Vận chuyển" screen, the `shipping_zones`/`shipping_methods` tables, and the `shipping.read`/`shipping.write` permissions) was dropped (migration `V264`). **Delivery is free to the customer (owner decision 2026-06-25): no shipping fee is charged either in-system or offline.** The web checkout summary and the cart page therefore display "Miễn phí vận chuyển" (free shipping) — superseding the earlier cart copy that mentioned a 35.000đ fee / free-over-2M threshold. The shop still arranges delivery via the order's shipping **address**, but no longer records shipment-tracking metadata: `trackingNumber`, `shippingCarrier` and `shippedAt` were removed entirely (owner decision 2026-07-23, migration `V350`). `order_shipping_items` is kept only as a historical snapshot for legacy/imported orders and gets no new rows. `CONFIRMED_FROM_CODE`
- No external shipping carrier integration was confirmed in active repo code. `NOT_FOUND_IN_REPO`
- **Hóa đơn điện tử (NĐ 123/2020) — chưa triển khai.** Owner decision 2026-07-15 (FIX_PROMPT_2026-07-15.md §2 #9): đây là dự án riêng, không thuộc đợt sửa audit 07/2026 — owner sẽ chọn nhà cung cấp HĐĐT; **vẫn là blocker trước khi bán thật**. Hệ thống hiện không phát hành hóa đơn cho đơn hàng. `NOT_IMPLEMENTED_BLOCKER`

Evidence:

- `SecurityConfig.java`
- `CheckoutService.java`, `CheckoutOptionsResponse.java`, `V264__remove_shipping_methods.sql`
- repo search for payment/shipping providers

> **Accounts Receivable (công nợ / bán chịu) was REMOVED platform-wide (2026-06-23).** The credit-sale flow (POS `CREDIT` payment method), per-customer credit limit / payment terms, the `accounts_receivable` ledger, receivable payments, write-off, and the overdue scheduler no longer exist. (POS itself was subsequently removed entirely, 2026-06-23, online-only — see "POS Rules".) There is no downstream debt-collection process. The old `AR_RULE_*` IDs and the three receivable state machines were deleted.

## Search Engine Indexing Rules (SEO)

Họ rule này được lập ngày 2026-08-06 sau đợt audit index/noindex toàn site. Trước đó
repo **không có** rule ID nào cho SEO — `AGENTS.md` §12 chỉ có 5 gạch đầu dòng hướng dẫn
chung. Các quyết định dưới đây do owner chốt trong phiên 2026-08-06.

- `SEO_RULE_001` (owner decision 2026-08-06): Mỗi **sản phẩm, danh mục, thương hiệu và bài viết** có **hai** cờ "cho Google hiển thị", tách riêng theo ngôn ngữ: một cho bản tiếng Việt, một cho bản tiếng Anh. Tắt cờ nào thì trang ngôn ngữ đó mang thẻ `noindex`, bị loại khỏi `sitemap.xml` và không được khai `hreflang`; trang **vẫn xem được** nếu khách truy cập thẳng — đây là điểm khác biệt với việc chuyển về Nháp (Nháp thì khách cũng mất quyền xem và link cũ hỏng). Mặc định cả hai cờ là "cho hiển thị" (`NOT NULL DEFAULT false` trên cột `seo_no_index` / `seo_no_index_en`). Cột được thêm ở `V371` cho `products`/`categories`/`brands` và bổ sung `seo_no_index_en` cho `articles` (`articles.seo_no_index` đã có từ `V222`). Lịch sử: cột từng tồn tại ở `V1` cho cả 5 bảng, bị DROP toàn bộ ở `V152`, khôi phục riêng cho bài viết ở `V222`. `CONFIRMED_FROM_OWNER_DECISION`

- `SEO_RULE_002` (owner decision 2026-08-06): Trang **tiếng Anh** chỉ được khai báo với công cụ tìm kiếm khi đạt **ngưỡng đủ nội dung tiếng Anh**. Chưa đạt thì trang mang `noindex`, không vào sitemap, không được khai `hreflang` — bất kể cờ ở `SEO_RULE_001` đang bật. Ngưỡng theo từng loại:

  | Loại | Đủ nội dung EN khi có |
  |---|---|
  | Sản phẩm | `name_en` **và** (`short_description_en` hoặc `description_en`) |
  | Danh mục | `name_en` **và** (`description_en` hoặc `intro_content_en`) |
  | Bài viết | `title_en` **và** `body_en` |
  | Thương hiệu | `description_en` |

  **`slug_en` KHÔNG nằm trong ngưỡng** — nhất quán với `PRODUCT_RULE_003`/`CATEGORY_RULE_003`/`ARTICLE_RULE_003` ("slugEn chỉ là slug EN ưu tiên, không phải điều kiện tồn tại trang"). Bảng `brands` không có `name_en`/`slug_en` (DROP ở `V352`) và tên thương hiệu là danh từ riêng nên không cần dịch — vì vậy ngưỡng của thương hiệu chỉ xét phần mô tả.

  Ngưỡng được **tính động từ dữ liệu**, không lưu thành cột: bản dịch mới tạo tự động ở trạng thái ẩn cho tới khi có nội dung, và dịch xong là tự hiện mà không cần ai nhớ bấm nút. Cờ thủ công ở `SEO_RULE_001` là lớp **ghi đè thêm**, dùng để ẩn một trang EN đã đủ nội dung.

  Số đo tại thời điểm chốt rule (2026-08-06): đạt ngưỡng 135/180 sản phẩm PUBLISHED, 75/174 bài viết PUBLISHED, 31/35 danh mục hiện, 13/19 thương hiệu hiện. `CONFIRMED_FROM_OWNER_DECISION`

- `SEO_RULE_003` (2026-08-06): `<link rel="canonical">` **luôn tự sinh từ slug theo đúng locale của trang**, cho cả 4 loại — không phải trường admin nhập. Ô "Địa chỉ chuẩn (canonical URL)" trên màn Danh mục và Thương hiệu là trường chết (web không bao giờ đọc `seo.canonicalUrl` để dựng thẻ canonical) nên được gỡ, thống nhất với màn Sản phẩm và với `ARTICLE_RULE_006`. Cột `seo_canonical_url` trong database **giữ nguyên**, không xoá dữ liệu cũ. `CONFIRMED_FROM_CODE`

- `SEO_RULE_004` (2026-08-06): `robots.txt` **chỉ được** `Disallow` những đường dẫn không render HTML nên không gắn được thẻ `noindex` — hiện là `/api/`, `/admin/`, `/_internal/`. Trang đã có thẻ `noindex` (giỏ hàng, thanh toán, đơn hàng, tài khoản, đăng nhập/ký, quên mật khẩu, tìm kiếm, xác nhận email) **không được** đồng thời bị chặn: Google phải tải được trang mới đọc được thẻ, nếu chặn thì URL đã lỡ index không bao giờ gỡ ra được. Các URL `.html` legacy cũng không được chặn, nếu không Google không thấy được redirect 301. Trang xem thử bản nháp (`/preview/*` và `/en/preview/*`) dùng header `X-Robots-Tag` trong `next.config.ts`, **không** dùng `robots.txt`. `CONFIRMED_FROM_CODE`

- `SEO_RULE_005` (2026-08-06): Sản phẩm ở trạng thái `TRASH` khiến backend trả **`410 Gone`** (`CatalogReadService.getProductBySlug`) — tín hiệu gỡ index mạnh hơn 404. Web trả **404** cho trường hợp này vì Next.js không có hàm phát 410 (chỉ có `notFound()` → 404). Google gỡ index với 404 và 410 như nhau nên chênh lệch không đáng kể; ghi lại ở đây để hành vi công khai này có chủ sở hữu. Bài viết, danh mục và thương hiệu đã ẩn trả 404. `CONFIRMED_FROM_CODE`

- `SEO_RULE_006` (2026-08-06): Redirect **chuẩn hoá slug ở tầng trang** (`PRODUCT_RULE_003`, `CATEGORY_RULE_003`, `ARTICLE_RULE_003`) phát mã **308**, không phải 301. Lý do kỹ thuật: `permanentRedirect()` của Next.js chỉ phát được 308 — `node_modules/next/dist/client/components/redirect-status-code.js` chỉ định nghĩa `SeeOther=303`, `TemporaryRedirect=307`, `PermanentRedirect=308`. Google xử lý 308 **tương đương 301** cho canonical hoá và chuyển link equity; 308 còn giữ nguyên phương thức request. Redirect ở tầng **cấu hình** (`next.config.ts` `redirects()`) và ở **proxy** vẫn đặt được 301 tường minh và giữ nguyên như cũ. Ba rule trên vì vậy ghi "301 hoặc 308 (vĩnh viễn)" thay vì chỉ 301. `CONFIRMED_FROM_CODE`
- `SEO_RULE_007` (owner decision 2026-08-16): Dữ liệu có cấu trúc dùng địa chỉ tuyệt đối cho mọi trang, ảnh, logo, biểu tượng và ảnh đại diện video; địa chỉ tuyệt đối đã có sẵn được giữ nguyên. Breadcrumb sản phẩm phải dùng đúng chuỗi danh mục đang hiển thị, gồm danh mục cha nếu có; thương hiệu không phải một nấc breadcrumb. Trang danh mục khai một `CollectionPage`/`ItemList` duy nhất cho các sản phẩm đang hiển thị ở trang hiện tại, với số thứ tự tiếp tục qua phân trang. `CONFIRMED_FROM_OWNER_DECISION`
- `SEO_RULE_008` (owner decision 2026-08-16): Khối FAQ danh mục chỉ dùng một kiểu khai báo JSON-LD riêng. HTML hiển thị dùng thẻ tiêu đề mục cho câu hỏi và không nhúng thêm FAQ microdata; câu trả lời trong JSON-LD là chữ thuần, khớp nội dung khách nhìn thấy. `CONFIRMED_FROM_OWNER_DECISION`

Evidence: `bigbike-web/app/sitemap.ts`, `bigbike-web/app/robots.ts`, `bigbike-web/lib/seo/metadata.ts`, `bigbike-web/next.config.ts`, `bigbike-backend/.../V371__add_seo_no_index_per_locale.sql`, `bigbike-backend/.../CatalogReadService.java`, `bigbike-web/__tests__/seo/`, `bigbike-web/e2e/http-status.e2e.ts`.

## Reports Rules

Status: `CONFIRMED_FROM_CODE` — derived from audit of `AdminReportService.java`, `OrderJpaRepository.java`, `OrderLineItemJpaRepository.java`, `AdminCustomerService.java`.

> **Refund metrics were removed (2026-06-23)** together with the refund feature: `refundAmount`, `netRevenue` (= paid − refund), and the `REFUNDED`-related status handling no longer exist. Net revenue now equals paid revenue.

### Metric Definitions

- `REPORT_RULE_001`: **GMV (`grossOrderValue`)** = `SUM(totalAmount)` for orders where `placedAt` is within the requested range AND `status != 'CANCELLED'`. `CONFIRMED_FROM_CODE`
- `REPORT_RULE_002`: **Paid Revenue (`paidRevenue`)** = `SUM(totalAmount)` for orders where `placedAt` is within the requested range AND `status = 'COMPLETED'`. The response field name remains for API compatibility; completion is the source of truth for recognized revenue. `CONFIRMED_FROM_CODE`
- `REPORT_RULE_005`: **Order Count (`orderCount`)** = `COUNT(id)` excluding `status = 'CANCELLED'`. `CONFIRMED_FROM_CODE`
- `REPORT_RULE_006`: **Average Order Value (`avgOrderValue`)** = `grossOrderValue / orderCount`. Returns zero if `orderCount = 0`. `CONFIRMED_FROM_CODE`

### Excluded Status Sets

- `REPORT_RULE_007`: Both revenue and ranking metrics exclude `status = 'CANCELLED'` — applied to GMV, paidRevenue, orderCount, avgOrderValue, daily revenue, topProducts and topCustomers rankings. (The separate `REFUNDED` ranking exclusion was removed 2026-06-23; `FAILED` was removed as an order status entirely on 2026-07-21 — old `FAILED` orders were migrated to `CANCELLED`, so this exclusion set no longer needs a separate `FAILED` entry.) `CONFIRMED_FROM_CODE`

### Timezone

- `REPORT_RULE_008`: All date boundaries (`from`, `to` params) are parsed in `Asia/Ho_Chi_Minh` timezone. Daily revenue grouping uses `AT TIME ZONE 'Asia/Ho_Chi_Minh'`. This matches `AdminDashboardService` behavior. `CONFIRMED_FROM_CODE`

### Product And Customer Rankings

- `REPORT_RULE_009`: **topProducts** uses `COALESCE(product_pk, product_id::text)` as group key. Admin-created products have `product_id = NULL` and `product_pk` set; regular products have both. Filtering `product_id IS NOT NULL` (legacy behavior) silently excludes admin-created products. `CONFIRMED_FROM_CODE`
- `REPORT_RULE_010`: **topCustomers** uses `COALESCE(customer_id::text, customer_email)` as group key to prevent the same customer appearing as multiple rows if their email changed over time. Display email is `MAX(customer_email)`. `CONFIRMED_FROM_CODE`

### Custom Date Range Limit

- `REPORT_RULE_011`: The admin Reports screen limits a custom `from`–`to` date range to a maximum of 90 days (inclusive) — enforced **client-side only** in `ReportsScreen.jsx` (`isRangeWithinLimit`). The backend `/admin/reports/analytics` endpoint does not itself reject wider ranges (it only validates `from <= to`). Selecting a wider custom range shows a validation message and blocks the query. Owner decision 2026-07-23: keep the limit as UI-only guardrail. `CONFIRMED_FROM_CODE` (frontend)

Evidence:

- `AdminReportService.java`
- `OrderJpaRepository.java`
- `OrderLineItemJpaRepository.java`
- `AdminCustomerService.java`
- `ReportsScreen.jsx` (bigbike-admin)

## Returns And Refunds

> **Returns (RMA) and Refunds were REMOVED platform-wide (2026-06-23).** The customer return flow, the admin returns module, per-item inspection, RMA stock-restore, and **every refund path** (cancel-time refund, POS refund, manual order refund) no longer exist. The `returns` / `return_items` / `return_history` tables, the `refund_amount` / `refund_reason` / `refunded_at` order & payment columns, and the `REFUNDED` order/payment status were dropped. Old `REFUNDED` orders were migrated to `CANCELLED`. The old `RETURN_RULE_*` IDs, the return state machine, and `RefundService` were deleted.
>
> A **paid order is now cancelled directly** — the admin reconciles the money manually outside the system (see `ORDER_RULE_004`). There is no system-tracked return lifecycle.
>
> **Customer-facing return/exchange policy text is kept** (CMS policy pages — including "Chính sách bảo hành" — and the per-product "Đổi size 30 ngày" line). That is a **manual commitment shown to customers**, not a system feature. (The separate warranty-lookup feature/page was removed entirely 2026-06-23, V266.)

## Contact Inbox Rules

> Removed. The public contact form and admin contact inbox were deleted (migration `V128__drop_contact_messages.sql`). Customers reach the shop through the contact info on `/lien-he` (hotline, Zalo, Facebook, address, map) — a **static page** (see "Contact Page Rules"); the shared contact values come from `site_settings` group `contact`. There is still no contact form, no `contact_messages` table, and no `contact.read`/`contact.write` permissions.

## Live Migration Rules

- `LIVE_MIGRATION_RULE_001`: Target nonblank data wins; only exact reviewed owner exceptions may change it. All new products are `DRAFT`. Customers, orders, admin users, sessions and auth data have no migration write path. `OWNER_CONFIRMED_2026-08-03`
- `LIVE_MIGRATION_RULE_002`: WordPress products `41038` and `41181` with SKU `SCS-S10X` are never merged. Select only the strictly newer `post_modified_gmt`, require selected ID `41181`, import it as `DRAFT`, and exclude `41038` from all content/SEO/translation/media mapping. A tie, missing timestamp or unexpected winner blocks execution. `OWNER_CONFIRMED_2026-08-03`
- `LIVE_MIGRATION_RULE_003`: Missing product SKU/brand/gender uses controlled evidence only. Brand is one exact normalized target token/alias or `uncategorized-brand`; gender is optional: Nam/Nữ may come only from exact approved title evidence, while legacy unisex or reviewed neutral-accessory evidence maps to NULL; SKU is one clear normalized title model code unique in source and target. No fuzzy brand, new brand, slug/source-ID/`WP-*` SKU, bulk apparel gender or price `0`. Every inferred field is reported; unresolved rows are `MANUAL_REVIEW` and block execution. `OWNER_CONFIRMED_2026-08-03`
- `LIVE_MIGRATION_RULE_004`: A legacy URL with no related safe target is `ACKNOWLEDGED_NO_SAFE_TARGET`, writes no redirect and remains 404. Homepage and generic product-listing fallback are forbidden. In HTML only, the exact dead `<a>` wrapper may be removed while preserving anchor content; plain-text/canonical/structured URLs are reported, not deleted. `OWNER_CONFIRMED_2026-08-03`
- `LIVE_MIGRATION_RULE_005`: Four staged recovery files may be copied only after exact path/bytes/MIME/SHA verification and an explicit gate, never over different bytes. The unavailable `2021/05/mua-giay-scoyco-alpinestar-1024x772.png` may only be restored from an exact original; otherwise only its exact dead image/reference in `wp-art-26064.body`/`body_en` is removed with whole-field optimistic hashes and audit. `OWNER_CONFIRMED_2026-08-03`
- `LIVE_MIGRATION_RULE_006`: Target missing-media rows may be deleted only after verified backup, final zero-reference scan, exact ID/key/reference list and separate destructive confirmation. Duplicate canonical priority is valid object/hash, references, admin/audit provenance, oldest record, then ID; never filename. Rebinds are optimistic and validated before exact DB-row deletion. Duplicate objects remain at least 24 stable hours after cutover and need another confirmation. `OWNER_CONFIRMED_2026-08-03`

## Maintenance Mode Rules (owner-confirmed 2026-08-06, thu gọn phạm vi cùng ngày)

Owner chốt lại phạm vi chiều 2026-08-06: bảo trì **chỉ** áp dụng cho trang quản trị, khách hàng không bao giờ bị ảnh hưởng, và chỉ tài khoản kỹ thuật riêng được bật/tắt. Các rule dưới đây thay thế hoàn toàn bản buổi sáng.

- `MAINTENANCE_RULE_001`: Chế độ bảo trì chỉ áp dụng cho **trang quản trị**. Storefront không bao giờ có chế độ bảo trì thủ công; không có công tắc nào tắt được trang bán hàng. Trang tĩnh xin lỗi khách chỉ xuất hiện khi container thật sự không phản hồi (Nginx `error_page 502/503/504`), không do ai bật.
- `MAINTENANCE_RULE_002`: Khách **luôn** đặt hàng được, ở mọi trạng thái bảo trì. Không tồn tại công tắc tạm ngưng nhận đơn và `POST /api/v1/checkout` không có guard bảo trì nào.
- `MAINTENANCE_RULE_003`: Có đúng ba trạng thái theo thứ tự `NORMAL` → `UPCOMING` → `ACTIVE`. `UPCOMING` chỉ là cảnh báo trước cho nhân viên; ở trạng thái này nhân viên **vẫn lưu được** dữ liệu bình thường.
- `MAINTENANCE_RULE_004`: Ở `ACTIVE`, backend **từ chối thật** mọi thao tác ghi dưới `/api/v1/admin/**` bằng HTTP `423` và mã `MAINTENANCE_ACTIVE`. Backend KHÔNG chặn thao tác đọc — nhưng giao diện admin **cố ý che kín toàn màn hình** với nhân viên không phải `DEVELOPER` (owner chốt 2026-08-06), nên trên thực tế nhân viên không tra cứu được gì trong lúc khoá. Đừng dựa vào việc "đọc không bị chặn ở backend" để hứa với nhân viên là họ vẫn tra được đơn. Cố ý dùng `423`, không dùng `503`: `503` dành cho lỗi upstream/bảo trì thật và không được lẫn với khoá nghiệp vụ hoặc `429 RATE_LIMIT_EXCEEDED`.
- `MAINTENANCE_RULE_005`: Chỉ vai trò `DEVELOPER` được đổi trạng thái. **Cố ý loại trừ `SUPER_ADMIN`** dù vai trò này giữ quyền `*`: cổng khoá phải so khớp TÊN VAI TRÒ, không phải permission, vì `DevAdminAuthService.hasAnyPermission` cho `*` vượt qua mọi permission check vô điều kiện. Mọi vai trò khác vẫn **đọc** được trạng thái để biết vì sao không lưu được.
- `MAINTENANCE_RULE_006`: Không có chuyển trạng thái tự động. Hệ thống không tự khoá theo đếm ngược và không tự mở lại theo thời gian. `maintenance_expected_at` chỉ để hiển thị. Lý do: một lần tự mở khoá giữa chừng khi dev đang chạy migration dở dang sẽ cho nhân viên ghi đè lên dữ liệu nửa vời — đúng thứ tính năng này sinh ra để ngăn.
- `MAINTENANCE_RULE_007`: Khi `ACTIVE`, nhân viên (không phải `DEVELOPER`) thấy hộp thông báo chặn toàn màn ghi rõ thao tác chưa lưu **CHƯA** được ghi nhận; `DEVELOPER` chỉ thấy dải thông báo không chặn để còn thao tác được. Trước khi khoá, giao diện phải cảnh báo số tệp đang tải lên dở dang để dev tự quyết định thời điểm.

## Trợ lý ảo AI “Trợ lý BigBike” (owner decision 2026-08-09)

- `CHAT_RULE_001` (làm rõ 2026-08-12 và 2026-08-14): Trợ lý BigBike là trợ lý bán hàng 24/7 trên toàn bộ storefront, ưu tiên xưng “em” và gọi khách “anh/chị”, trả lời theo locale Việt/Anh của trang. Lớp an toàn **cấm** gọi khách là “em” hoặc dùng câu xua đuổi/cộc lốc, nhưng phải xét đúng ngữ cảnh: “em cần/tìm…” khi trợ lý tự xưng, “từ tìm kiếm” và câu hỏi “hai hoặc ba mẫu nào” không phải vi phạm. Số chỉ bị coi là khẳng định catalog/tồn kho khi câu thực sự khẳng định số lượng hàng; mọi câu soạn sẵn VI/EN phải có kiểm thử đi qua cùng lớp an toàn, đồng thời chiều chặn gọi khách bằng “em”, xua đuổi, bịa số, mã kỹ thuật, số điện thoại và URL lạ vẫn giữ nguyên. Header phải ghi rõ đây là trợ lý ảo AI và nút **Gặp nhân viên** luôn hiện.
- `CHAT_RULE_002`: Phạm vi chỉ gồm hàng đang bán, chọn/so sánh sản phẩm, size, giá, trạng thái còn/hết hàng, chính sách cửa hàng, thông tin cửa hàng và đơn của chính khách đã đăng nhập. Trợ lý BigBike không tư vấn xe, chính trị hoặc kiến thức ngoài shop.
- `CHAT_RULE_003` (owner decision 2026-08-11, Đợt 2): Gemini chỉ được chọn **sáu** công cụ đọc cố định: `search_products`, `list_categories`, `get_product`, `get_policy`, `get_shop_info`, `get_my_orders`. `list_categories` chỉ trả nhóm hàng công khai cùng số sản phẩm đang bán của từng nhóm; không trả danh sách sản phẩm. `capture_lead` là hành động ghi riêng của backend sau consent theo `CHAT_RULE_012`, không được khai báo làm function cho model. Không sinh SQL, không nhận truy vấn động và không có công cụ sửa/xoá dữ liệu shop. Thuộc tính biến thể khi gom nhóm phải bỏ dấu và không phân biệt hoa thường (`Màu`/`Màu sắc`/`color`, `Size`, `Model`).
- `CHAT_RULE_004`: `get_my_orders` lấy `customer_id` duy nhất từ phiên đăng nhập hợp lệ phía server; model và request không được truyền id/email khách. Mặc định chỉ trả trạng thái, ngày đặt và tổng tiền; không gửi danh sách sản phẩm hay địa chỉ. Khách vãng lai được dẫn sang trang tra cứu đơn hiện có, không tra trong chat.
- `CHAT_RULE_005` (owner decision 2026-08-18): Mỗi lượt gửi Gemini câu hỏi hiện tại dưới nhãn riêng, danh sách nhỏ các **nhóm hàng và thương hiệu công khai** hiện có, kết quả công cụ liên quan và tối đa **12 cặp hỏi–đáp gần nhất của chính hội thoại đó** dưới nhãn `RECENT_TURNS`. Số cặp là setting quản trị `ai_assistant_recent_turn_pairs`, mặc định `12`, tối đa `12`; `0` tắt hoàn toàn. Mỗi tin lịch sử phải che số điện thoại, email, địa chỉ và mã/số đơn hàng trước khi gửi rồi cắt tối đa 450 ký tự; không được ghi payload đã che vào log. Lịch sử chỉ giúp hiểu tham chiếu và ý nối tiếp, tuyệt đối không phải nguồn dữ liệu: tên mẫu, giá, tồn kho, biến thể, thông số, chính sách và mọi con số phải được công cụ xác minh lại ở **lượt hiện tại**. Tối đa tám slug công khai đã xác minh được giữ dưới nhãn `RECENT_VERIFIED_PRODUCTS`; backend kiểm tra allowlist trước mọi lần đọc. Khoá Gemini chỉ ở backend/biến môi trường, không xuống trình duyệt, DB hoặc log.
- `CHAT_RULE_006` (owner decision 2026-08-18): Câu hỏi thường gặp về bảo hành, đổi trả, giao hàng/phí giao, thanh toán, giờ mở cửa, địa chỉ và chọn size dùng câu trả lời soạn sẵn hoặc dữ liệu shop hiện có. Với một mẫu cụ thể, mọi thông số, bảng size, màu, giá và tồn kho phải được xác minh ở lượt hiện tại. Hệ thống giữ **nhóm sản phẩm đang được nói tới** qua các câu hỏi chính sách hoặc câu phụ không đổi chủ đề; “mẫu thứ hai” bám đúng thứ tự của nhóm gần nhất. So sánh trực tiếp giới hạn tối đa ba mẫu. Nếu tham chiếu vẫn có nhiều khả năng, Trợ lý BigBike phải nêu đúng tên để khách chọn, không đoán. Không có dữ liệu phải nói rõ chưa lưu và đưa ra một bước hỏi tiếp phù hợp.
- `CHAT_RULE_007` (owner decision 2026-08-18): Một câu trả lời đúng và đầy đủ là hợp lệ; không còn sàn hai câu. Nội dung hiển thị tối đa **10 câu hoặc 2.000 ký tự**, danh sách và bảng có giới hạn hàng; khi rút gọn phải giữ câu hoàn chỉnh và mọi cảnh báo/lời khai bắt buộc. Kết quả tìm hàng hiển thị tối đa tám thẻ theo `CHAT_RULE_020`; so sánh trực tiếp vẫn tối đa ba mẫu. Trợ lý BigBike không hứa giảm giá, không cam kết ngày giao, không nhận đổi/trả ngoài chính sách và không chốt đơn thay khách.
- `CHAT_RULE_008` (giữ nguyên theo owner decision 2026-08-11): Khiếu nại, bảo hành phức tạp và thương lượng giá chuyển ngay sang người thật. Hai lượt lệch chủ đề liên tiếp cũng dừng tư vấn và mời khách qua Zalo/Messenger.
- `CHAT_RULE_009` (owner decision 2026-08-18): Mỗi hội thoại tối đa 12 lượt hỏi–đáp. Hết lượt thì khóa ô gửi và mời gặp nhân viên. Rate limit riêng là 10 yêu cầu ghi/phút/IP và áp dụng đồng đều cho gửi tin, để lại lead và từ chối lead; backend có thêm khóa hội thoại đã HMAC để chống đổi IP. Nhiều hội thoại phải được xử lý đồng thời, nhưng các thao tác ghi của cùng một hội thoại phải tuần tự. Deadline xử lý toàn lượt ở backend là **65 giây**; storefront dừng chờ sau **75 giây**, báo rõ có thể thử lại và luôn giữ nút Gặp nhân viên. Giới hạn này không thay thế quy tắc một lead/đúng consent tại `CHAT_RULE_012`.
- `CHAT_RULE_010` (owner decision 2026-08-18): Cài đặt quản trị gồm bật/tắt, trần lượt AI theo ngày giờ Việt Nam, câu chào và danh sách gợi ý nhanh. Trần mặc định là **120 lượt AI/ngày**; không đặt trần tiền tháng và `0` nghĩa là không gọi AI. Mỗi logical response đi vào AI phải giữ đúng một slot bằng phép tăng nguyên tử theo ngày `Asia/Ho_Chi_Minh`; fast-path và các từ chối cục bộ theo `CHAT_RULE_027` không dùng slot. Retry mạng/provider của cùng `requestId` không tạo slot thứ hai. Hệ thống đo chi phí ước tính để owner theo dõi, không dùng số tiền đó để tự khoá trợ lý.
- `CHAT_RULE_011` (owner decision 2026-08-11): Khi setting tắt, thiếu khoá hoặc chạm trần ngày, Trợ lý BigBike fail-safe trong cùng một khung chat, không chuyển sang màn liên hệ riêng. Thẻ Hotline–Zalo–Messenger hiện có chỉ mở inline khi khách bấm **Gặp nhân viên**, nút này luôn hiển thị; ô nhập bị khoá và phải có dòng giải thích lấy từ câu trả lời của backend. Vấp kỹ thuật trong lúc đang tư vấn xử lý theo `CHAT_RULE_019`, không được làm mất kênh liên hệ người thật hoặc làm hỏng kiểm duyệt đánh giá.
- `CHAT_RULE_012` (bổ sung điều kiện kích hoạt 2026-08-13): Trợ lý BigBike chủ động mời khách để lại số/Zalo đúng một lần trong mỗi hội thoại khi có dấu hiệu quan tâm — khách đã xem hoặc hỏi một sản phẩm cụ thể, hỏi giá/size/còn hàng — hoặc khi lượt tra cứu không trả lời được và phải dùng câu fallback phục hồi. Lời mời chỉ được trả khi `lead_offer_status=NONE`, không mời trong lời chào, câu ngoài phạm vi, handoff đã đóng hoặc sau khi khách đã từ chối/đồng ý; model chỉ có thể đề xuất, backend là nơi quyết định cuối cùng và cập nhật `OFFERED`. Chỉ `capture_lead` sau khi khách chủ động đồng ý; từ chối thì không hỏi lại. Lead đã đồng ý được lưu và đẩy thông báo về admin, nhưng không được ghi PII vào log.
- `CHAT_RULE_025` (owner decision 2026-08-13): Với khách đã đăng nhập, nếu tài khoản có tên hiển thị và số điện thoại đã lưu ở dạng dùng được, widget được hiển thị thẻ xác nhận nhanh song ngữ: che phần giữa số điện thoại, có nút đồng ý rõ ràng và nút từ chối. Không tự ghi lead chỉ vì khách đăng nhập. Khi chọn đồng ý nhanh, backend phải lấy tên và số từ `CustomerPrincipal`/bản ghi tài khoản hiện tại, bỏ qua tên/số/ghi chú do trình duyệt gửi lên và lưu `source=ACCOUNT`; thẻ nhanh không có ghi chú. Nút “Dùng số khác” mở lại biểu mẫu đầy đủ, điền sẵn thông tin tài khoản để khách sửa và gửi theo `source=FORM`. Khách vãng lai, tài khoản thiếu tên/số hoặc số không dùng được vẫn dùng biểu mẫu đầy đủ. Lead `ACCOUNT` và `FORM` phải phân biệt được ở admin; các giới hạn một lời mời, một lựa chọn và lưu trữ 90 ngày của `CHAT_RULE_009`/`CHAT_RULE_012` giữ nguyên.
- `CHAT_RULE_013`: Lịch sử hội thoại, tin nhắn và lead **phía backend** lưu tối đa 90 ngày rồi job backend xoá tự động. Admin chỉ xem được bằng quyền DB-driven `chat.read`; không có thao tác sửa/xoá thủ công trong giai đoạn 1. Bản sao cục bộ trên máy khách của widget không thay đổi retention server và chỉ được giữ theo `CHAT_RULE_026` trong tối đa 24 giờ.
- `CHAT_RULE_026` (owner decision 2026-08-18): Widget lưu cục bộ cuộc trò chuyện đang dở trong tối đa 24 giờ và khôi phục đúng `conversationId`, `requestId` của lượt đang gửi/thử lại, tin nhắn, sản phẩm, số lượt còn lại và trạng thái lead/CONTACT. Hạn 24 giờ tính từ lần tạo bản sao, không được gia hạn vô hạn chỉ vì tải lại. Nút “Xoá cuộc trò chuyện” và đăng xuất xoá ngay bản sao. Chỉ lưu dữ liệu tối thiểu để hiển thị/khôi phục; không lưu bản nháp liên hệ, số điện thoại chưa gửi, thông tin hồ sơ hay secret. Retry phải dùng lại `requestId`, không tạo hội thoại, tin nhắn hoặc lượt AI mới.
- `CHAT_RULE_014` (owner decision 2026-08-18): Trợ lý được cho khách chọn đủ size/màu và thêm hàng vào giỏ ngay trong chat theo `CHAT_RULE_028`. Gợi ý theo trang đang xem, báo cáo FAQ tự động, tra cứu đơn khách vãng lai trong chat và nhân viên chen vào hội thoại vẫn ngoài phạm vi.
- `CHAT_RULE_015` (owner decision 2026-08-09, sửa lỗi hiểu ngược giá): `search_products` phải đọc **đúng hướng** mức giá khách nêu, không được quy mọi con số về giá trần.
  - Trần (`max_price`): `dưới`/`không quá`/`không hơn`/`tối đa`/`rẻ hơn`/`đổ lại`/`trở xuống`/`under`/`below`/`up to`.
  - Sàn (`min_price`): `từ`/`trên`/`hơn`/`ít nhất`/`tối thiểu`/`trở lên`/`from`/`above`/`over`/`at least`.
  - Khoảng (`min_price`+`max_price`): `từ X đến Y`, `X - Y`, `X tới Y`, `from X to Y`, `between X and Y` — số đứng trước mượn đơn vị của số đứng sau (`3 đến 5 triệu` = 3.000.000–5.000.000; `between 3 and 5 million` = 3.000.000–5.000.000).
  - Cách viết giá đã duyệt: `NtrD` đến `NtrDDD` (ví dụ `1tr5`, `2tr3`, `1tr50`, `1tr500`; phần sau `tr` là phần thập phân của triệu) và cách nói `1 triệu 5` đều được hiểu theo giá trị triệu tương ứng; chúng vẫn đi theo từ chỉ hướng/khoảng ở trên. Số viết bằng chữ từ `một` đến `mười` khi đứng ngay trước đơn vị giá cũng được đọc theo giá trị tương ứng. Tiếng lóng `củ` và `lít` trong ngữ cảnh giá bằng một triệu đồng. Không tự đoán các dạng số rút gọn khác chưa được nêu rõ.
  - Áng chừng (`khoảng`/`tầm`/`about`/`around`/số trần trụi không kèm từ khoá): dải 70%–120% quanh con số đó.
- `CHAT_RULE_016` (owner decision 2026-08-09): Thứ tự sắp xếp phải bám ý khách, **không mặc định rẻ nhất trước**. Hỏi `cao cấp`/`xịn nhất`/`đắt nhất`/`premium` → giá giảm dần; hỏi `giá rẻ`/`tiết kiệm`/`bình dân` → giá tăng dần; có nêu mức giá → giá tăng dần trong phạm vi đã lọc; không nêu giá và không nêu tính từ → hàng mới nhất trước.
- `CHAT_RULE_017` (owner decision 2026-08-11, Đợt 2): Trong chế độ mới, AI được phép diễn giải chữ viết tắt và cách nói tự nhiên thành nhóm hàng, thương hiệu hoặc từ khoá loại hàng nhờ danh sách công khai theo `CHAT_RULE_005`; backend vẫn là bên quyết định cuối cùng trước khi đọc dữ liệu. Backend dùng parser giá và nhận diện catalog hiện có để **đối chiếu kết quả**, không tự thay AI bóc lại câu khách làm bộ lọc chính.
  - Giá chỉ lấy từ parser của câu khách hoặc tầm giá hợp lệ đang kế thừa. Khách không nêu giá mà AI tự thêm giá, hoặc AI đưa giá khác parser, thì chỉ bỏ tham số giá đó và vẫn tiếp tục lượt tìm.
  - Nhóm hàng/thương hiệu AI đưa phải khớp đúng một mục công khai hiện hành; mục không tồn tại hoặc mơ hồ chỉ bị bỏ, không được làm hỏng cả lượt. Thứ tự sắp xếp luôn do backend quyết theo `CHAT_RULE_016`.
  - AI có thể chuẩn hoá từ khoá loại hàng chung, nhưng không được tự tạo tên sản phẩm, model hoặc mã hàng. Backend chỉ giữ tên/model/mã mà khách đã nêu; màu và size cũng chỉ giữ khi có trong câu khách. Tham số không đạt đối chiếu bị loại riêng lẻ. Nếu sau khi loại không còn điều kiện tìm an toàn, hệ thống phải xin khách nói rõ hơn, không quét rộng toàn shop.
  - Trước khi tạo từ định danh, backend vẫn tách lớp dẫn nhập/câu hỏi theo ngữ cảnh: cụm hỏi thuộc tính biến thể như “có size và màu nào” và cụm định hướng như “theo ngân sách” không phải tên/model sản phẩm; câu hỏi gốc vẫn được giữ để nhận diện yêu cầu xem chi tiết và option cụ thể. Các từ đệm/hư chỉ được bỏ khi nằm trong danh sách trắng đã duyệt; không dùng suy đoán mơ hồ để xoá từ. Khi khách nêu tên/model/slug, các từ định danh còn lại phải cùng khớp metadata catalog theo cả locale VI/EN; không được đổi một tìm kiếm tên/model không khớp thành quét riêng theo nhóm hàng/thương hiệu. Sau đó vẫn chỉ áp dụng hàng đang bán/còn hàng và có giá bán hợp lệ.
  - Bảng viết tắt được duyệt phải được mở rộng trước khi nhận diện danh mục/thương hiệu, khớp theo nguyên từ hoặc nguyên cụm từ và theo thứ tự riêng trước/chung sau: `mbh`, `mũ bh`, `mu bh`, `nón` → “mũ bảo hiểm”; `kiếng` → “kính”; cụm `mũ ff` → “mũ fullface”; `tn` → “tai nghe”; `bh` → “bảo hành”; `sz` → “size”; `sdt` → “số điện thoại”; `cty` → “công ty”; `ship` → “giao hàng”; `ko`/`hok`/`khong` → “không”; `bn`/`bnhieu` → “bao nhiêu”; `z` → “vậy”; `j` → “gì”; `dc`/`đc` → “được”; `ntn` → “như thế nào”; `ae` → “anh em”; `ad` → “admin”. Không ánh xạ chung `hong`, `gt` hoặc `k`: `hong`/`hông` chỉ mang nghĩa “không” khi ở cuối câu hỏi xác nhận; `GT` có thể là một phần tên/model; `k` sau số vẫn được bộ đọc giá hiểu, ví dụ `500 k`. Toàn bộ alias phải được quét với tên sản phẩm, màu, thương hiệu và danh mục thật; alias va chạm bị vô hiệu hoá thay vì ghi đè dữ liệu catalog. Kết quả quét catalog ngày 2026-08-18 đã vô hiệu alias đơn `ff`, `sp`, `v` vì chúng xuất hiện trong dữ liệu màu/mã model hiện hành; chỉ cụm không va chạm `mũ ff` còn được chuẩn hoá.
  - Có thể sửa sai **một ký tự** chỉ cho tên danh mục/thương hiệu: so với alias công khai đã chuẩn hoá, chỉ xét từ chữ cái dài từ 4 ký tự và chỉ khi đúng một mục duy nhất khớp. Không áp dụng cho tên/model/mã hàng, màu, size hay giá; nhiều khả năng khớp thì không tự chọn.
- `CHAT_RULE_018` (owner decision 2026-08-11, làm rõ 2026-08-12): Cấm âm thầm nới rộng tìm kiếm. Không còn kiểu tìm hụt rồi quét cả shop mà không báo. Khi phải bỏ bộ lọc giá, nới từ khoá hoặc AI đã chọn một phạm vi tìm khác với phạm vi khách nêu/đang kế thừa, `search_products` gắn ghi chú vào kết quả và Trợ lý BigBike **bắt buộc nói rõ** với khách là tầm giá đó hiện chưa có hàng hoặc danh sách đang rộng hơn yêu cầu, kèm phương án gần nhất. Tìm hụt tên/model cụ thể không được bỏ định danh để trả một danh sách cùng danh mục/thương hiệu ngẫu nhiên. Nếu bộ lọc kế thừa (giá hoặc thương hiệu) cho kết quả rỗng, chỉ bộ lọc kế thừa đó được bỏ để thử lại trong phạm vi khách vừa hỏi và Trợ lý BigBike phải nói rõ việc này; bộ lọc khách nêu ở lượt hiện tại không được tự bỏ. Chủ đề, bộ lọc và slug cũ chỉ được xoá sau một lần tra hàng thành công đã xác minh rằng khách chuyển sang nhóm khác; câu hỏi phụ không tra hàng phải giữ nguyên context. Với **khoảng giá** không có hàng, phương án gần nhất lấy một mẫu sát mép dưới và một mẫu sát mép trên (nếu mỗi phía có hàng), không lấy danh sách rẻ nhất. Lớp chặn câu trả lời phải từ chối câu thiếu lời khai báo này. Tìm theo giá mà không kèm từ khoá (vd "sản phẩm từ 5 triệu") là hợp lệ, không tính là nới rộng.
- `CHAT_RULE_019` (owner decision 2026-08-18): Vấp kỹ thuật — gồm bộ lọc chặn câu trả lời, AI lỗi/không trả kết quả hoặc công cụ tra dữ liệu lỗi — là lỗi có thể phục hồi. Một logical response dùng tối đa **ba công cụ**, tối đa **bốn request nhà cung cấp**, cho phép công cụ đọc hợp lệ chạy song song hoặc nối tiếp. Provider chỉ retry một lần cho `429`, `5xx`, lỗi kết nối hoặc timeout; không retry lỗi schema/safety/guard. Mọi tên công cụ, tham số, slug và quyền phải được backend kiểm tra trước khi đọc. Trợ lý giữ hội thoại mở, ưu tiên nội dung/thẻ an toàn và chỉ fallback khi không còn nội dung an toàn. Mọi nhánh phải log mã nguyên nhân và telemetry theo `CHAT_RULE_029`, tuyệt đối không log câu hỏi, PII, payload, slug hay giá.
- `CHAT_RULE_020` (owner decision 2026-08-18): Khi lượt `search_products` hiện tại có số tổng do backend trả về, Trợ lý BigBike được nêu đúng con số đó và không được suy diễn thêm về toàn kho. Số sản phẩm hiển thị phải bằng `min(tổng khớp, 8)`; nếu tổng lớn hơn số hiển thị, phải nói rõ đang hiện bao nhiêu mẫu tiêu biểu trong tổng bao nhiêu. So sánh trực tiếp vẫn giới hạn ba mẫu. Số chỉ hợp lệ cho đúng phạm vi của lượt hiện tại; không có số backend cấp thì cấm kết luận định lượng về số mẫu, toàn bộ kho hoặc tồn kho.
- `CHAT_RULE_021` (owner decision 2026-08-11, Đợt 2): Quản trị viên có công tắc riêng để chuyển ngay giữa cách tìm hàng cũ và cách AI diễn giải có đối chiếu mới, không cần triển khai lại hệ thống. Mặc định bật cách mới. Khi tắt, `search_products` phải giữ nguyên cách xác minh cũ; công tắc không làm yếu bất kỳ giới hạn lượt, quyền, an toàn dữ liệu, điều kiện hàng đang bán hay lớp chặn câu trả lời nào.
- `CHAT_RULE_022` (owner decision 2026-08-11, làm rõ 2026-08-14): Khi khách hỏi toàn bộ shop bán những gì, AI dùng `list_categories` để lấy mọi nhóm hàng công khai, kể cả nhóm đang tạm chưa có hàng, cùng số sản phẩm đang bán theo từng nhóm. Riêng nút **Tìm theo nhu cầu/Find by need** và **Đổi nhu cầu/Change needs** là fast-path: chỉ nêu tối đa 4 nhóm công khai cấp cao nhất theo thứ tự catalog hiện có rồi hỏi khách cần loại nào; “Đổi nhu cầu” xoá các bộ lọc tìm kiếm kế thừa. Không thay đổi dữ liệu hoặc thứ tự nhóm hàng. Công cụ chỉ đọc và chỉ trả tổng đã hậu kiểm; không được gửi hay suy ra danh sách sản phẩm, giá hoặc tồn kho chi tiết.
- `CHAT_RULE_023` (owner decision 2026-08-12): Dữ liệu lựa chọn biến thể chỉ được hiện cho khách dưới nhãn dễ đọc. Giá trị màu chứa slug/mã kỹ thuật phải được chuẩn hoá an toàn (bỏ dấu nối, chuẩn hoá hoa/thường và dấu tiếng Việt khi nhận diện được) hoặc bị bỏ; không sửa dữ liệu gốc chỉ để phục vụ chat. Guard coi chuỗi mã nội bộ thường có dấu nối là rò rỉ dữ liệu kỹ thuật.
- `CHAT_RULE_024` (owner decision 2026-08-12): Khi tên/model khách gõ không khớp chính xác nhưng gần một mẫu **đang bán đã xác minh**, Trợ lý BigBike phải nêu đúng tên mẫu và hỏi “có phải anh/chị đang tìm … không?” trước khi tra chi tiết; không sửa ngầm. Nếu truy vấn khớp 2–3 mẫu thật, Trợ lý BigBike nêu đủ tên và giá đã xác minh rồi hỏi khách chọn. Nếu không có ứng viên đủ gần, Trợ lý BigBike nói thẳng shop chưa có mẫu đó và hỏi loại hàng hoặc tầm giá để tìm phương án tương đương; cấm dí sản phẩm ngẫu nhiên. Ứng viên gần đúng chỉ được xét trong hàng `PUBLISHED`, đang bán, còn hàng và có giá hợp lệ; nhiều ứng viên ngang nhau thì không tự chọn.
- `CHAT_RULE_027` (owner decision 2026-08-18): Trước quota và AI, backend dùng bộ lọc cục bộ để phân loại ngoài phạm vi, nội dung người lớn, chủ ý gây hại/tự hại, xúc phạm, hướng dẫn lừa đảo và câu lái vai; trả lời từ chối soạn sẵn VI/EN với nguồn `OUT_OF_SCOPE`, `CONTENT_REFUSAL` hoặc `ROLE_DEFENSE`, không tính quota AI. Câu tư vấn bảo hộ có “va chạm”, “tai nạn”, “chấn thương”, “đâm” vẫn hợp lệ nếu không thể hiện ý định gây hại. Bốn bộ lọc Gemini cấu hình `BLOCK_ONLY_HIGH`; `promptFeedback.blockReason` và `finishReason=SAFETY` phải đi nhánh từ chối nội dung, không hiện lỗi kỹ thuật.
- `CHAT_RULE_028` (owner decision 2026-08-18): Câu trả lời dùng Markdown giới hạn ở đoạn văn, in đậm, danh sách và bảng; cấm HTML, mã, ảnh, liên kết và URL. Bảng so sánh/bảng size chỉ dựng từ dữ liệu đã xác minh. Kết quả tìm hàng hiện tối đa tám thẻ cuộn ngang. Mỗi thẻ có Xem chi tiết, Chọn size/màu và Thêm vào giỏ; sản phẩm nhiều lựa chọn phải chọn đủ biến thể còn hàng ngay trong chat, mặc định số lượng 1. `assistantConversationId` chỉ được gắn vào dòng giỏ khi backend xác minh đúng sản phẩm đã xuất hiện trong hội thoại đó.
- `CHAT_RULE_029` (owner decision 2026-08-18): Mỗi lượt ghi token đầu vào, đầu ra, suy luận, số request nhà cung cấp, độ trễ và chi phí ước tính mà không ghi nội dung/PII/payload. Quản trị với quyền `chat.read` xem được tổng/giá trị trung bình, số từ chối nội dung, đơn và doanh thu được trợ lý hỗ trợ. Doanh thu quy theo đúng dòng hàng được thêm từ chat và giá cuối cùng backend xác nhận khi checkout; phát lại checkout không tạo attribution trùng. Khi hội thoại quá 90 ngày bị xoá, nội dung/liên kết hội thoại biến mất nhưng tổng doanh thu hỗ trợ đã ghi vẫn được giữ.
