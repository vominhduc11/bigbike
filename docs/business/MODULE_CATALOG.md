# Module Catalog

## Public Platform Modules

| Module | Surface | Current implementation | Status | Evidence |
|---|---|---|---|---|
| Catalog browse | web, backend | Products, categories, brands, articles, settings, menus, sliders, home videos. Static CMS/page builder vẫn đã gỡ; 8 route thông tin/chính sách giữ cố định trong web. Tiêu đề/thân bài Bảo hành và Đổi trả là bốn resource song ngữ cố định trong backend, dùng chung cho web và Trợ lý BigBike; Bảo mật vẫn tĩnh. Khối hotline/Zalo/Messenger/địa chỉ/giờ mở cửa trong policy lấy động từ nhóm `contact`. Nội dung song ngữ đọc theo locale URL; EN tùy chọn fallback từng trường về VI. Storefront dùng next-intl với URL là nguồn locale duy nhất: VI không prefix, EN prefix `/en`, không tự chọn locale bằng cookie/`Accept-Language`. | `OWNER_CONFIRMED_2026-09-01` | public controllers, `StorePolicyService`, `ChatAssistantSettings`, `public-api.ts`, `bigbike-web/i18n/routing.ts` |
| Listing page hero banners | web, backend, admin | Admin-managed hero (desktop background + title + gear illustration) on the **listing pages** (`/san-pham`, `/brands`, `/tin-tuc`) — backed by the `public_hero` settings group; each listing page has its own background and gear illustration, falling back to shared defaults. The desktop background is used at every breakpoint with responsive CSS; legacy mobile-image values remain stored but are not editable or rendered. Managed in admin via the dedicated **Banner trang** screen (`BannerScreen.jsx`) with a live composite preview. (CMS-page hero via `PageEntity` removed 2026-06-24 — info/policy pages now static in web.) | `CONFIRMED_FROM_CODE` | `SettingDefinitionRegistry` `hero_*` keys, `BannerScreen.jsx`, `bigbike-web/components/layout/PageHero.tsx` |
| Search | web, backend | `GET /api/v1/search-suggest` (typeahead dropdown). Trang tìm kiếm `/tim-kiem` dùng `GET /api/v1/products`. (`GET /api/v1/search` gỡ 2026-07-15, AUD-066 — không caller.) | `CONFIRMED_FROM_CODE` | `PublicSearchController.java`, `SecurityConfig.java`, clients |
| Cart | web, backend | Guest/customer cart with CSRF-protected mutations | `CONFIRMED_FROM_CODE` | `CartController.java`, `CartService.java`, tests |
| Checkout | web, backend | Cart checkout with idempotency. (The quick-buy "Mua nhanh" flow was fully removed by owner decision 2026-07-15, reversing AUD-010 — PDP button, `POST /orders/quick-buy` endpoint, service method, DTO and tests all deleted; customers order through the cart.) Storefront customers choose COD or BANK_TRANSFER; omitted payment is normalised to COD and unsupported codes are rejected (`PAY_RULE_001`). No shipping-method choice; orders carry no shipping fee (owner decision 2026-06-23, `SHIP_RULE_001`). | `CONFIRMED_FROM_CODE` | `CheckoutService.java`, tests, clients |
| Customer account | web, backend | Profile, addresses, orders | `CONFIRMED_FROM_CODE` | customer controllers, clients |
| Trợ lý BigBike | web, backend | Widget toàn site tư vấn hàng thật, một model Gemini 3.7 Flash cố định ở máy chủ, thử lại chính model khi lỗi và hướng khách tự liên hệ qua Hotline/Zalo/Messenger nếu cần. Không có chat với người thật; giữ thêm giỏ, nhớ trong phiên trình duyệt và đọc ảnh luôn khả dụng khi dịch vụ AI đã khai báo; ảnh riêng tư 90 ngày, không đoán size/bảo hành/đơn từ ảnh. Tên tiếng Anh hiển thị là BigBike Assistant. | `OWNER_CONFIRMED_2026-08-30; DISPLAY_NAME_OWNER_CONFIRMED_2026-09-01` | `CHAT_RULE_001`–`059` (các rule còn hiệu lực), `ChatController`, `FloatingChat.tsx`, private MinIO |
| Wishlist | — | **REMOVED (owner decision 2026-07-06).** Customer wishlist ("Sản phẩm yêu thích") dropped entirely — backend API + `wishlist_items` table removed; never had a web UI (no client ever called it). | `REMOVED` | `V320__drop_wishlist_items_table.sql` (drops table V103); deleted `CustomerWishlistController.java` / `WishlistItemEntity.java` / `WishlistItemJpaRepository.java` + `CustomerWishlistApiTest.java` |
| Product comparison | — | **REMOVED (owner decision 2026-07-06).** "So sánh sản phẩm" dropped — was previously documented as a browser-local compare feature but **no implementing code ever existed** in `bigbike-web` (all 4 cited files are absent); the owner confirmed not to build it. | `REMOVED` | (none — feature was never implemented in code) |
| Vietnam address lookup | web, backend | Mô hình 2 cấp sau cải cách 2025: tỉnh/thành → phường/xã. Web đọc danh sách tích hợp sẵn (`VN_PROVINCES`); backend còn API đọc công khai cùng dữ liệu nhưng hiện không có caller nội bộ. Field `district` chỉ giữ để đọc địa chỉ lịch sử. | `CONFIRMED_FROM_CODE` | `VnAddressController.java`, `VnAddressFields.tsx`, `vn-address-data.ts`, `DATA_CONTRACT.md` §Address fields |

## Admin Platform Modules

| Module | Surface | Current implementation | Status | Evidence |
|---|---|---|---|---|
| Catalog admin | admin, backend | Product/category/brand CRUD and related reads. A product has ordered multiple categories; the first is primary for breadcrumb/SEO. The product editor uses multi-select and ordering; list filters still select one category but match any direct/descendant attachment. JSON import validates each row independently and uses ordered `categorySlugs` (or `categoryIds`); single-product export emits the complete category list. Category `image` is the shared homepage category image and level-1 header-menu icon; the old `menuIcon` field remains only for data compatibility and is no longer shown in the form. | `OWNER_CONFIRMED_2026-08-29` | `CategoryDetailScreen.jsx`, `CategoryMutationService.java`, `AdminMenuService.java`, `V1069__move_khuyen_mai_hot_menu_icon_to_category_image.sql`, `DATA_CONTRACT.md` |
| Legacy discontinued history | admin, backend, web | Nhân viên quản lý riêng các URL mặt hàng cũ không còn bản ghi sản phẩm: tên VI/EN, thương hiệu, nhóm hàng, ảnh nguồn và bật/tắt. Không tạo giá/tồn kho giả. Web chỉ đọc chính xác URL `/sp/{slug}.html`, hiện “Đã ngừng bán” và tối đa 3 gợi ý đang bán cùng nhóm. Hàng sản phẩm hiện hữu vẫn dùng cờ `products.discontinued`. | `OWNER_CONFIRMED_2026-08-15` | `legacy_discontinued_products`, `LegacyDiscontinuedProductsScreen`, `REDIRECT_RULE_013` |
| Featured products admin | admin, backend, web | Dedicated “Sản phẩm nổi bật” workspace owns the ordered homepage `FEATURED_GRID`, with at most 12 published products. Opening the workspace requires both `products.read` (load/search products) and `products.update` (reorder, edit and save homepage placement); the atomic save endpoint remains gated by `products.update`. Removing a product from `PUBLISHED` automatically clears its homepage placement, while republishing never restores it automatically (`PRODUCT_RULE_015`). The default `ADMIN` and `SHOP_MANAGER` roles retain both permissions. | `OWNER_CONFIRMED_2026-07-28` | `FeaturedProductsScreen.jsx`, `HomepageBlockMutationService.java`, `ProductMutationService.java`, `V49__create_roles_permissions_tables.sql`, `API_CONTRACT.md` homepage placement |
| Order admin | admin, backend | Order list/detail/status workflows plus reversible historical classification. The operational view excludes actively classified legacy imports; staff can switch to Historical/All for customer lookup. Historical rows remain complete and read-only. | `OWNER_CONFIRMED_2026-08-31` | `ORDER_RULE_013`–`015`, `AdminOrderController.java`, admin app |
| Customer admin | admin, backend | Customer list/detail/update | `CONFIRMED_FROM_CODE` | `AdminCustomerController.java` |
| Reviews admin | admin, backend, web | Khách gửi đánh giá mới ở trạng thái `PENDING`; tài khoản đăng nhập dùng danh tính từ session, khách vãng lai phải nhập tên và có thể bỏ trống email. Admin xem list/detail (`reviews.read`), trong đó email chỉ có ở detail; đổi trạng thái đơn/bulk (`reviews.write`) theo vòng đời có kiểm soát và `expectedVersion`. `TRASH` có thể khôi phục; xóa vĩnh viễn chỉ từ `TRASH`, yêu cầu đồng thời exact built-in role `SUPER_ADMIN` và `reviews.write` (kể cả bulk). Ảnh review có claim đúng sản phẩm, tự dọn upload mồ côi và chỉ hiển thị công khai sau khi review được duyệt. **Kiểm duyệt tự động (2026-08-08):** sau khi khách gửi, hệ thống chạy 2 lớp lọc — danh sách từ cấm do shop tự quản, rồi AI đọc hiểu — và chỉ được **chặn** (`PENDING → SPAM|TRASH`), không bao giờ được duyệt. **Mời đánh giá sau mua (2026-09-01):** hệ thống tự chạy, cố định chờ 7 ngày và tối đa 20 lượt thử/ngày; cutoff được mở ở callback scheduler đầu tiên sau deploy, đơn cũ/legacy không gửi bù, link mở form hiện có không cần đăng nhập và khách có quyền từ chối vĩnh viễn. Không có mặt quản trị; công tắc khẩn chỉ ở env máy chủ. | `OWNER_CONFIRMED_2026-09-01` | `REVIEW_RULE_009`–`016`, review/invitation services, public controller and web routes |
| Media admin | admin, backend | Upload (đơn/nhiều file, kéo-thả)/list/detail/update/delete/restore media, cây thư mục hai cấp + thẻ để phân loại. Thư mục cha đếm gộp nội dung con; media mới được gắn vào Product/Content/Brand/Category/Slider/Settings chỉ tự vào thư mục nghiệp vụ khi chưa được xếp thủ công. **Bề mặt quản trị đã tinh gọn 2026-07-30:** màn thư viện chỉ còn một kiểu xem (lưới ảnh) và 4 bộ lọc cơ bản (tìm kiếm, loại file, đã dùng/chưa dùng, sắp xếp) cộng thư mục + thẻ ở sidebar — bỏ dạng bảng, bộ lọc theo ngày/dung lượng/kích thước. Mỗi thẻ ảnh tối đa 3 thao tác (copy URL, sửa, xoá mềm); thao tác hàng loạt còn chuyển thư mục · xoá mềm · khôi phục. **Xoá vĩnh viễn chỉ làm được từng file trong bảng chi tiết khi file đang ở Thùng rác** (vẫn gác quyền `*`, kèm cảnh báo file đang được dùng ở đâu) — không còn xoá vĩnh viễn hàng loạt trên giao diện, dù endpoint `bulk-hard-delete` vẫn tồn tại ở backend. Thay ảnh và danh sách biến thể kích thước nằm trong mục "Nâng cao" đóng sẵn của bảng chi tiết; tạo/đổi tên/xoá thư mục ẩn sau công tắc "Quản lý" ở sidebar. Cửa sổ chọn ảnh/video (media picker) chỉ làm tìm — tải lên — chọn; sửa mô tả ảnh và xoá file đã dồn hết về màn Thư viện. Đợt sắp xếp một lần ngày 2026-08-30 chỉ đổi thư mục, có thêm `Ảnh minh hoạ`; công cụ xem trước/chạy/hoàn tác/lịch sử đã được gỡ sau khi hoàn tất. | `OWNER_CONFIRMED_2026-08-30` | `AdminMediaController.java`, `AdminMediaFolderController.java`, `AdminMediaService.java`, `MediaAutoFolderService.java`, `MediaLibraryScreen.jsx`, `MediaFolderSidebar.jsx`, `MediaDetailPanel.jsx`, `MediaPickerModal.jsx` |
| Settings admin | admin, backend | Site settings read/update, including the dedicated listing-page banner editor and owner-controlled bản tin hết hàng. Thư mời đánh giá không còn thuộc Cài đặt; tính năng chạy tự động với công tắc khẩn ở env máy chủ. Ngưỡng đơn quá hạn hiệu lực cố định 2 ngày; `order_overdue_days` vẫn được giữ trong dữ liệu nội bộ cho nhắc nhở/lọc/CSV nhưng không còn tab hoặc ô chỉnh trong Cài đặt theo quyết định chủ shop ngày 2026-09-01. Không còn tab hoặc ô sửa `store_policy`; Bảo hành/Đổi trả đọc từ bốn resource backend cố định, còn Contact vẫn là dữ liệu động dùng để điền vào policy. | `OWNER_CONFIRMED_2026-09-01; POLICY_FREEZE_OWNER_CONFIRMED_2026-09-01` | `AdminSettingsController.java`, `SettingsScreen.jsx`, `StorePolicyService`, `V1078__freeze_store_policy_content.sql`, `ORDER_RULE_015`, `STOCK_ALERT_RULE_002`, `REVIEW_RULE_014`–`016`, tests |
| Vận hành Trợ lý BigBike | admin, backend | Tài khoản có `chat.read` chỉ xem danh sách, transcript, ảnh và thống kê. Owner dùng `settings.write` cho bật/tắt trợ lý, quota AI/ngày, ghi nhớ gần đây và hiểu cách nói tự nhiên. Không có chat người thật, hàng chờ, `chat.reply`, email/chuông handoff, giờ trực, câu chào/gợi ý hoặc công tắc ảnh. | `OWNER_CONFIRMED_2026-08-30` | `CHAT_RULE_010`, `040`, `045`–`047`, `049`, `057`–`059`, chat/settings admin screens |
| Menu admin | admin, backend, web | Menu-item CRUD and reorder inside the single primary system slot (`primary`). Header category items use the level-1 category `image` as a 24×24 current-colour icon; level-2/level-3 items have no icon, and a missing image does not remove the menu item. Legacy system slots (`footer`, `guide`, `policy`) have been removed (with footer/guide hardcoded, and policy made static on the storefront). Menu containers themselves are system-defined — admins cannot create new locations or delete the system slot. | `OWNER_CONFIRMED_2026-08-29` | `AdminMenuController.java`, `AdminMenuService.java`, `HeaderMenu.tsx`, `MenuLocations.java`, tests |
| Content admin (Tin tức) | admin, backend | Article (blog/news) CRUD only. Static page/guide builders and the entire content-category feature are removed; product categories are unaffected. Menu label + screen title is "Tin tức". Gated by `content.read` / `content.update`. | `CONFIRMED_FROM_OWNER_DECISION` (2026-08-03) | `AdminContentController.java` (articles only), `ContentDetailScreen.jsx` |
| Homepage sliders admin | admin, backend, web | Xem bằng `sliders.read`; toggle/reorder/delete bằng `sliders.write`. Tạo mới hoặc full-edit cần đồng thời `sliders.write`, `products.read`, `media.read`; upload ảnh mới đi qua Media API và cần `media.write`. Picker bị khóa trước khi gọi API nếu thiếu quyền. | `OWNER_CONFIRMED_2026-07-31` | `SliderListScreen.jsx`, `AdminSliderController.java`, `SliderReadService.java`, `HeroSlider.tsx` |
| Homepage videos admin | admin, backend, web | Xem bằng `home_videos.read`; sửa URL/provider/trạng thái/thứ tự bằng `home_videos.write`. Hệ thống tự lấy tối đa 15 video mới nhất từ đúng `youtube_url` mỗi đêm, thêm mới lên đầu và public tối đa 10 video đang bật; không tự bật lại video owner đã tắt và không xoá video bị đẩy ra. Cấu hình kênh được đặt ngay trong màn Video trang chủ, đọc/ghi lần lượt bằng `settings.read`/`settings.write`. Chọn video/thumbnail nội bộ cần `media.read`, upload file mới cần `media.write`; các nguồn nhập tay hiện hữu không đổi. | `OWNER_CONFIRMED_2026-08-31` | `HOME_VIDEO_RULE_001`–`003`, `HomeVideoListScreen.jsx`, `AdminHomeVideoController.java`, `HomeVideoSyncScheduler.java` |
| Homepage highlights admin | admin, backend, web | Xem bằng `home_highlights.read`. Picker và save dùng workspace composite `home_highlights.write AND products.read`; product imagery được đọc từ catalog, không sao chép vào module. | `OWNER_CONFIRMED_2026-07-31` | `HomeHighlightsScreen.jsx`, `AdminHomeHighlightsController.java`, `HomeHighlightsService.java` |
| Admin users | admin, backend | Xem danh sách bằng `admin-users.read`; invite/sửa/reset/status bằng `admin-users.write`. Tải/chọn/gán role cần `roles.read`; vì vậy `admin-users.write` phụ thuộc cả `admin-users.read` và `roles.read`. | `OWNER_CONFIRMED_2026-07-31` | `AdminUsersScreen.jsx`, `AdminAdminUsersService.java`, `STATE_MACHINES.md` §5 |
| Roles and permissions | admin, backend | Xem catalog/role bằng `roles.read`; tạo/sửa/xóa role bằng `roles.write`, trong đó `roles.write` phụ thuộc `roles.read`. Roles API từ chối permission set thiếu dependency; UI tự đóng dependency và giữ toàn bộ self-lockout/SUPER_ADMIN/system-role guards. | `OWNER_CONFIRMED_2026-07-31` | `RolesScreen.jsx`, `AdminRoleService.java`, `PERMISSION_MATRIX.md` Role Governance |
| ~~Static CMS pages + Guide-page builder~~ | — | **REMOVED (2026-06-24).** Module pages và trình dựng trang Hướng dẫn vẫn không được khôi phục. Tám route hiện hành và sidebar ba chính sách vẫn cố định trong web. Quyết định owner ngày 2026-09-01 thay thế ngoại lệ ngày 2026-08-23: tiêu đề/thân bài Bảo hành và Đổi trả nay nằm trong bốn resource backend cố định, không còn thuộc nhóm setting `store_policy`; khối liên hệ vẫn lấy động từ Contact. Đây không phải khôi phục CMS/page builder. | `REMOVED` | `V271__drop_pages_and_guide_page.sql`, `V1078__freeze_store_policy_content.sql`, `POLICY_PAGE_RULE_003` |
| Inventory supporting capability | admin, backend | Không có module Kho độc lập. `inventory.read` chỉ gate API/topic và widget tồn kho trên Dashboard; thiếu quyền này không chặn Dashboard và frontend không fetch/subscribe tồn kho. Còn/Hết vẫn được sửa trong Product form bằng `products.update`; không khôi phục `inventory.write`. | `OWNER_CONFIRMED_2026-07-31` | `AdminInventoryController.java`, `AdminInventoryService.java`, `DashboardScreen.jsx`, `PermissionCatalog.java`, `V364__remove_orphan_inventory_write_permission.sql` |
| Returns/refunds admin | — | Removed platform-wide 2026-06-23 (return/RMA + refund features dropped). | `REMOVED` | — |
| Redirect admin | admin, backend | Redirect CRUD and internal redirect support | `CONFIRMED_FROM_CODE` | `AdminRedirectController.java`, `SecurityConfig.java` |
| ~~POS~~ | — | **REMOVED (owner decision 2026-06-23, online-only).** The point-of-sale / walk-in module was dropped entirely: admin POS screen, `POST /admin/pos/orders` + `GET /admin/pos/products/search` endpoints, `AdminPosController` / `PosOrderService`, and the `pos.read` / `pos.write` / `pos.price_override` / `pos.sell_below_cost` permissions. BigBike now only takes online orders. | `REMOVED` | — |
| ~~WordPress migration toolchain~~ | — | **REMOVED (2026-08-18, dọn kho mã sau khi cutover hoàn tất).** Toàn bộ gói `migration/wordpress/**` bị xóa: parser dump WP, mapper/importer/normalizer, `MediaCopyRunner`, `WordPressMigration*Runner`, `LiveMigrationPreflightCli` / `LiveMigrationExecutionCli` và các planner `Live*`, khối cấu hình `bigbike.migration.wordpress.*` trong `application.properties`, bộ test `Phase2*` + fixtures `src/test/resources/fixtures/wordpress/`. Cutover WordPress → BigBike đã xong từ 2026-06, công cụ chỉ dùng một lần. **Giữ lại** duy nhất `ProductSlugGenerator`, chuyển sang `com.bigbike.bigbike_backend.util` vì tính năng nhập sản phẩm từ file (`ProductImportService`) và tạo thuộc tính (`AdminAttributeService`) đang dùng. Runbook + data contract giữ nguyên làm hồ sơ lịch sử, đã gắn nhãn REMOVED. | `REMOVED` | Xem lịch sử git trước 2026-08-18; `docs/engineering/LIVE_MIGRATION_RUNBOOK.md` |
| Admin order WebSocket | admin, backend | Subscribe to `/topic/admin/orders` for order events | `CONFIRMED_FROM_CODE` | `WebSocketConfig.java`, `AdminOrderWsService.java`, `adminWebSocket.js` |
| Audit logs admin | admin, backend | Read-only paginated activity log with filters (actorType, resourceType, action, date range). Enriches actor name and resource label. Permission: `audit-logs.read`. | `CONFIRMED_FROM_CODE` | `AdminAuditLogController.java`, `AdminAuditLogService.java` |
| ~~Shipping admin~~ | — | **REMOVED (owner decision 2026-06-23, `SHIP_RULE_001`).** The shipping zone/method CRUD module was dropped entirely: admin screen, `/api/v1/admin/shipping/*` endpoints, `shipping_zones`/`shipping_methods` tables (migration `V264`), and the `shipping.read`/`shipping.write` permissions. Online orders carry no shipping fee. | `REMOVED` | `V264__remove_shipping_methods.sql` |
| Notification center admin | admin, backend | Kho chuông dùng chung cho thông báo đơn, bản tin đơn vận hành quá hạn và bản tin hết hàng; danh sách, số chưa đọc và mốc đã đọc vẫn riêng theo tài khoản. Bản tin đơn quá hạn gộp một lần/ngày, không lặp từng đơn. Đơn hàng cần `orders.read`, bản tin tồn kho cần `inventory.read`; rows older than six months are removed while de-dup ledgers remain. | `OWNER_CONFIRMED_2026-08-31` | `AdminNotificationController.java`, `AdminNotificationService.java`, `NOTIFICATION_RULE_001`–`002`, `STOCK_ALERT_RULE_001`–`006` |
| Dashboard admin (Tổng quan) | admin, backend | Dashboard dùng `orders.read`. Cảnh báo/badge/trạng thái/đơn gần đây chỉ tính đơn vận hành; tiền và xếp hạng vẫn tính cả lịch sử theo rule tài chính hiện hành và phải công bố phạm vi. | `OWNER_CONFIRMED_2026-08-31` | `ORDER_RULE_015`, `REPORT_RULE_012`, `AdminDashboardService.java`, `DashboardScreen.jsx` |
| Reports admin (Báo cáo) | admin, backend | Analytics dùng `reports.read`; export dùng `reports.export`. Mọi số liệu đơn giữ cách tính hiện hành, tính cả lịch sử và ghi rõ phạm vi; file đơn thô ghi scope từng dòng/header. | `OWNER_CONFIRMED_2026-08-31` | `REPORT_RULE_012`, `AdminReportService.java`, `ReportsScreen.jsx` |

## Inventory And Receiving Subdomains

| Subdomain | Current implementation | Status | Evidence |
|---|---|---|---|
| Stock movement timeline | **Dormant (V261).** Movement types `IN`/`OUT`/`ADJUSTMENT`/`RETURN` still exist in schema but no movements are written for sales/restores; inventory is a boolean availability toggle. | `CONFIRMED_FROM_CODE` | `AdminInventoryService.java`, `V261__inventory_availability_toggle.sql` |
| Stock movement serials | Removed (serial feature removed platform-wide 2026-06-23); the `stock_movement_serials` table was dropped. Quantity adjustments themselves were also removed (V261 — boolean availability toggle replaced them). | `REMOVED` | `V259__remove_serial_management.sql`, `V261__inventory_availability_toggle.sql` |
| Stock receipt schema | Dropped in V120 (business decision 2026-05-16). Tables were schema-only and never built. (Inventory is now a boolean availability toggle — no receiving flow, V261.) | `REMOVED` | `V120__drop_stock_receipt_tables.sql` |
