# Module Catalog

## Public Platform Modules

| Module | Surface | Current implementation | Status | Evidence |
|---|---|---|---|---|
| Catalog browse | web, backend | Products, categories, brands, articles, settings, menus, sliders, home videos. (Static CMS pages + guide-page modules removed 2026-06-24 — 10 info/policy pages now hardcoded in `bigbike-web`.) Nội dung sản phẩm + danh mục + thương hiệu song ngữ Việt/Anh (V136/V137) — đọc theo `lang`, tiếng Anh tùy chọn, fallback về tiếng Việt theo từng trường. Storefront `bigbike-web` dùng next-intl + cookie `NEXT_LOCALE`. Đợt 3 (V137): bilingual category/brand đã hoàn tất. | `CONFIRMED_FROM_CODE` | public controllers, `public-api.ts`, `V136__add_product_bilingual_content.sql`, `V137__add_category_brand_bilingual_content.sql`, `bigbike-web/i18n/request.ts`, `bigbike-web/components/layout/LanguageSwitcher.tsx` |
| Listing page hero banners | web, backend, admin | Admin-managed hero (image + title + description + kicker) on the **listing pages** (`/san-pham`, `/brands`, `/tin-tuc`) — backed by the `public_hero` settings group; each listing page has its own background AND gear illustration, falling back to shared defaults. Managed in admin via the dedicated **Banner trang** screen (`BannerScreen.jsx`) with a live composite preview. (CMS-page hero via `PageEntity` removed 2026-06-24 — info/policy pages now static in web.) | `CONFIRMED_FROM_CODE` | `SettingDefinitionRegistry` `hero_*` keys, `BannerScreen.jsx`, `bigbike-web/components/wp/WpCategoryHero.tsx` |
| Search | web, backend | `GET /api/v1/search` and `GET /api/v1/search-suggest` | `CONFIRMED_FROM_CODE` | `PublicSearchController.java`, `SecurityConfig.java`, clients |
| Cart | web, backend | Guest/customer cart with CSRF-protected mutations | `CONFIRMED_FROM_CODE` | `CartController.java`, `CartService.java`, tests |
| Checkout | web, backend | Cart checkout and quick buy with idempotency. No shipping-method or payment-method choice; orders carry no shipping fee (owner decision 2026-06-23, `SHIP_RULE_001`). | `CONFIRMED_FROM_CODE` | `CheckoutService.java`, tests, clients |
| Customer account | web, backend | Profile, addresses, orders | `CONFIRMED_FROM_CODE` | customer controllers, clients |
| Wishlist | — | **REMOVED (owner decision 2026-07-06).** Customer wishlist ("Sản phẩm yêu thích") dropped entirely — backend API + `wishlist_items` table removed; never had a web UI (no client ever called it). | `REMOVED` | `V320__drop_wishlist_items_table.sql` (drops table V103); deleted `CustomerWishlistController.java` / `WishlistItemEntity.java` / `WishlistItemJpaRepository.java` + `CustomerWishlistApiTest.java` |
| Product comparison | — | **REMOVED (owner decision 2026-07-06).** "So sánh sản phẩm" dropped — was previously documented as a browser-local compare feature but **no implementing code ever existed** in `bigbike-web` (all 4 cited files are absent); the owner confirmed not to build it. | `REMOVED` | (none — feature was never implemented in code) |
| Vietnam address lookup | web, backend | Province -> district -> ward lookup | `CONFIRMED_FROM_CODE` | `VnAddressController.java`, web helpers |

## Admin Platform Modules

| Module | Surface | Current implementation | Status | Evidence |
|---|---|---|---|---|
| Catalog admin | admin, backend | Product/category/brand CRUD and related reads. PDP authoring (V229–V231): mô tả dạng khối song ngữ (Việt + Anh đều dùng trình dựng khối), thông số "đưa lên ô nổi bật" (tối đa 4), và **quản lý tab PDP theo từng sản phẩm** (ẩn/hiện, đổi thứ tự, đổi tên, thêm tab tự do dựng bằng khối) — xem trước bố cục thật qua live preview. **Nhập sản phẩm hàng loạt từ file JSON** (V2026-07-04; **bỏ hẳn CSV 2026-07-06** — 1 file JSON chứa trọn hàng hoá + nội dung): validate (dry-run, không lưu) + commit (ghi thật, mỗi dòng transaction riêng để lỗi 1 dòng không huỷ cả mẻ), upsert theo SKU/slug, danh mục/thương hiệu tra theo slug (không tự tạo), biến thể đối chiếu theo SKU để bảo toàn lịch sử tồn kho khi cập nhật lại. Kèm export toàn bộ catalog thành **JSON đầy đủ** (đúng shape file nhập `UpsertProductRequest[]`) để tải về sửa rồi nhập lại (khác với export báo cáo CSV của module Reports). | `CONFIRMED_FROM_CODE` | `AdminCatalogController.java`, `AdminProductImportController.java`, `ProductImportService.java`, `ProductDetailScreen.jsx`, `ProductTab`/`ProductTabsConverter`, migrations `V229`–`V231`, `V316` |
| Order admin | admin, backend | Order list/detail/status/payment/note workflows | `CONFIRMED_FROM_CODE` | `AdminOrderController.java`, admin app |
| Customer admin | admin, backend | Customer list/detail/update | `CONFIRMED_FROM_CODE` | `AdminCustomerController.java` |
| Media admin | admin, backend | Upload/list/detail/update/delete/restore media | `CONFIRMED_FROM_CODE` | `AdminMediaController.java`, `AdminMediaService.java` |
| Settings admin | admin, backend | Site settings read/update | `CONFIRMED_FROM_CODE` | `AdminSettingsController.java`, tests |
| Menu admin | admin, backend | Menu-item CRUD and reorder inside the single primary system slot (`primary`). Legacy system slots (`footer`, `guide`, `policy`) have been removed (with footer/guide hardcoded, and policy made static on the storefront). Menu containers themselves are system-defined — admins cannot create new locations or delete the system slot. | `CONFIRMED_FROM_CODE` | `AdminMenuController.java`, `MenuLocations.java`, tests |
| Content admin (Tin tức) | admin, backend | Article (blog/news) CRUD + content categories. After V275, static page/guide builders are removed, category picker is removed, and all articles are automatically mapped to the default 'tin-tuc' category. Menu label + screen title is "Tin tức". Gated by `content.read` / `content.update`. | `CONFIRMED_FROM_CODE` | `AdminContentController.java` (articles only), `ContentDetailScreen.jsx` |
| ~~Static CMS pages + Guide-page builder~~ | — | **REMOVED (2026-06-24).** Module "Trang tĩnh CMS" (pages) và trình dựng trang Hướng dẫn dropped: admin pages CRUD + `reference/pages` + `GET`/`PUT /admin/guide-page`, public `GET /api/v1/pages` + `/api/v1/guide-page`, tables `pages` + `guide_page_layout` (V271), `PageEntity` / `GuidePageLayoutEntity` / domain `Page`/`PageType`/`GuideEntry`, and the WordPress `PageImporter`. The 10 info/policy pages are now hardcoded in `bigbike-web` (`lib/content/static-pages.json`). | `REMOVED` | `V271__drop_pages_and_guide_page.sql` |
| Inventory admin | admin, backend | Standalone "Kho hàng" admin screen removed 2026-06-23 (grouped list / availability-toggle UI / CSV export gỡ khỏi admin). Còn/Hết is now toggled per-variant inside the product edit form (`isAvailable`, via `products.update`). Backend summary endpoint (`totalItems`/`inStockCount`/`outOfStockCount`) is kept and powers the Dashboard out-of-stock alert (links to Products filtered `stockState=OUT_OF_STOCK`). Grouped-list / `PATCH .../availability` / CSV endpoints remain in `AdminInventoryController` but are no longer surfaced in admin UI. Quantity model removed V261; only Còn/Hết (no "sắp hết"). | `CONFIRMED_FROM_CODE` | `AdminInventoryController.java`, `AdminInventoryService.java` |
| Returns/refunds admin | — | Removed platform-wide 2026-06-23 (return/RMA + refund features dropped). | `REMOVED` | — |
| Redirect admin | admin, backend | Redirect CRUD and internal redirect support | `CONFIRMED_FROM_CODE` | `AdminRedirectController.java`, `SecurityConfig.java` |
| ~~POS~~ | — | **REMOVED (owner decision 2026-06-23, online-only).** The point-of-sale / walk-in module was dropped entirely: admin POS screen, `POST /admin/pos/orders` + `GET /admin/pos/products/search` endpoints, `AdminPosController` / `PosOrderService`, and the `pos.read` / `pos.write` / `pos.price_override` / `pos.sell_below_cost` permissions. BigBike now only takes online orders. | `REMOVED` | — |
| Admin order WebSocket | admin, backend | Subscribe to `/topic/admin/orders` for order events | `CONFIRMED_FROM_CODE` | `WebSocketConfig.java`, `AdminOrderWsService.java`, `adminWebSocket.js` |
| Audit logs admin | admin, backend | Read-only paginated activity log with filters (actorType, resourceType, action, date range). Enriches actor name and resource label. Permission: `audit-logs.read`. | `CONFIRMED_FROM_CODE` | `AdminAuditLogController.java`, `AdminAuditLogService.java` |
| ~~Shipping admin~~ | — | **REMOVED (owner decision 2026-06-23, `SHIP_RULE_001`).** The shipping zone/method CRUD module was dropped entirely: admin screen, `/api/v1/admin/shipping/*` endpoints, `shipping_zones`/`shipping_methods` tables (migration `V264`), and the `shipping.read`/`shipping.write` permissions. Online orders carry no shipping fee. | `REMOVED` | `V264__remove_shipping_methods.sql` |
| Notification center admin | admin, backend | Persistent admin notifications (`admin_notifications`, V102) — list unread, mark-read, mark-all-read. Complements the `/topic/admin/orders` WebSocket feed so offline admins do not miss events. Gated by `orders.read`. | `CONFIRMED_FROM_CODE` | `AdminNotificationController.java`, `AdminNotificationService.java`, `V102__create_admin_notifications_table.sql` |
| Dashboard admin (Tổng quan) | admin, backend | Dashboard summary including KPI aggregates, daily revenue chart series, order status breakdown, recent orders list, and top products by revenue. Gated by `orders.read` permission. | `CONFIRMED_FROM_CODE` | `AdminDashboardController.java`, `AdminDashboardService.java`, `DashboardScreen.jsx` |
| Reports admin (Báo cáo) | admin, backend | Analytics reports (PeriodSummary, daily revenue chart series, top products, top customers) and export functionality to CSV (orders, customers, products). Gated by `reports.read` and `reports.export` permissions. | `CONFIRMED_FROM_CODE` | `AdminReportController.java`, `AdminReportService.java`, `ReportsScreen.jsx` |

## Inventory And Receiving Subdomains

| Subdomain | Current implementation | Status | Evidence |
|---|---|---|---|
| Stock movement timeline | **Dormant (V261).** Movement types `IN`/`OUT`/`ADJUSTMENT`/`RETURN` still exist in schema but no movements are written for sales/restores; inventory is a boolean availability toggle. | `CONFIRMED_FROM_CODE` | `AdminInventoryService.java`, `V261__inventory_availability_toggle.sql` |
| Stock movement serials | Removed (serial feature removed platform-wide 2026-06-23); the `stock_movement_serials` table was dropped. Quantity adjustments themselves were also removed (V261 — boolean availability toggle replaced them). | `REMOVED` | `V259__remove_serial_management.sql`, `V261__inventory_availability_toggle.sql` |
| Stock receipt schema | Dropped in V120 (business decision 2026-05-16). Tables were schema-only and never built. (Inventory is now a boolean availability toggle — no receiving flow, V261.) | `REMOVED` | `V120__drop_stock_receipt_tables.sql` |
