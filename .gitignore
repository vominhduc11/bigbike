# Documentation (local only, not pushed to remote)
# docs/

# Legacy reference data (root-level only — KHONG chan themes/bigbike, wp-content/uploads...)
/bigbike/
/uploads/

# Design assets (large binary/zip files, not for git)
*.zip
Bigbike Design System-web/
BigBike Design System-admin.zip

# Java build output
bigbike-backend/target/

# Test/screenshot output (root-level only)
/output/

# Local backup output (root-level only)
/backups/

# Local agent/tooling state
# .claude/

# Temporary local dev data (Docker volume mounts, pgdata, etc.)
.tmp/

# Environment files
.env
.env.*
!.env.example
!.env.vps.example

# Node / Next.js / frontend build output
node_modules/
.next/
out/
# Build output cua frontend app — neo cu the, KHONG chan 'dist' trong public/wp-content (theme/plugin assets goc)
bigbike-admin/dist/
bigbike-web/dist/
coverage/
*.tsbuildinfo

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# OS / editor files
.DS_Store
Thumbs.db
.vscode/
.idea/
