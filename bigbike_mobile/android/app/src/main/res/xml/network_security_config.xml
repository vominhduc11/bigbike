<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext HTTP to local dev hosts only.
  10.0.2.2  — Android emulator loopback (maps to host machine localhost)
  localhost  — physical device / iOS parity
  These domains are only reachable during debug builds; release builds use HTTPS.
-->
<network-security-config>
    <!-- Deny cleartext HTTP for every host by default — production traffic must be HTTPS. -->
    <base-config cleartextTrafficPermitted="false" />
    <!-- Narrow exception: cleartext is permitted ONLY for the local dev hosts below.
         Release builds never resolve to these hosts (see AppConfig.apiBaseUrl). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
