# ⚠️  KHÔNG PHẢI FILE ĐANG CHẠY TRÊN MÁY CHỦ (kiểm chứng 2026-08-06).
#
# Máy chủ nạp cấu hình từ /etc/nginx/sites-enabled/ -> /etc/nginx/sites-available/<host>
# (tên file KHÔNG có đuôi .conf, và bigbike.vn là symlink tới bigbike.vn.nextjs).
# Nội dung bên đó KHÁC file này. Sửa file này KHÔNG có hiệu lực gì trên production.
#
# Copy đè file này lên máy chủ sẽ XOÁ MẤT các sửa đổi đang chạy thật, trong đó có:
#   - location /ws trên admin.bigbike.vn (WebSocket: chuông báo đơn + thông báo bảo trì)
#   - error_page 502/503/504 -> trang xin lỗi khách ở /var/www/bigbike-static/maintenance/
# Luôn `diff` với bản live trước khi triển khai. Xem DEPLOYMENT_GUIDE.md §Nginx.

# Shared Nginx configuration for bigbike.vn production infrastructure.
# Deploy to: /etc/nginx/conf.d/shared-config.conf

# ==================== Rate Limits ====================
# Nginx otherwise uses 503 for limit_req rejections, which collides with the
# maintenance/outage contract. Every limiter rejection must be a real 429.
limit_req_status 429;
limit_conn_status 429;
limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
limit_req_zone $binary_remote_addr zone=general_limit:10m rate=30r/s;
limit_conn_zone $binary_remote_addr zone=per_ip_connections:10m;

# Privacy-safe rate-limit audit stream. It deliberately excludes client IP, query string and
# dynamic URI. Feed this file to the existing log pipeline/Prometheus log metric exporter.
log_format bigbike_rate_limit_json escape=json
    '{"timestamp":"$time_iso8601","host":"$host","method":"$request_method",'
    '"status":$status,"requestId":"$request_id"}';
map $status $bigbike_rate_limit_log {
    default 0;
    429 1;
}

# ==================== WebSocket Upgrade Mapping ====================
map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

# ==================== Upstreams ====================
upstream api_backend {
    server 127.0.0.1:8080 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

upstream main_frontend {
    server 127.0.0.1:3000 max_fails=3 fail_timeout=30s;
    keepalive 32;
}

upstream admin_frontend {
    server 127.0.0.1:4000 max_fails=3 fail_timeout=30s;
    keepalive 16;
}
